# Weekly roadmap - week of 2026-08-15

> For the implementing agent (Opus). Fable reviews per batch; the user runs
> UAT sessions. Review baseline lives in Documentation/Process/HANDOFF.md
> (pass 7; everything after it is this week's work). Coordinate build
> windows - two agents share the machine, concurrent builds corrupt gcm.

## Opus build queue, in order

1. **MSAA P1g** - DONE 2026-08-15 (ba0ec1f5 and parents), awaiting Fable
   review. Nothing further; MSAA P1 closes when the review passes.
2. **Property animation** (task #129) - the main build of the week.
   Spec: Documentation/Specs/property-animation.md. Build phased as
   specced; land tests with every phase; Systems docs update in the same
   commit as behavior.
   STATUS 2026-08-16: BUILT - all 7 phases (A curve math, B data model +
   binding, C cooked resource, D pipeline asset+builder, E engine
   component+manager, G scripting, F editor clip page) + CurveCanvas
   adopted in the clip page. Green clang+gcc; 222 new assertions.
   PHASE H DONE 2026-08-16 (user-prioritized in-scene authoring mode): H1 panel
   seam (editor.app:tool_panel, keyed by tool id, NOT on IViewportTool) + ScenePage
   dock rail -> H2 shared ClipEditorView over IClipEditorHost -> H3
   PropertyAnimationTool + docked panel + add-from-selection -> H4 live preview
   (runtime WriteBinding path, transient snapshot/restore via new foundation
   ReadBinding, disabled outside EDIT, overlay marker). Green clang + gcc; editor
   links on both; +~225 assertions across the H suites. Remaining = the user's
   on-screen visual pass (smoke-checklist in-scene-authoring section) + the paused
   focused-page follow-ups (physics-rule test, demo-scene UAT).
3. **Asset variants + texture compression P1-P2** (task #133).
   Spec: Documentation/Specs/asset-variants.md (read in full - the
   capability-profile keying and the authored `usage` field were added
   2026-08-15 after external review; mobile-web is IN scope, P3).
   P1 = vendor bc7enc/rgbcx + astcenc (approved), Texture.Compression lib,
   BCn cook + policy table + import knob, upload verified Vulkan AND
   WebGPU. P2 = the per-target variant axis (lazy DBs + invariant
   copy-forward). P3 (mobile-web ASTC pak + boot selection) only after the
   user's mobile-web smoke has run (UAT checklist, 2026-08-15 section).
   STATUS 2026-08-15: P1 COMPLETE (all of a-h). bc7enc + astcenc vendored,
   Texture.Compression lib + policy table, builder v3 encodes, block-aware
   upload, cook size-drop test, BC1/BC7/BC5 sample probe green on
   Vulkan+WebGPU. ASTC DEVELOPED not deferred (user directive): functional
   astcenc encode + policy + WebGPU/Vulkan format maps + gated GPU probe +
   PSNR test. Texture-page Usage/Compression knobs + normal-map import
   heuristic.
   P2 + P3 BUILT (Fable-reviewed design). Cook: Variance/CookTarget/recipe-salt/
   copy-forward + ContentDatabase::CopyContentForward + `Tools.Cook --target`
   (per-target DB at sibling Cooked-<id>/). Export: ExportContent packs a
   content-variant list; the Web preset ships Content-bc.pak + Content-astc.pak
   (each complete, reachability scanned once) via CookVariantTargets reusing
   CookForTarget; desktop stays byte-identical (executable test). Web boot:
   WebMain probes the adapter's bc/astc and fetches the matching pak as
   Content.pak (builds for wasm). Cook + export tests green (247 + 796).
   REMAINING: (a) manifest built manifest-free (pak-name convention) - Fable/
   user sign-off on that deviation; (b) BROWSER/mobile functional check = UAT
   (gated on the Decision-4 mobile-web smoke precursor).
4. **MCP P1 resume** (task #131) - if the week has room. Build order is
   the RESUME POINT v2 list in Documentation/Specs/mcp-agent-access.md;
   item 1 (scene_read/write/validate + prefab read/write) first.

Fillers if blocked waiting on review: SDL static (#119, sdl-static.md).
(Camera preview #118 was ALREADY BUILT 2026-08-03 - stale task-list entry;
only the on-screen user verify remains, in the UAT editor-misc session.)

Do NOT build this week: mesh LOD (Documentation/Specs/mesh-lod.md is
PREPARED for a later week; meshoptimizer approved but unscheduled),
anything on the deferred-by-design list, Wren retirement (user-deferred),
physics debug draw / issues I1 (user-parked to the physics track).

## Fable (review + hands-on between review gates)

- Review each Opus batch: full two-compiler battery, ASAN when
  script/executor code changes, rulings recorded in the spec docs,
  HANDOFF.md pass record per milestone.
- Issues burn-down (task #130): I10 Windows path separators, I8 swapchain
  fallback hardening + logging, I3 prefab-page design resolution, I7 audio
  clip page pause/volume, smoketest-fixes #9 sandbox game-UI samples.
  Stretch: #128 I4 remainders (eviction + partial header parse).

## User (UAT)

- Smoke checklist burn-down (task #132): list audited 2026-08-15 (131
  open, session groupings in the audit; name-substitution table now at the
  top of Documentation/UAT/smoke-checklist.md). Suggested first session:
  bespoke asset pages (17 items). Boot-splash visual verify closes #123.
- Mobile-web smoke (new 2026-08-15 section in the checklist): open the
  current web export on Chrome/Android + iOS Safari; record adapter
  compression features + a memory/framerate baseline. Gates asset-variants
  P3.

## UAT findings - 2026-08-15 session (task #132 burn-down)

Triaged from the smoke checklist's raw-notes tail (removed from there; fixed items became regression
checks in the checklist). `[x]` = fixed this session, `[ ]` = queued. Grouped by area.

### Fixed (user to re-confirm on next UAT)
- [x] UI blanks (but stays interactive) on a huge draw list - the asset-browser bulk `.fbx` drop AND
  the model-manifest page (Sponza). Same cause: the VG vertex buffer overflowed a fixed cap and
  Prepare returned an empty slice. Buffers now grow on demand (9e35fb51). If the manifest still blanks,
  suspect the separate 64-uniform-slot cap instead.
- [x] Empty content group pruned on reopen - the DB rebuilds by scanning folders and CreateGroup was
  in-memory only; a user-created group now materializes a real directory (b375b849 + 178c4626).

### Audio (aligns with Fable I7 - audio clip page)
- [x] Sound cue: a freshly-created cue immediately fails to cook ("no playable variant") - poor UX;
  an empty cue should not auto-cook / should message clearly. DONE (Opus 2026-08-18, Fable-ruled -
  Specs/audio-cue-empty-cook.md, ruling A+D+health): the builder now cooks an empty cue to a valid
  ZERO-VARIANT product instead of failing (Version 1->2; runtime already no-ops it); the SoundCuePage
  shows a persistent "Empty cue - assign at least one clip." hint that clears on assignment; and
  project_health warns on emptyCues (does not flip `sound`). Tests: empty cue cooks -> empty product +
  ResolveSoundCue -1.
- [x] Audition: cannot pause/stop; clicking audition repeatedly stacks overlapping playback. DONE
  (Opus 2026-08-18): the SoundCuePage Audition() now stops the prior voice first (no more stacking),
  and gained Stop + Pause/Resume buttons (Engine Stop / SetPaused, mirroring the audio-clip page).
- [x] AudioSource component: too many fields; hide the irrelevant ones with `visibleWhen` and a
  clip-vs-cue mode/type discriminant. DONE (Opus 2026-08-18): added an AudioSourceType {Clip,Cue}
  discriminant (clip Ref shows only in Clip mode, cue only in Cue mode via visibleWhen) + the 9
  spatial-only fields hidden for a non-spatial source (visibleWhen spatial), the physics RigidBody
  pattern. Runtime uses sourceType (replaces implicit "cue wins"); Serialize v4 with pre-v4 migration
  (cue assigned -> Cue). Round-trip test pins the discriminant.

### Scene / inspector / hierarchy
- [x] Marking an entity inactive should stop it BOTH rendering and simulating. NOTE (Opus 2026-08-17):
  NOT a quick win - no system honors OnEntityActiveChanged and the render extractor never checks
  IsActive, so it needs gates across the render extraction AND each sim system (physics/scripts/
  animation/particles). Scope as its own item.
  SPEC'D (Fable 2026-08-17): Documentation/Specs/entity-active-state.md - effective-active cache
  in foundation.scene, poll gates in extraction + each sim domain (latch pattern for stateful
  ones), scene-starts-inactive covered by construction. Build order P1-P4 inside.
  BUILT (Fable 2026-08-18): all four phases shipped + battery-green (see the spec's BUILT
  section for deviations); remaining = the user's on-screen verify (toggle the inspector
  Active checkbox on a mesh/light/scripted/physics entity in Simulate).
- [ ] Easier scene-settings selection - open the scene inspector without clicking empty viewport space.
- [x] Arrow-key navigation in the hierarchy does not update the inspector. FIXED (5a714819): the
  hierarchy subscribes to SelectionModel::OnSelectionChanged (keyboard nav moves the ListView selection
  without firing OnItemClick), guarded against the scene->tree sync loop.

### Bespoke asset pages (aligns with the bespoke-pages UAT group)
- [x] Animation clip page: no preview mesh. DONE (Opus 2026-08-18): a "Mesh" picker in the
  transport (SkinnedMeshAsset) adds a skinned mesh to the preview scene, deformed by an
  AnimationPlayer driven from the SAME m_time as the bone wireframe (SetCurrentTime -> feed
  MeshComponent.boneMatrices) - play/scrub drive both. Added a sun light so it's lit. Skeleton
  wireframe stays; mesh is optional (none = bones only). On-screen verify pending.
- [x] Mesh page: no material options. DONE (Opus 2026-08-18): a "Material" picker row atop the
  mesh-viewer stats panel - pick any MaterialAsset (AssetPickerDialog) to render the preview with,
  or "Default" to reset to the neutral PBR. Applied live to the preview MeshComponent (survives
  cook/hot-reload via the shared ApplyPreviewMaterial). On-screen verify pending.
- [x] Collision mesh page: no preview. DONE (Opus 2026-08-18): the dedicated
  CollisionShapeEditorPage now hosts a 3D viewport beside its controls (SplitView) drawing the
  cooked CollisionShape.outline as an unlit green wireframe; the orbit camera frames the shape
  when it first appears + reframes on re-cook (vertex-count change), and the Proxy auto-follows
  re-cooks so it stays live. Built on the NEW shared PreviewViewport substrate (see
  Documentation/Specs/editor-preview-viewport.md - the extraction this triggered): editor.camera
  (EditorCamera, graduated + FrameBounds) + editor.preview (PreviewViewport), and all six existing
  3D-preview pages (mesh/material/particle/animgraph/skeleton/clip) migrated onto it too, deleting
  their triplicated scaffolding. User-verified on screen 2026-08-18.
- [x] Animation graph: no preview mesh; nodes have no connectable ports; parameter type should drive
  the field editor (bool -> checkbox/dropdown); draw a skeleton preview with an on/off toggle (and a
  mesh-preview toggle, so skeleton-only is possible). DONE (Opus 2026-08-18), resolved per sub-part:
  * Preview MESH + skeleton/mesh TOGGLES (c0e537d4): a "Mesh" picker (SkinnedMeshAsset) binds a mesh
    the graph player skins each frame (GetSkinningMatrices -> MeshComponent), so the graph plays on
    the mesh; "Bones: on/off" + "Mesh: on/off" toggles (mesh via Scene::SetActive), skeleton-only
    stays possible. Reuses the clip-page skinned-preview pattern.
  * Param-type field editor: was ALREADY type-driven (BuildParameterInspector - Float->field,
    Int->field, Bool/Trigger->checkbox). No work needed.
  * Connectable ports: NOT a bug. Transitions are authored via right-click state -> "Make Transition"
    -> click target (Unity-Mecanim style; the canvas's StartLinkFrom rubber-band). State nodes are
    deliberately portless (ConnectionStyle::StraightNodeToNode). User chose (2026-08-18) to keep the
    right-click flow rather than add a drag-to-connect affordance - closed as working-as-designed.
  On-screen verify of the mesh preview pending.
- [x] Particle editor: inspector fields should be conditionally visible (the components' `visibleWhen`
  idea, applied here) - show only the fields applicable to the current render mode / emitter config.
  Concrete gap: MESH render mode has NO mesh picker (you cannot select a mesh). DONE (Opus 2026-08-17):
  render-mode-conditional inspector sections (d59bd64b) - Texture for textured modes, Flipbook for
  billboards, Trail for Trail, Mesh for Mesh; AND a mesh on the effect asset itself (3ff9aaec) - a
  per-system meshRef + meshScale mirroring textureRef through model/resource/render/cook, with a Mesh
  section picker (StaticMesh/SkinnedMesh) shown in Mesh mode. Broader follow-ups broken out below.
- [ ] Particle editor follow-ups (beyond the shipped render-mode-conditional sections): emitter-config-
  driven field visibility (not just render mode), an effect-level material for Mesh render mode, and
  the other "broader improvements beyond this" from the original UAT note.

### Import UX (asset browser + model)
- [x] Importer chooser when MULTIPLE importers match a file - e.g. image vs texture overlap on the
  same extension. DONE (8dd8718d): ImporterRegistry::FindAllFor + the asset browser pops a Label menu
  when more than one claims the extension; one match imports directly. Unit-tested.
- [x] Asset-browser right-click context menu needs an "Import..." entry: browse for a file, then show
  the matching import dialog (or a clear "no importer registered" message) - Sedulous had this. DONE
  (9f431db2): Import... in the background menu -> native ShowOpenFile (multi-select) -> ImportFile
  (which does the chooser / options dialog / no-importer warning).
- [x] Import dialog should let the user choose the destination group (default = active group) rather
  than always creating a folder (Sedulous had this). DONE (6130b8f0): the dialog's Into: row gained a
  Change... button -> a chooser of every project group; CommitImport imports into the chosen group.
- [x] Allow importing a model AS A SCENE (not only resources + prefab). DONE (9f84e205): "Generate
  scene" toggle beside "Generate prefab" (independent). Shared BuildModelScene builds the node->Scene;
  GenerateModelScene encodes it via SaveScene into a SceneDocument "Scene" (+ text stream) beside the
  manifest, regenerated by name/guid on re-import. Design call settled with the user: BARE scene (model
  nodes only, no default camera/light). Round-trip test covers hierarchy + mesh/material refs + reuse.
- [~] Design question: weigh unifying scenes and prefabs (the only real difference is scene systems) -
  pros/cons; may not be worth it. WRITTEN UP (Opus 2026-08-18): Documentation/Ideas/scene-prefab-unification.md (moved out of Specs/ 2026-08-18 - ideation, not a spec; Fable review appended)
  - WIP, awaiting Fable. Key findings: the question splits into three independent decisions (rename
  Scene->World; settings-as-components; one graph type), prior art (Bevy/ezEngine) says unification does
  NOT require "everything is a component", and the user wants the Scene->World rename regardless. Four
  options laid out; tentative lean = rename now + only unify when a concrete feature (nested/streamed
  scenes, per-placement overrides) asks for it.

### Project / scripting
- [ ] Script backend as a PROJECT setting - chosen at creation, changeable in project settings; gates
  which script options the editor shows + which VM runs (likely needs a project reload on change).

### Editor UX
- [x] Page action toolbar (Save/Undo/Redo/Discard) - reusable `app::PageToolbar` + `EditorPage::
  DiscardChanges`; POC on the Sound Cue page (ec35c8a2 + 8ba9580b). Toolbar + Save + Discard
  USER-CONFIRMED working 2026-08-15. Discard reverts to the last-saved snapshot.
- [ ] Sound Cue page: its edits do NOT go through the per-page command stack (they mutate the asset
  directly), so the toolbar's Undo/Redo stay disabled on this page. Route each edit through a blob
  command (like the bus-layout page) so Undo/Redo light up. Confirmed by the user 2026-08-15.
- [ ] Roll the page toolbar out to the other bespoke pages (bus-layout already has blob-command undo,
  so its Undo/Redo would work immediately).

### Export
- [x] Player executable needs the OS extension (Windows "Player" -> "Player.exe"). FIXED (5a714819):
  detail::PlayerOutputName ensures the target platform's exe suffix (Windows .exe, idempotent) + a unit
  test.
- [ ] A template build script to build export templates for all supported platforms quickly.

## Standing rules that bit recently (do not relearn)

- Builder/product schema change => Version() bump in the SAME commit.
- Serialized-identity renames => legacy-name fallback + test, same commit.
- Bulk data => binary sidecar stream, never inline in text envelopes.
- Pipeline targets (and their tests) stay UI-free.
- New third-party archives need distinct archive names (case-insensitive
  platforms).
- -j4 build cap; one target per cmake --build; never rm -rf Bin/.
- Stage explicit paths only; never git add -A (user files live untracked
  at the repo root and in EditorProject/).

## Queued for next week (2026-08-22)

- Editor extensibility seams (inspector action-row registry + external gizmo
  registration) - USER DECISION 2026-08-18, pre-seeded in week-2026-08-22.md.
  Origin: navigation P4b-3's finding that per-domain editor surfaces all
  couple into Editor.Scene's central dispatch.
