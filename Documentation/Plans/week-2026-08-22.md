# Weekly roadmap - week of 2026-08-22

> PRE-SEEDED (Fable, 2026-08-18). CLOSED OFF 2026-08-29: the completed items are
> kept below as this week's DONE record; the incomplete items were carried
> forward to week-2026-08-29 (listed at the bottom).

## DONE: HEADLINE - the first game (PaperKid) + its scripting prerequisite

game-ready-scripting2 P2-1..P2-5 + PaperKid P0/P1 shipped 2026-08-19..28 (verified against git).
- P2-1 run bus + Game `on<Event>` inbox (Messaging P2, e38a6046; GameInstance.cppm:321).
- P2-2 run facade `run.events()`/`run.loadScene*` with SceneLoader folded in (GameInstance.cppm:70-161;
  old SceneLoader deleted 70d950f8).
- P2-3/4 script HUD via the reflected `ui` facade + View handles (10030ce7) - a per-context `ui` root,
  not a literal `scene.ui` (UiScriptFacade.cppm:11).
- P2-5 `run.ui` + ScriptName aliases (ScriptSurfaceImpl.cpp:49, GameInstanceImpl.cpp:62).
- PaperKid P0 scripted seven-screen flow, play-in-editor (f2e18e7d), then P1 gameplay + script HUD
  (b923f7b5, ca3226d4; hud.sml). SampleProjects/PaperKid/ is the tracked project.
- Absorbs the "authored editor sample project" seed: PaperKid IS that project (tracked, editor-authored,
  dogfoods import/cook/inspector/gizmos/nav Bake/play-in-editor).

Still-open game DECISIONS (win condition, crash model, lives, aim discoverability) are design choices
tracked with paperkid.md, not this roadmap.

## DONE: editor-side domain debug draw

SHIPPED 2026-08-28. Edit-time collider wireframes drawn straight from component shape data + the entity
transform (NO physics world/bodies/sync) as editor IGizmoRenderers - RigidBody
(box/sphere/capsule/plane/heightfield/cooked-outline) + Character (controller capsule) (commit 7df03730),
plus the Joint gizmo (anchor cross + link line to the target + hinge/slider axis, commit 1fcce90e) -
registered through the central gizmo-builtins seam, drawn into the viewport DebugView, gated on a per-scene
"Show Colliders" toolbar toggle (SceneViewPref v3, GizmoContext.showColliders). DELIBERATELY separate from
the RUNTIME PhysicsSceneSettings.debugDraw (live Jolt bodies during simulation), which is untouched - user
ruling; no simulation gating. Traktor draw-from-desc model (vs Lumix's live edit-time world). Full I1 scope
(RigidBody + Character + Joint) done.

## DONE: mesh LOD P0-P3

All four phases landed (spec: mesh-lod.md).
- P0 vendor meshoptimizer + optimize every cooked static mesh: ThirdParty/meshoptimizer,
  Geometry.Pipeline/MeshOptimizeImpl.cpp (c496a491).
- P1 LOD wire + authored-LOD name collapse + coverage selection + hysteresis + component knobs +
  mesh-page dropdown: Foundation/Lod/LodMath.cppm, per-view selection MeshRenderer.cppm:271,
  MeshComponent.lodBias/forceLod (RenderComponents.cppm:86-87, reflected+serialized), mesh-page dropdown
  MeshPageImpl.cpp:280-311 (fe55a09d, 90f3b117; source v4 migration e4cf5b43).
- P2 auto meshopt_simplify chains at import (4224edaf).
- P3 polish - shadow coarsest-LOD rule, debug LOD tint, skinned (162dcb00, a507ef1b).
P4 (skinned LOD chains, per-instance bucketing, cross-fade, LOD-aware physics) stays DEFERRED per the spec.

## DONE: PaperKid editor-use feedback - completed items

Dogfooding items from SampleProjects/PaperKid/EditorUseFeedback.md shipped this week (verified against git).
Three were done but had NOT been re-marked when first triaged: the .sss theme page, the collision-group
refresh, and the stuck detached-window drag.
- [x] Loading-screen project setting: KEEP decision - loadingDocumentId brands the native boot / first-scene
  overlay pushed before the Game script exists; complementary by timing.
- [x] Store .sml/.sss payload as a linked Sources/ file like scripts (3dd2e39d).
- [x] Bespoke asset page for .sss theme files: UIThemePage + live preview (Editor.GameUI, 7076df2b).
- [x] String Guid ctor + right-click Copy GUID / Copy Path on an instance (e8874ed1).
- [x] PIE waits for the cook to finish before starting (20740bd4).
- [x] Exported player resolves `run::loadScene` / `run::requestExit` - facade registered via the composition
  root (f8dca21f; user re-export UAT was the noted follow-up).
- [x] Input gating: Tab in an editor field no longer leaks to the PIE game - one-app-keyboard focus
  arbitration (6836bad2, 5dd44bf1).
- [x] Adding a collision group refreshes the property immediately - RequestRebuild on OnAddGroup
  (InspectorViewImpl.cpp:660-672, 55277f27).
- [x] Stuck detached-window drag (float kept following the mouse after release) - stuck-drag watchdog
  force-delivers the release (UIRuntime.cppm:367, a69cc716).
- [x] HiDPI (all at 1.75 scaling): button heights squished / game-page debug panel off-screen / slider
  numeric fields squished - root cause Px->Dp (65 sites, 27 files), user-verified on-screen (37d36336).

## DONE: physics shape sweep / overlap queries

SHIPPED 2026-08-26 (91f0ad36 runtime + tests, 340bc273 / 5c90ab80 script surface).
Was seeded for week-2026-08-29 but landed same-week; moved here as the DONE record.
`PhysicsWorld::ShapeCast` + `ShapeOverlap` over Jolt's NarrowPhaseQuery with a
primitive QueryShape (sphere / box / capsule), RayCast-parity filtering, and the
script facade's `sphereCast` / `overlapSphere` returning the value-typed RayCastHit
/ a native entity array (the full overlap set). Headless Jolt tests cover
nearest-hit + miss + fraction/normal and exact overlap membership.

## Carried forward to week-2026-08-29 (incomplete)

Moved into week-2026-08-29's "Carried from week 2026-08-22 (incomplete)" section: editor extensibility
seams, script-backend-as-a-project-setting, the Sound Cue undo + toolbar roll-out, the AngelScript
delegate-funcdef cleanup, the week-08-15 asset-variants + user-UAT verifies, and six PaperKid feedback
items (organize new-component menu, drag instance into group, auto-seed default assets, Shift+multi-select
>2, WASD default input map, detached-window viewport render).
