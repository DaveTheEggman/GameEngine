# Weekly roadmap - week of 2026-08-29

> PRE-SEEDED (user, 2026-08-26). The full plan gets authored at week start;
> the items below are USER DECISIONS queued ahead of time - do not drop them
> when the plan is filled in.

## Seeded: FIX - blend trees do not fire animation events (user 2026-08-26) - DONE 2026-08-31

Origin: parity doc "known hole: blend trees don't fire events -
AnimationGraph.cppm:282". Confirmed in code: `BlendTree1D::FireEvents` (:282)
AND `BlendTree2D::FireEvents` (:419) are EMPTY no-ops ("blend trees don't fire
clip events"), so any state driven by a blend tree SWALLOWS every clip event
(footsteps, hit/attack frames, sound-cue triggers) - only a single
`ClipStateNode` fires events today. A real bug, not a feature; small + contained.

Fix: fire the events of the DOMINANT clip (the highest-weight entry at the
current parameter) - firing ALL blended clips would DOUBLE-FIRE shared events
(two walk clips both carrying a footstep). `ClipStateNode::FireEvents` (:70-102)
already has the correct wrap-aware normalized-time -> clip-time firing; extract
it into a shared helper both the clip node and the blend trees call with their
dominant clip (BlendTree1D: the lower/upper entry by `blend < 0.5`; BlendTree2D:
the max inverse-distance weight). Keep the normalized-time sync the blend tree
already uses (each clip sampled at `normalizedTime * clip->duration`).

Tests: a blend-tree state whose dominant clip has an event fires it ONCE across a
frame step (incl. loop wrap); changing the blend parameter changes WHICH clip's
events fire; no double-fire when both entries carry the same event.

FIXED as specified: shared FireClipEvents helper (extracted from ClipStateNode);
BlendTree1D fires the nearest-threshold clip (Duration's exact pick, so timing
and events agree), BlendTree2D the nearest-position entry. Both test-pinned
including the loop-wrap span and the parameter switching which clip fires.

## Seeded: investigate CCD + mass override (physics) (user 2026-08-26) - DONE 2026-08-31

BUILT (user: "yes to both"): BodyDesc + RigidBodyComponent gained
`continuousCollision` (Jolt LinearCast, dynamic-only, opt-in) and `mass`
(explicit kg; 0 = density-derived; inertia stays density-derived via
CalculateInertia + mMassPropertiesOverride). Component DataVersion 2->3 with
version-gated reads; PhysicsWorld::BodyMass query added. Tests: a 200-unit/s
sphere tunnels a 20cm wall discretely and STOPS with LinearCast; explicit mass
reads back exactly; unset matches the density path; static bodies report 0.
GOTCHA: a box thinner than Jolt's default convex radius (0.05) asserts in
BoxShape - test walls must be >= 10cm half-extent.


Origin: parity doc, physics - "Draconic has NO CCD and no explicit mass override
(density only)." Both are Jolt-NATIVE per-body toggles we simply do not surface
yet - so this is a small INVESTIGATE-then-maybe-build (does our content need
them, and the component/wire surface), not a port.

- **CCD (continuous collision detection)**: fast small bodies (a thrown object, a
  fast bike/projectile) tunnel through thin geometry with discrete stepping.
  Jolt exposes it per body via `EMotionQuality::LinearCast` (vs `Discrete`). The
  ask: expose a per-RigidBody motion-quality flag (default Discrete; LinearCast
  opt-in), map it at body creation. Cost is an extra cast per fast body, so it is
  opt-in, not global. Investigate: do PaperKid / the samples actually tunnel
  today (is it needed now), and confirm the Jolt mapping + a probe.
- **Mass override**: we derive mass from DENSITY only -
  `EOverrideMassProperties::CalculateMassAndInertia` (PhysicsWorldImpl.cpp:580).
  Games often want an explicit mass for FEEL (a light-looking prop that shoves
  hard, or vice versa). Jolt supports it via `CalculateInertia` +
  `mMassPropertiesOverride.mMass` (keep density-derived inertia, override the
  scalar mass) or fully provided. The ask: an explicit mass field on RigidBody
  (0/unset = derive from density as today; >0 = override), mapped at creation.

Deliverable: a short recommendation (are both worth adding now?) and, if yes, the
component fields + Jolt mapping. Likely a SMALL build if approved - two
per-RigidBody fields + the creation-path branch + a versioned component bump +
serializer-strict-gated reads.

Tests (if built): a fast body with CCD on does NOT tunnel a thin wall a discrete
body passes through (headless step probe); an explicit mass override yields the
Jolt body's actual mass (not the density-derived one); unset = byte-identical to
today (density path).

## Seeded: nav-terrain integration (user 2026-08-26) - P0 DONE 2026-08-31

P0 shipped: `CollectNavigationGeometry` triangulates every terrain's heightfield
surface inside the zone box (zone-local, stride capped at the zone cellSize so
Recast never re-voxelizes an over-fed grid; last row/column always included so the
surface reaches the zone edge; +Y winding for the walkable filter). Headless tests:
flat terrain paths, a ~7-degree ramp paths, an ~88-degree cliff splits the navmesh
while each plateau stays walkable. Holes: no terrain-hole feature exists yet, so
nothing to exclude; the seeded hole-cutting item must subtract its cells here when
it lands. Tiled/large-terrain bake remains the known follow-on.


Origin: parity doc, navigation - "Draconic is single-tile, synchronous,
MESHES-ONLY (TERRAIN DOES NOT CONTRIBUTE - grep Terrain/Heightfield in
NavigationBakeImpl.cpp = 0)." Also a documented terrain deferral
([[terrain-track]]: nav-bake integration, [[navigation-track]]). So a navmesh
bakes over meshes but agents CANNOT walk on terrain - the terrain surface is
invisible to Recast. The nav stack itself is complete (Recast/Detour zones +
agents + crowd + FindPath); this feeds it the missing geometry.

Goal: the navmesh bake rasterizes the TERRAIN surface (within the zone / bake
bounds) alongside meshes, so agents walk on terrain.

The pieces:
- **Geometry source** - triangulate the shared `Heightfield` samples (the source
  of truth physics/nav already share) into Recast INPUT GEOMETRY within the bake
  bounds, alongside the mesh geometry `NavigationBakeImpl.cpp` already collects.
  A coarse cell step is fine - Recast voxelizes to its own cell size anyway.
- **Bounds** - zones are LOCALIZED, which mitigates the current single-tile /
  synchronous limit; bake only the terrain inside the zone, not the whole
  (possibly huge) heightfield.
- **Slope** - Recast's walkable-slope filter already drops too-steep faces, so
  cliffs fall out for free.

Scope for the week: P0 = the heightfield surface within a zone contributes to the
bake; an agent paths across flat + sloped terrain. Large-terrain / tiled-bake
performance is a known follow-on (the tiled bake is its own parity gap).

Open questions for week start:
- Source: triangulated heightfield samples vs the terrain chunk meshes - the raw
  heightfield is simplest and matches what physics uses.
- Resolution/cell step for the triangulation vs the Recast cell size (avoid
  over-feeding triangles Recast just re-voxelizes).
- HOLES: holed terrain cells (the hole-cutting item seeded this week) must NOT
  contribute walkable surface - agents cannot cross a hole; sequence so holes
  exclude their cells if both land.
- Multi-terrain / determinism: preserve the "same input -> byte-identical bake"
  contract when terrain joins the input set.

Tests: baking a zone over a terrain produces a navmesh covering the surface + an
agent path crosses it (headless nav test); a too-steep slope is excluded; (if
holes land) a holed cell has no navmesh over it.

## Seeded: spline + spline editor (user 2026-08-26) - P0 DONE 2026-08-31

APPROACH RULING (user): BEZIER-NATIVE, road tools later. Each point stores
position + in/out handles + a mode - Auto (Catmull-Rom-derived handles: drop
points, smooth curve through them), Smooth, Broken (independent handles, the
road-grade corner case) - so authoring feels like Catmull-Rom while the data
model never migrates when the road tool arrives. Sedulous has only per-segment
Vector3.Hermite/CatmullRom (no authored spline type), so the curve class is
from-scratch per the port-first policy.

P0 shipped: foundation.spline (Core-only leaf, the Foundation::Lod precedent) -
SplineCurve with stored-handle eval, UpdateAutoHandles, arc-length table +
DistanceToT/EvaluateAtDistance, ClosestPoint (coarse + ternary refine), open +
closed; engine.spline SplineComponent (data-only manager, serialized versioned,
rides scene composition - surface module count 10->11); editor.spline
SplineEditTool on the viewport-tool framework (drag points on a camera-facing
plane, Ctrl+click segment inserts at the closest curve position, Del/X removes
with a two-point floor, one undo command per gesture) + a SplineGizmoRenderer
in Editor.Scene (curve + points visible whenever the entity is selected, gizmo
registry 10->11). Points are ENTITY-LOCAL; the transform places the curve.

P1 DONE 2026-08-31: tangent-handle dragging (the selected point draws its
handles; dragging one promotes Auto->Smooth, Shift breaks the pair - Smooth
keeps the opposite handle collinear at its own magnitude) and the script facade
`SceneSplines.of(scene)` (length / pointCount / isClosed / sampleAt /
sampleAtDistance / closestPoint, all WORLD-space, returning the value-typed
SplineHit {valid, t, position, tangent} - facade name count 34->36).

P2 path-follow DONE 2026-08-31: PathFollowComponent (EntityRef to the spline
entity + speed/distance/playing/loop/alignToTangent; simulation-only Update
tick) advances its entity by arc distance, wraps on loop/closed, clamps + stops
at an open end, and yaw/pitch-aligns -Z to the tangent. Unparented-follower
convention (the NavAgent moveEntity precedent); a parented follower needs
world->parent-local conversion when someone asks for it.

Remaining (P2): scatter-along-spline, particle spline opcode, road extrusion.


Origin: parity doc - "SPLINES: Lumix-ahead - a first-class SPLINE SCENE COMPONENT
with interactive gizmo editor, Lua access, and a particle spline opcode. Draconic
has a general n-key Hermite Curve type + CurveCanvas widget (better keyframe
curves) but NO authorable 3D SPLINE entity." So we have the CURVE MATH (1D
keyframe curves) but nothing to place + edit a 3D path in the world.

Goal: a first-class 3D spline you place as a component and edit with control-point
handles in the viewport - the substrate for roads, paths, cable, patrol routes,
scatter-along-a-curve, and spline-driven particles.

The pieces (build on the curve math + the gizmo/viewport-tool framework):
- **SplineComponent** - control points (3D position + tangents) + a curve type
  (Catmull-Rom for easy through-points and/or Bezier for tangent control),
  extending the existing Hermite curve to Float3; serialized. Open + closed loops.
- **Editor tool** - an interactive spline tool (the IViewportTool framework +
  per-component gizmo seam, [[viewport-tool-plan]] / ComponentGizmos): drag
  control points, INSERT on a segment, delete, edit tangents; reuse the transform
  gizmo for the point handles.
- **Runtime eval** - position/tangent at t, ARC-LENGTH parameterization (even
  spacing), closest-point; script-bound (Lumix has Lua access + a particle spline
  opcode).

Scope for the week: P0 = the SplineComponent + eval + interactive control-point
editing in the viewport (place a spline, drag/insert/delete points, see it
drawn). Script access + the CONSUMERS (path-follow, scatter-along-spline,
particle spline) as follow-ons - land the spline + editor first.

Open questions for week start:
- Curve type: Catmull-Rom (interpolates the points, simplest to author) vs Bezier
  (tangent handles) - or both selectable.
- Editor UX: a control-point gizmo each + insert-on-segment; tangent handles for
  Bezier; the debug-draw of the curve.
- Arc-length param for even distribution (scatter/path-follow want it).
- Which consumers to seed vs defer (path-follow component is the obvious first;
  the particle spline opcode ties to the particles system).

Tests: spline eval (position/tangent at t for known control points, headless);
insert/delete a control point preserves the rest of the curve; arc-length param
distributes samples evenly; component serialization round-trip.

## Seeded: EditText commit-on-blur (user 2026-08-27) - DONE 2026-08-31 (option b)

RULING (user): option (b). EditText gained `OnCommit`: fires on Enter/activate
(with OnSubmit) and on focus-lost when the text changed since focus gain -
change-detected, so an untouched blur never re-commits, and an Enter-then-blur
never double-fires. OnSubmit stays Enter-only for activation side effects. The
"18 consumers" count was stale: StringEditor, NumericField (float/int rows),
and the entity-name row already blur-committed; the two real gaps migrated -
ColorPicker's hex field -> OnCommit, and the collision-matrix name row's
keystroke-mirror workaround (6e66c479) removed (its OnSubmit + change-detected
OnEditingFinished pair already covers both paths; OnCommit there would
double-commit).


Origin: renaming a collision group then clicking Add reverted the name -
`EditText` fires `OnSubmit` only on Enter/activate, NOT on focus-lost, so an
edit typed and then clicked-away-from is dropped. Patched LOCALLY for the
collision-matrix name field (mirror typed text into the model on OnTextChanged,
commit 6e66c479), but the quirk affects EVERY inspector text field (float / int
/ string property editors all lose an edit if you type then click away without
Enter).

Goal: decide whether `EditText` should commit on blur globally (the standard,
expected UX). Blast radius = 18 `OnSubmit` consumers to vet - some may do
navigation/side effects that should NOT fire on blur. Options: (a) fire
OnSubmit on OnFocusLost guarded by a "changed since focus gained" flag; (b) a
separate OnCommit event distinct from OnSubmit that blur raises and property
editors subscribe to. Review the consumers, pick one, and remove the local
matrix workaround if the global fix subsumes it.

## Seeded: FIX - intermittent AngelScript thread-manager assertion in the cook (user 2026-08-28) - DONE 2026-08-31

FIXED: AngelScriptManager's constructor pins the process-global thread manager
once (asPrepareMultithread, never unprepared - the upstream multi-engine idiom),
so no engine-teardown interleaving can free it early or double-unprepare. Test:
8 sequential engine lifecycles compile a script each; PaperKid cooked 3x clean.


Origin: Tools.Cook aborts intermittently at process teardown with
`asCThreadManager::Unprepare(): Assertion 'threadManager' failed`
(ThirdParty/angelscript/source/as_thread.cpp:193), observed while cooking
several AngelScript scripts (PaperKid: PaperKidGame + Level + Bike) in one
run. NOT harmful in practice - the cooked products are written and the cook
reports success; a plain re-run exits 0. But it is a real crash-on-exit that
would fail CI on a bad roll and is noise during content iteration.

Cause (to confirm): each script cook spins up + tears down its OWN
AngelScript engine, and AngelScript refcounts a PROCESS-GLOBAL thread manager
via asPrepareMultithread / asUnprepareMultithread (first engine creates it,
last release frees it). Cooking N engines in sequence unbalances that
refcount on some interleaving, so a later Unprepare finds `threadManager`
already null and asserts. The multi-engine-per-process cook is the trigger
the single-engine editor/runtime path does not hit.

Fix direction: give the cook ONE owner of the AngelScript thread manager for
its whole run - call asPrepareMultithread once at cook start and
asUnprepareMultithread once at the end (pinning the manager across every
engine create/destroy), or otherwise ensure the AngelScriptManager lifecycle
cannot double-unprepare. Small + contained in the AngelScript cook /
Script.AngelScript backend init.

Tests: cook a project with several AngelScript scripts N times in one process
without the assert (a headless multi-script cook loop); single-script cook
unaffected; products byte-identical.

## Carried from week 2026-08-22 (incomplete)

Moved forward from week-2026-08-22 (its completed items stayed there as that week's DONE record). These
are the still-open items:

### Editor extensibility seams (user ruling 2026-08-18: "ultimately must fix")

Origin: navigation P4b-3 (Documentation/Specs/navigation-editor-ui.md, the FOLLOW-UP + RE-RULING
sections). Building nav's Bake button + zone gizmo surfaced that the editor's per-domain surfaces are NOT
plugin-extensible - every domain couples into Editor.Scene's central dispatch:
- `InspectorViewImpl.cpp` is a hardcoded per-type chain (physics settings, RigidBody, the ref-picker
  table, ...) that grows a direct domain import per entry.
- `GizmoRendererRegistry` is per-ScenePage and populated only by `RegisterBuiltinGizmoRenderers` inside
  Editor.Scene - no external hook.

Nav landed as (B) (join the central dispatch - correct for one consumer, per the re-ruling), but the user
has ruled the seams ARE coming; this supersedes the "second consumer pays for it" promotion rule as a
scheduling decision (the DESIGN of the seams should still be validated against the by-then-real consumers,
nav + whatever else has landed).

Scope:
1. **Inspector per-component action-row registry** - keyed by component type; Editor.Scene consults it
   after (or within) the central chain. First migrations: nav Bake (from the (B) placement), and
   reflection-probe re-bake if/when it exists. Outcome flashing stays (no silent success).
2. **External gizmo-renderer registration** - a hook so ScenePage adds domain-registered renderers after
   the built-ins (the per-page registry instantiation is the wrinkle: registration must be a provider list
   the page consults, not a global mutable registry).
3. **Migrate Editor.Navigation onto both**; Editor.Scene drops its nav imports.
   `RegisterNavigationEditor(context, host)` returns as the per-domain registrar (the shape the original
   P4b ruling described).
4. **Audit (stretch)**: which existing central InspectorView entries (ref-picker table dispatch, per-type
   sections) should ride the same mechanism vs stay central. Produce the list; migrating them is NOT scope
   unless trivially mechanical.

Constraints: registries follow the house composition-root style (explicit registration from a registrar
function, count tripwires where a lost registration would fail silently); headless tests for both
registries + the nav migration; both compilers green per landing.

### Script backend as a PROJECT setting (user punt 2026-08-18)

Origin: punted from the week-2026-08-15 work. Today the script backend is a BUILD-TIME choice
(OPTION_ENABLE_/OPTION_HAS_<backend> + SCRIPTLIB_* link aliases, guarded by #ifdef - see the
script-backend-gating memory). The ask is to make it a PER-PROJECT choice instead:
- **Chosen at project creation**, stored in the project settings, changeable later in project settings.
- Gates **which script options the editor surfaces** (New-Script creators, the CodeEditView lexer,
  script-facade validation) to the selected backend, and **which VM actually runs** for that project.
- Changing it **likely needs a project reload** (the running VM + registered script surface are
  established at load) - decide reload-vs-live at design.

Open questions:
- Interaction with build-time gating: a project can only select a backend that was COMPILED IN
  (OPTION_ENABLE_ set). The setting is a runtime choice AMONG the enabled backends, not a way to enable a
  non-built one - the editor should offer only the built backends and message clearly if a project asks for
  one that is absent.
- Where the selection lives (ProjectSettings field + the project-create dialog) and how the editor's
  per-backend registration (currently all-enabled) becomes selection-driven.
- Migration: existing projects with no stored backend default to the current primary (the build's default).

### Bespoke-page Undo/Redo - Sound Cue blob commands + toolbar roll-out (user 2026-08-15/18)
### -> Sound Cue commands DONE 2026-08-31 (d50cc9a5); toolbar batch 1 (bus layout / collision shape / texture / image) MERGED 13a7b6ac; remaining pages documented in week-2026-09-05

Origin: UAT (user-confirmed 2026-08-15). Two related pieces:
1. **Sound Cue page edits do not undo/redo.** The page HAS the page toolbar (m_toolbar->Refresh syncs
   Save/Discard/Undo/Redo) AND the blob machinery (SnapshotAsset / ApplyAssetBlob, used today only for
   Discard), but every edit (slot pick/clear, mode toggle, the pitch/volume jitter fields) mutates m_asset
   DIRECTLY - nothing is pushed to Commands(), so Undo/Redo stay disabled. Fix: route each edit through a
   blob command exactly like the bus-layout page does - snapshot before, mutate, snapshot after,
   `Commands().Execute(EditSoundCueCommand(before, after))` with merge-by-key coalescing for scrubbed
   fields. Precedent: AudioBusLayoutEditorPage (EditBusLayoutCommand + SnapshotAsset, already working). Low
   effort - the snapshot/apply halves already exist on the page.
2. **Roll the page toolbar out to the bespoke pages that lack it.** Pages that ALREADY carry the toolbar:
   material, animation-clip, animation-graph, particle, bus-layout, sound-cue, scene. Pages WITHOUT it that
   EDIT and should get it: collision-shape (Editor.Physics), plus any editing page still missing it
   (texture/image/game-ui/input - audit). Pure VIEWERS (mesh, skeleton) have no re-authorable fields, so
   Save is a no-op and they need no toolbar - skip them.

Tests: per-page snapshot/undo/redo round-trip (a scrub coalesces to one step; undo restores the
before-blob) - the clip/graph pages already have this shape to copy.

### AngelScript delegate funcdef surface is ad-hoc (Fable to look into, user 2026-08-19)

Origin: the game-ready-scripting2 P2-6 `button.onClick` seam (script functions as native click handlers via
`IScriptDelegate`; landed 2026-08-19). The native side is general - a reflected `RefPtr<IScriptDelegate>`
param is spelled `?&in`, so ANY funcdef handle is accepted and `Invoke` marshals against the handler's
actual params. The wrinkle is the SCRIPT side: to wrap a function into a delegate handle, AngelScript needs
a PRE-REGISTERED named funcdef, and the backend ships only two ad-hoc shapes (AngelScriptScriptImpl.cpp,
RegisterDelegateSurface):
- `ScriptDelegate` = `double ScriptDelegate(double)` - a confusingly-named, arbitrary double-in/double-out
  shape (it reads as THE delegate type but only fits that one signature).
- `Action` = `void Action()` - the common void no-arg callback (what onClick handlers use).

A handler of any other signature has no funcdef to wrap it, and using the wrong one (`ScriptDelegate` on a
void fn) fails at compile with "Invalid expression: stand-alone anonymous function". Luau has no such limit.
For FABLE: a cleaner delegate-signature scheme - a blessed set of funcdef shapes, an auto-registered funcdef
per reflected delegate-param signature, or at minimum renaming/documenting so `double(double)` is not the
confusing default. NOT urgent - `Action` covers PaperKid's void handlers. (The delegate-callback
`CurrentScriptContext` bug this seam surfaced was already fixed 2026-08-19.) Relates to
[[script-facade-call-syntax]].

### Carried from week 2026-08-15 (user on-screen / sign-off)

- [ ] Asset-variants REMAINING (a): the web pak selection is built MANIFEST-FREE (pak-name convention) -
  needs the Fable/user sign-off on that deviation, or a manifest gets built. (b): the BROWSER/mobile
  functional check of the variant paks = user UAT, gated on the Decision-4 mobile-web smoke precursor.
- [x] Entity-active on-screen verify PASSED (user 2026-09-03). Still owed: the remaining
  smoke-checklist property-animation items not covered by the live UAT rounds.

### PaperKid editor-use feedback (still-open items)

- [ ] Organize the new-component / new menu.
- [ ] Drag an instance into a group.
- [ ] Seed default assets + set them up in project settings automatically for new projects.
      (PARTIAL: SeedNewProject seeds the UI font + sky texture + primitive meshes; the input map / bus
      layout / UI theme are not yet seeded or wired into project settings.)
- [ ] Shift+up/down can't select more than 2 items (multi-select bug).
- [ ] Better default input map - movement WASD (left / down / right / up).
- [ ] Detached window doesn't render its viewport unless the main window is visible.

## Moved to week-2026-09-05 (triage 2026-08-31, user: "the weekly is too big")

The unstarted project seeds moved wholesale to Documentation/Plans/week-2026-09-05.md:

- Seeded: occlusion culling (user schedule 2026-08-26)
- Seeded: investigate deferred rendering (user 2026-08-26)
- Seeded: GPU profiler + profiler visualization (user 2026-08-26)
- Seeded: contact shadows (user 2026-08-26)
- Seeded: CDLOD morph for terrain seam quality (user 2026-08-26)
- Seeded: erosion + hole cutting for terrain (user 2026-08-26)
- Seeded: grass / vegetation / foliage for terrain (user 2026-08-26)
- Seeded: root motion for animation (user 2026-08-26)
- Seeded: inverse kinematics (user 2026-08-26)
- Seeded: ragdoll (user 2026-08-26)
- Seeded: morph target animations (user 2026-08-26)
- Seeded: animation compression (user 2026-08-26)
- Seeded: alpha-coverage-preserving mips + stochastic mips (user 2026-08-26)
- Seeded: impostors - octahedral bake as final LOD (user 2026-08-26)
- Seeded: PLAN - in-editor shader editing + material/shader node-graph editor (user 2026-08-26)
- Seeded: investigate large world support (user 2026-08-26)
- Seeded: gizmo vertex snapping (user 2026-08-26)
- Seeded: memory profiling (user 2026-08-26)
- Seeded: editor key rebinding (user 2026-08-26)
- Seeded: crash reporting + dumps (user 2026-08-26)
- Seeded: mountable content directories (user 2026-08-29)

## (plan to be filled at week start)

## Filed by pass-17 review (2026-08-29, Fable)

### DONE (2026-08-29, user ruling: "make Lua work like AS"): container-PROPERTY semantics
RESOLVED - Luau now binds container members exactly like AngelScript: owner ops
(`<name>_count/_at/_add/_removeAt/_move`, ZERO-based, write-through element handles via the
shared getAt/Borrow machinery), and the bare property is NO LONGER exposed (it used to cross as
a detached copy-table whose mutations were silently lost - reads as nil now). The conformance
battery gained a `containerModule` contract certified on BOTH backends (KN/KZ/KM/KR/KL globals),
plus a standalone Luau test proving C++-side visibility of a script mutation.

### (superseded) DECIDE: container-PROPERTY semantics diverge between backends
Luau's PushVariant container branch (LuauScript.cppm ~:1084) converts EVERY container Variant -
property reads and event payloads included, not just facade returns - into a detached 1-indexed
table of element COPIES, so `obj.someArray[1].x = 5` silently writes into a copy. AngelScript
surfaces the same member as mutable `_count()/_at(i)` borrow handles. The two backends now
disagree on container-property mutation semantics. Decide the contract (copy-out everywhere vs
borrow handles everywhere) and add a cross-backend conformance case for a container PROPERTY
read+mutate. Also: move the array-RETURN tests into the shared BackendConformance battery so a
future backend inherits them.

### DONE (2026-08-29): physics query allocations
RESOLVED - BuildQueryShape replaced by WithQueryShape: stack-constructed Jolt shapes +
SetEmbedded (the Jolt transient-shape idiom), zero heap allocations for the shape per query.

### (superseded) CLEANUP: physics query allocations (per-frame path)
Every ShapeCast/ShapeOverlap heap-allocates its Jolt query shape (PhysicsWorldImpl.cpp
BuildQueryShape) + engine Arrays per overlapSphere call; the motivating use (auto-aim per
projectile per frame) makes that 4+ mallocs/projectile/frame. Jolt idiom: stack shape +
SetEmbedded(). Not wrong, just unjustified in the hot path.

### DONE (2026-08-29): editor gizmo gaps
RESOLVED - (a) DrawEntity now stamps ctx.entityEffectivelyActive; the physics renderers
(body/child-collider/character/joint) draw DIMMED gray for effectively-inactive entities;
(b) capsule gizmos (collider + character) draw cap spheres + four side lines via a shared
DrawWireCapsule - they read as capsules now; (c) FloatingPanel's resize hit band moved into the
6px border inset (never over hosted content) with a 12px bottom-right corner grab square.
All three test-pinned.

### (superseded) POLISH: editor gizmo gaps (pass-17 notes)
- GizmoRendererRegistry::DrawEntity does not consult IsEffectivelyActive; the collider/joint
  gizmos are the first renderers where "the sim will ignore this entity" is the interesting
  information (draw dimmed or skip?).
- The capsule collider gizmo draws a sphere + bounding box; on screen it reads as a box with a
  sphere inside, not a capsule (draw hemisphere caps + side lines).
- FloatingPanel resize band (9px) overlaps the content's scrollbar edge column (~3px dead zone);
  consider carving the band out of the header/borders only.

## Seeded: bone attachments + joint-node import noise (user 2026-08-31)

Origin: user observed entity-marker crosses staying at bind pose while the
skinned mesh animates in Simulate. VERDICT: correct display - skeletal
animation never writes entity transforms (the AnimationPlayer evaluates bone
poses internally and feeds skinning matrices straight to the MeshComponent;
the scene graph does not participate). Two follow-ups fall out, both design
work, neither a bug fix:

1. **BoneAttachmentComponent** - opt-in `{ animator entity (EntityRef),
   joint name }` that copies ONE joint's world pose onto its own entity
   after the animation tick (a new ordering point: after
   SkeletalAnimationComponentManager, before render extraction). Gives
   props-on-joints (sword in hand, hat on head); only attached entities pay.
   Needs: joint pose in MODEL space from the player (the skinning matrices
   are bind-relative - expose the player's model-space pose array), then
   compose with the animator entity's world matrix. Trigger: first game need
   (PaperKid props / pickups).

2. **Skip or collapse joint nodes on model import** - the
   hierarchy-preserving importer turns every GLTF node into an entity
   including the whole armature joint chain; those entities are dead weight
   (nothing reads them - the skeleton resource carries its own hierarchy)
   and are exactly the frozen crosses the user noticed. An import option
   (default on?) to drop pure-joint nodes from the generated prefab/scene.
   RIDES the import-UX planning pass (with merge-meshes, user 2026-08-31) -
   do not build standalone.

## Lumix nav parity round (user 2026-09-02) - ALL FOUR DONE 2026-09-02

Surfaced by cross-referencing the Lumix parity doc's navigation section with
the weekly; user approved the list + the working order. (The parity doc's
"meshes-only bake" claim was already stale - nav-terrain shipped 2026-08-31.)

- **Crowd per-agent speed / stop distance (c04702ae)**: SetAgentParams
  updates the LIVE dtCrowd agent; the tick value-compares maxSpeed/
  maxAcceleration and re-applies on any change (script setSpeed, navigateAt,
  inspector edits). stopDistance = arrival radius - finished inside the
  ring, crowd target RELEASED on arrival so the agent parks on the ring
  (walk-NEAR-the-door / follow-at-distance). navigateAt(x,y,z,speed,stop)
  is the one-call scripted move. Agent envelope v2 (gated).
- **Agent-state introspection (c04702ae)**: NavigationCrowd::AgentState
  remaps Detour internals to a STABLE contract (crowd state / move-request
  stage / speed intent / corridor corners); tick-cached on the component
  for scripts (state/targetState/desiredSpeed/corners); debugDrawPaths
  floats a 'walking [valid] 3.5u/s' label per agent, failed = red.
- **Tiled bake + per-tile regen (e2ec496f)**: BuildTiled splits the zone
  into tileCells tiles (64 @ 0.3 = 19.2u), border-overlap stitching,
  row-major deterministic, ONE v2 multi-tile blob (v1 still loads - no
  forced re-cook). BuildTileAt = the generateTileAt-parity primitive;
  BuildTiled IS it in a loop, so regen is byte-identical (pinned). Editor
  zone bake is tiled; queries/crowds/debug draw untouched.
- **Bake stage visualization (10b475a4)**: opt-in NavigationBakeStages
  capture (walkable span tops + region contours); the editor bake parks it
  on NavigationSceneSystem; debugDrawBakeStages (settings v2) overlays it
  in the zone's rigid frame. Observer-pure - blob byte-identical, pinned.

Still AHEAD of Lumix (not to be "fixed"): determinism, 4-way bake status,
navmesh-as-cooked-asset, FindPath's reachable contract, ReportOnly mode,
crowd UpdateOrder -100. REMAINING (recorded in week-2026-09-05): partial-
rebake consumer wiring (obstacles/terrain edits), multi-threaded bake
behind the measured-stutter gate. USER VISUAL VERIFY owed on all four.

### Follow-on (2026-09-02, b2534d95): parallel bake + partial-rebake consumer +
### domain-contributed editor settings

- **Parallel tiled bake**: BuildTiled fans tiles across a scoped JobSystem
  (params.parallelBake); row-major assembly keeps blob + stage capture
  byte-identical parallel or serial (pinned) - the toggle trades latency only.
- **Domain-contributed editor settings (FIRST consumer: navigation)**:
  EditorContext contribution registry (category + bool fields, get/set
  closures) + UserEditorSettings accessor; the Preferences dialog renders
  contributions generically. editor.navigation owns
  NavigationEditorSettings{parallelBake} as its own user-store section
  (types register at the composition root BEFORE the settings file loads -
  the ordering rule for every future domain section). Inspector Bake reads
  the toggle.
- **Partial rebake wired end to end**: ReadTiledBlobGrid + BuildTileInGrid
  (anchored to the RECORDED grid; vertical range from current geometry) +
  PatchTiledNavMeshBlob (row-major splice) + NavigationMesh::ReplaceTile
  (live swap; v1 refuses) + editor RebakeNavigationZoneRegion (region ->
  affected tiles + bake apron -> patch the zone asset; full-bake fallback).
  PINNED: patched == full rebake byte-for-byte when the vertical envelope
  is unchanged (documented caveat); live swaps reroute paths immediately.
  Obstacles/terrain-edit TRIGGERS now just call RebakeNavigationZoneRegion.

## Found: pre-existing ASAN leaks (2026-09-02, while gating I5 P0/P1)

RESOLVED 2026-09-03: the full ASAN battery is green (151/151). The cleanup
pass landed as described below - `freeResult` in Shaders.Tests, encoder +
backend destroys across the probe suites (Msaa/Orientation/VG/BcTexture),
device/pool/layout teardown in RHI.Null/Validation/WebGPU tests, a
stack-use-after-scope fix in GUI.Shell BridgeTests, and TestMain.h gained
ASAN-only LSan suppressions (libdbus/nvidia) plus a GPU-driver-module pin so
loader-dlclosed ICD leaks symbolize instead of reporting as unmatchable
"<unknown module>" frames.

Both reproduce on master WITHOUT the allocator changes (verified via stash):
- **Shaders.Tests**: `Compiler::compile` fills `CompileResult::bytecode` with a
  raw `new u8[sz]` (Compiler.cppm:316) and the test at ShadersTests.cpp:35
  never frees it (373 bytes, 3 allocations). Fix = give CompileResult owned
  storage (Array<u8>) or free in the test.
- **Integration.TextureCompression**: `VkCommandPoolImpl::CreateEncoder`
  encoders leaked by `RenderTexturedCube`/`ProbeBcSampling`
  (BcTextureProbeTests.cpp:187/271) + a `CreateBackend` VkBackendImpl leak at
  ForEachBackend (BcTextureProbeTests.cpp:297) + nvidia driver noise (~200KB
  total). Fix = release encoders/backend in the probe helpers.
- **VG.Backend.Tests**: same probe-helper family - VkBackendImpl/VkAdapterImpl/
  VkCommandEncoderImpl + WebGPU queue objects leaked by `RenderScene`/
  `ProbeDevice` (VGPixelProbeTests.cpp:155/400). The GPU-probe suites
  (VG.Backend / Terrain.Backend / TextureCompression) flicker in and out of
  the ASAN report run to run - one cleanup pass over the shared probe-helper
  pattern should clear all of them.

## Landed: text sidecars get .data; binary keeps .bin (user 2026-09-03)

Scene/prefab SOURCE streams are XML text but were stored as
`<asset>.<stream>.bin`, which GitHub force-renders as binary. The content DB
now takes a `StreamEncoding` on `WriteData` (default Binary - no caller churn):
text streams land as `.data`, a successful write deletes the other-suffix
sibling (auto-migration on next save), `ReadData` probes `.data` then legacy
`.bin`, and clone/copy-forward/rename/delete accept both suffixes and preserve
each stream's own. Writers marked Text: scene save, prefab save/apply,
model-import prefab, MCP scene_write. Cooked/staged stays binary `.bin`;
cook recipes hash stream BYTES via ReadData, so no re-cook. PaperKid's three
`.scene.bin` renamed to `.scene.data`. Tests: Content.Tests round-trip +
legacy fallback + migration + suffix preservation.

## Landed: editor startup black-gap fix - MSDF bake parallel + disk-cached (user 2026-09-03)

The launch-to-first-frame black window measured 5.6s (Debug, PaperKid); 4.5s
of it was msdfgen re-baking the two editor font families EVERY launch (VG/UI
shader pipelines - the suspected cause - measured 27ms, courtesy of the cooked
shader pack). Two fixes: (1) DFFontAtlasBaker now packs sequentially
(deterministic layout) and generates/blits glyphs via a scoped JobSystem
ParallelFor - bake-twice determinism is pinned by a new DFBaker test; (2) a
`FontAtlasDiskCache` (editor.app:font_atlas_cache, installed into the new
`FontAtlasBakerFactory::SetAtlasCache` seam) persists baked DF atlases under
`<user-data>/font-atlas-cache/` keyed by font bytes + bake options + format
version. Cold launch ~2.7s (parallel bake + store), warm ~1.9s (atlas load).
Cache tests: byte-identical round-trip, key sensitivity, truncated-file
fail-closed.

## Landed: viewport debug views - layer 1 (user 2026-09-03)

First item of the decided renderer order (debug views -> SSGI -> probe volumes ->
lightmaps). The generic graph-resource inspector shipped end-to-end:

- `ViewDebugView` + `DebugResourceInfo` in render.api; `RenderScene` gains an
  ephemeral per-view `debugView` param (the ViewPostOverride pattern) and
  `ISceneRenderer::GetDebugResources` exposes the previous frame's graph-texture
  inventory (deduped names + size + depth/samples flags).
- `DebugBlitPass` (foundation.render:debug_blit, FxaaPass-shaped): fullscreen blit
  of ANY named graph texture into the view's sub-rect, declared after the compose
  chain and BEFORE overlays (gizmos stay on top). The read is a real graph
  dependency, so transient aliasing keeps the source alive - no keep-alive
  machinery needed. Source binds UnfilterableFloat + shader Loads (no sampler), so
  every format works incl. depth (via the depth-only view); channel select, range
  remap, and depth linearization in `debug_blit.ps.hlsl` push constants (no
  cooked variants - the drift lint stays quiet).
- Name lookup is VIEW-SCOPED: names repeat per view ("forward.depth" x N), so the
  pipeline searches this view's contiguous resource range first, then the
  frame-global prefix (shadow atlas etc.). Multisampled sources are skipped (the
  WebGPU Load rule); their resolved twins are listed instead.
- Editor: a "Debug" toolbar button beside "Post" on the scene viewport opens the
  picker (Final + every listed resource with size/depth annotation), stored as
  ephemeral per-page state like the post show-flags.
- Tests: PipelineTests on Null RHI + real DXC - selecting "forward.depth" appends
  the debug.blit pass and the inventory row carries the depth flag + dimensions;
  unknown/empty names add no pass. The engine shader-corpus cook test covers the
  new .hlsl. RenderFrame gained a read-only Graph() accessor for introspection.

FIXED same day (735aa0f4, found in the user's first visual pass): the picker
listed the view's IMPORTED color target (forward.color 0x0 - selecting it made
the blit read its own write target, desyncing Vulkan layout tracking) and
desc-less persistents (ibl.prefilter/ibl.brdf 0x0). The inventory + lookup now
accept only transients with resolved dimensions; listing persistents (shadow
atlas) joins the track tail (needs rhi::Texture dimension enrichment +
depth-aspect handling).

VISUAL VERIFY PASSED (user 2026-09-03) - resource views work in-editor.
Remaining after layer 2 (below): pixel inspector, wipe/compare.

## Landed: viewport debug views - layer 2 semantic modes (user 2026-09-03)

The instrumented modes: the FORWARD SHADER outputs the named term instead of
the lit result. `ViewDebugSemantic` (Albedo / Normal(mapped world) / Roughness
/ Metallic / Cascades / ClusterHeat / Overbright) on `ViewDebugView`; the mode
rides a NEW dedicated `DebugParams.x` field in the View cbuffer + ViewData
mirror (528 -> 544; a proper named home per user ruling - no spare-component
squatting in ShadowParams). Uniform branch in forward.ps.hlsl - free when off,
never a cooked variant, drift lint untouched (corpus cook test verifies).
Cascade mode tints albedo luma by CSM selection (red/green/blue/yellow, grey =
unshadowed); ClusterHeat ramps per-cluster light count blue->red over the
8-light budget; Overbright flags non-finite/negative lit results magenta via
NaN-safe comparisons.

Display path: semantic values ride the scene HDR, so the pipeline reuses the
layer-1 DebugBlitPass to show the POST-RESOLVE hdr RAW (tonemap would grade
the encoded terms; source captured before decals/AO/SSR/TAA touch it). The
no-tonemap path needs no blit - the forward writes the LDR target directly.
Editor: a semantic section in the viewport Debug menu; semantic + resource
picks are mutually exclusive. Coverage note: forward mesh materials only -
sky, terrain, sprites, unlit draw normally in semantic modes (their shaders
have their own View blocks; extend when needed).

Tests: PipelineTests - semantic mode + tonemap path appends debug.blit; the
direct-LDR path does not. The forward.ps change compiles through real DXC in
the existing cube-draw tests. VISUAL VERIFY PASSED (user 2026-09-03) -
semantic modes work in-editor.

## Seeded: renderer debug views in the editor viewport (user 2026-09-03)

LAYERS 1 + 2 LANDED 2026-09-03 (see week-2026-08-29.md) - graph-resource
inspector + DebugBlitPass + viewport "Debug" picker + semantic shader modes.
Remaining below: pixel inspector, wipe/compare, semantic coverage for
terrain/sprite/unlit shaders when needed.

Origin: user request 2026-09-03; design discussion in-session. FIRST in the
decided order (debug views -> SSGI -> probe volumes -> lightmap baking) -
both GI phases get far easier to bring up when every intermediate is one
dropdown away.

Two layers:
- **Layer 1 - generic graph-resource inspector (highest leverage)**: the
  RenderGraph already registers every pass's inputs/outputs as NAMED resources
  (the Graphviz/validator names). Enumerate the resource table after compile
  into a per-VIEWPORT dropdown; selecting one appends a DebugBlitPass sampling
  it with channel select / range remap (min/max, log) / depth linearize /
  array-slice + mip pickers / alpha checkerboard. Mechanical key: a keep-alive
  on the selected resource so transient aliasing does not recycle it before
  the blit (a graph-level lifetime extension). Payoff: every FUTURE pass
  (SSGI, lightmap intermediates) is automatically inspectable with zero
  per-feature work. WebGPU caveats: unfilterable-float + MS resources need
  texelFetch/resolve paths in the blit shader.
- **Layer 2 - instrumented semantic modes**: albedo/normals/roughness/metallic
  (need forward-shader cooperation), shadow-cascade tint, cluster/froxel
  heatmap, overdraw counter, lightmap UV2 charts + texel density, probe
  influence, NaN/Inf highlight. NOT a cooked shader variant (would double the
  variant power set vs the drift lint) - a runtime uniform branch
  (debugViewMode != 0 is uniform-coherent, free when off), compiled out in
  Shipping.

Editor wiring = existing seams: mode picker on the viewport toolbar or the
editor.app:tool_panel seam (terrain-brush precedent); per-viewport, matching
the existing per-view post override (disable-post/freeze-auto-exposure) -
docked viewports can show final + debug side by side via multi-view today.
Early polish: pixel inspector (readback under cursor -> raw value tooltip),
producer metadata in the dropdown (pass name, format, size), grouped by pass.
Later: dovetails with the profiler-UI gap (pass timeline -> click -> outputs).

Order within the track: layer 1 + toolbar picker, then
material/cascade/cluster modes, then pixel inspector, then wipe/compare.

## Landed: web networking - WebSocket transport both directions (2026-09-01)

(A) of the web-net plan BUILT: a5c14e69 native WS accept (RFC 6455 handshake
via foundation.http, frame codec with malformed-input doctests, gateway +
WebSocketHybridSocket so session/reliability run unchanged); c8bef210 browser
emscripten client socket (pre-open send buffering, JoinServer on web);
9038b969 the port+1 browser-gateway convention (fixed-port hosts open ws on
port+1, web join redirects - one script nets everywhere). OWED + next steps
tracked in week-2026-09-05.md (browser smoke is manual by design).

## Landed: I5 allocator plumbing - TRACK COMPLETE (2026-09-02)

13 commits P0-P7: enablers, nav exemplar, Core consumers (Context ctor-default
removal + JobSystem/host threading), the data backbone (XML DOM, Settings,
Http, ContentDatabase/ResourceManager/NativeFileSystem), fonts, audio, net,
script backends (the MemoryAllocator() inheritance idiom at scale), resource
factories, the UI cluster (MakeRef pending-control handoff = MemoryAllocator()
valid in ctors), engine subsystems (Scene ctor default removed; Render +
GameUI tags), editor+samples (~1,950 sites), and the P7 lockdown: Core.Tests
scans every run against the pinned allocator-allowlist.txt (113 root/bound
files after the editor-root pass + the pipeline slice; tagged Editor root +
one AppRoot per binary) - new ambient references fail CI, stale entries fail
too. MemoryTagReport() (per-tag live/peak/total) feeds the I4 memory report
with Navigation/Render/GameUI/Editor tags.

## Landed: picker-dialog thumbnails (2026-09-01)

b808126b - asset-picker rows query the thumbnail service first (icon until a
tile exists; passive re-query like the inspector slots, so a freshly generated
tile lands on the next rebind). Remaining tail + deferred live-refresh:
week-2026-09-05.md (thumbnails section).

## Texture page UX - built 2026-09-01; verify owed

texture-page-ux.md BUILT in full (archived to Archive/texture-page-ux-history.md):
usage-first Content block, derived color space, mismatch lint, profile buttons,
importer inference, Cooks-to row. OWED: the user visual pass on the reworked
page (open any texture: profiles row on top, Content then Sampling groups, lint
appears on a Normal+sRGB combination, Cooks-to updates as usage/compression
change). UNBLOCKED next: the compression track
(Plans/texture-compression-hdr-and-normals.md - BC6H HDR + the normal-map
format fix; its resolved-format preview lands in the existing Cooks-to row).

## Landed: auto-exposure per-frame bind-group slots (user repro 2026-09-03)

User's visual pass surfaced VUID-vkFreeDescriptorSets-00309 spam on project
open (auto-exposure scenes). Breakpoint-debugged at the user's direction (gdb
on the ERROR-severity validation callback, backtrace at the bad free) - the
culprit was NOT the font rebinding the log suggested: ExposurePass slotted its
bind-group cache by viewIndex ONLY, unlike every sibling pass. The exposure
adaptation ping-pongs its prev-frame view, so the per-view slot mismatched
EVERY frame and the rebuild freed a descriptor set the previous frame's
in-flight command buffer still referenced.

Fix = the established per-(view,frame) scheme: ExposurePass takes
framesInFlight, DeclareExposure takes frameIndex; a slot is only rewritten
framesInFlight frames later, after its command buffer completed. Identical
gdb repro post-fix: zero validation errors; user confirmed in-editor. A
PipelineTests case drives the path across the ping-pong wrap.

Follow-up noted (small, benign): ApplyProjectUiDefaults recreates the UI
ResourceFontService on every cook-finished callback despite documenting
itself idempotent - "default font bound" log churn; make SetDefaultFont
early-out on the same Font product.

## Verified: post-processing stack phases 1-3 (user 2026-09-03)

The paused-pending-verify gate on the post stack is CLEARED - the user's
visual pass confirms the phase 1-3 behavior in-editor. Post phases 4/5 are
now UNBLOCKED (they stay on the deferred-by-design list awaiting an explicit
go-ahead, per that list's rule). Thumbnails visual pass also confirmed the
same session (prefab/scene/particle + the newer type tiles).

## Landed: GI tier 1 - SSGI (user 2026-09-03)

Second item of the decided renderer order. SsgiPass (:ssgi) is SsrPass's
diffuse twin, structure-identical (trace + temporal resolve, per-view
ping-pong history, generation-keyed bind-group caches with deferred frees):

- **Trace** (ssgi.ps.hlsl): 1-4 stratified cosine-hemisphere rays per pixel
  around the mapped view-space normal, marched against the depth buffer with
  the SSR DDA core (shorter budget: 24 steps/ray, 3.0 view-units radius, 2
  refine iters); hits gather the lit HDR as one-bounce radiance. Noise is
  FRAME-ROTATED (IGN + R2 sequence) - unlike SSR's static dither - so a still
  camera converges under the temporal blend. Misses contribute nothing (the
  ambient/IBL already in the HDR covers off-screen light).
- **Resolve** (ssgi_resolve.ps.hlsl): reproject + YCoCg variance-clip +
  ghost-reject accumulate (blend 0.92 - noisier than SSR, leans on history),
  then composite ADDITIVELY: out = hdr + gi * intensity. RULING (v1):
  unit-albedo approximation - SSR lerp-replaces because it stands in for IBL
  specular; the bounce is added light. Per-pixel albedo modulation arrives
  when the forward integrates indirect terms with the tier-2 probe volume.
- **Placement**: after decals, BEFORE SSR - reflections see the bounce; the
  chain is postHdr -> ssgi.scene -> ssr input. needsMotion ORs in ssgiEnabled
  (the temporal resolve reprojects by velocity).
- **Authoring**: PostProcessSettings.ssgiEnabled/ssgiIntensity (serialized at
  DataVersion 3, reflected with visibleWhen; default OFF - no visual change
  until authored). Editor Post show-flags menu gains "No SSGI"; the master
  disablePost drops it too.
- **Debug views dividend**: ssgi.raw and ssgi.scene appear in the viewport
  Debug picker automatically - tune with zero extra tooling.

Tests: PipelineTests on Null RHI + real DXC (enabled -> trace+resolve declare,
inventory lists both textures, history survives frame 2, disabled -> absent);
ExtractTests (ResolveScenePost mapping, master-override drop, v3 round-trip
through the scene); the corpus cook lints both new shaders. USER VISUAL
VERIFY OWED: enable Screen-Space GI on a lit scene (a colored wall near a
floor shows the bleed; check stability under camera motion).

## Landed: nested-sidecar import fix + toasts reach the console (user repro 2026-09-03)

chess_set_4k.gltf (sidecars in a textures/ SUBFOLDER) imported its 88 assets
but every deferred provenance copy into Sources/textures/ failed - raw
WriteFile creates no parent directories, unlike the inline path's
NativeFileSystem::Save (why every headless repro passed). The failed worker
job then skipped FinishImport, so the prefab/scene generators never ran -
the user's "failed to write scene and prefab".

Fixes: DeferredImportWrite::Execute creates the destination's parents before
copying; the worker job logs each failing write's label (the toast's "see
Console" is now true); and - user ruling - EditorContext::Notify mirrors
every Error/Warning toast to LOG_ERROR/LOG_WARNING, so transient toasts
always leave a console trail. The prefab/scene generators' silent early-outs
also log their specific reason now. Regression test: FoxNested fixture
(texture in textures/) through the DEFERRED write path, asserting every
write succeeds and Sources/textures/ lands. USER: re-import the chess set to
confirm end-to-end.
