# Weekly roadmap - week of 2026-09-05

> Seeds moved here 2026-08-31 from week-2026-08-29 (user triage: that week was
> over-stuffed and not feasible). Content carried verbatim; re-prioritize at
> week start.

## Seeded: occlusion culling (user schedule 2026-08-26)

Origin: Documentation/Parity/LumixEngine/parity-2026-08-26.md - occlusion
culling is called out as ABSENT ON BOTH SIDES (the GPU-driven bullet + the
gap list). Draconic already culls by view frustum (brute-force bounding
sphere, default-off - [[view-frustum-culling]]) and by screen-coverage LOD,
but NOTHING skips geometry hidden BEHIND other geometry, so a dense interior
still pays for every occluded draw. `DrawIndirect` exists in the RHI and is
UNUSED - occlusion culling is the natural first consumer of a GPU-driven path.

Goal: skip (or GPU-cull) draws whose bounds are fully occluded, layered on top
of the existing frustum + coverage culls, per view.

Approach decision is the FIRST gate (design before build):
- **Hi-Z GPU occlusion (two-phase)**: we already run a depth prepass (MRT /
  depth-prepass, [[renderer-postfx-plan]]); build a Hi-Z depth pyramid from it
  and test each object's bounds against the coarsest covering mip, then a
  second pass for the false-negatives. Pairs with the unused `DrawIndirect` and
  is the modern GPU-driven shape - but WebGPU has no cheap GPU->CPU readback, so
  the cull result must stay on-GPU (indirect draw args), not stall the CPU.
- **CPU software occlusion**: rasterize a few chosen occluders into a tiny
  depth buffer and test bounds CPU-side. Portable (no backend divergence, works
  on the Null device + headless tests), no readback latency, but needs
  occluder selection and costs CPU.
- Portability-first is the house bias (Vulkan + WebGPU + DX12), so weigh the
  Hi-Z path's WebGPU story carefully; a CPU pre-pass may be the pragmatic v1
  with the GPU path as the follow-on that also lights up `DrawIndirect`.

Open questions for week start:
- Which path (Hi-Z GPU vs CPU software vs hybrid), and does v1 target meshes
  only (like the LOD v1) or also terrain chunks / instanced sets?
- Occluder selection: all opaque geometry, or a curated occluder set?
- Interaction with clustered-forward + the depth prepass already in the graph
  (occlusion wants to run AFTER prepass, BEFORE the opaque pass records draws).
- Test story: coverage math is unit-testable headless (bounds-vs-Hi-Z or
  bounds-vs-software-depth); on-screen = a boxes-behind-a-wall probe.

## Seeded: investigate deferred rendering (user 2026-08-26)

Origin: same parity doc - Draconic is clustered FORWARD (GPU-built froxels,
16px tiles, MSAA-compatible) while Lumix is DEFERRED (CPU-built 64px clusters,
cheaper at high light counts, no MSAA). Several Lumix-ahead items downstream of
having a G-buffer: deferred G-buffer decals (ours are v1 forward
fullscreen-tri-per-decal blends - [[decals]]), cheaper many-lights.

This is an INVESTIGATION, not a build - produce a written recommendation, not a
renderer rewrite. The question: should Draconic add a deferred (or hybrid /
visibility-buffer) lighting path, or stay clustered-forward?

Grounding the investigation must not skip:
- We ALREADY write a fat MRT in the forward path (SV_Target1 view-space normal,
  Target2 motion, Target3 roughness/metallic - the terrain PS + mesh PS write
  the full 4-target GBUFFER the post stack reads). So a deferred path is closer
  than "from scratch" - it moves lighting to a fullscreen pass over that buffer.
- What forward BUYS us that a naive deferred loses: MSAA (clustered-forward is
  MSAA-compatible; classic deferred is not), portability (the whole
  Vulkan/WebGPU/DX12 clustered-forward + froxel + post infrastructure is built
  and test-covered - [[renderer-roadmap-status]]), and per-material shading.
- What deferred BUYS: high-light-count cost, G-buffer decals, and a cleaner
  many-effects G-buffer surface - weigh against the above.
- Consider the MODERN alternatives, not just classic deferred: a hybrid (deferred
  opaque + forward transparents/MSAA-edge), or a visibility-buffer / deferred-
  texturing path that sidesteps the fat-G-buffer bandwidth. Note the dead
  mesh-shader + ray-tracing pipeline objects that exist unused (parity §1 LOD).

Deliverable: a decision doc (Documentation/Specs/ or a Parity follow-up) with a
recommendation - stay forward, add an OPTIONAL deferred path, or go hybrid - and
if "add", a phased sketch. No renderer code this week.

## Seeded: GPU profiler + profiler visualization (user 2026-08-26)

Origin: same parity doc - "GPU PROFILER: Lumix-ahead - unified CPU/GPU profiler
timeline + pipeline statistics + NVML telemetry + PIX/RenderDoc; Draconic:
per-pass graph timestamps + CPU record-time report, SEPARATE from its CPU
profiler." We have both halves but they are text-dump only and disjoint.

What exists ([[profiler-system]]): `draconic.profiler` CPU scopes
(compile-gated) + a GPU `GraphProfiler` (per-pass Vulkan timestamps); the P key
DUMPS TEXT. Two gaps, and the user asked for both:

1. **GPU profiler** - broaden past the current per-pass Vulkan-only timestamps:
   finer scopes and coverage on the OTHER backends (WebGPU timestamp-query where
   available, DX12) so the GPU timeline is not Vulkan-only. Pipeline statistics
   (draw/primitive counts) alongside the timings if cheap.
2. **Profiler visualization** - replace the text dump with a VISUAL profiler
   panel (timeline / flamegraph) that UNIFIES the CPU scopes and the GPU
   timestamps on ONE per-frame timeline (the "separate" problem the parity note
   calls out). ImGui is already integrated ([[imgui-integration]]) - a candidate
   host for the overlay - but decide editor-panel vs in-app-overlay at design.

Open questions for week start:
- The shared data model: merge CPU scope trees + GPU pass timings into one frame
  record with a common clock/axis (GPU timestamps resolve a frame or two late -
  the view must align them to the right CPU frame).
- Where it lives: an editor profiler panel, an ImGui overlay in the samples, or
  both reading the same capture. Keep the capture layer UI-free.
- Portability: WebGPU timestamp-query is optional/limited and DX12 differs from
  Vulkan - the GPU side degrades gracefully where timestamps are unavailable.
- Compile-gating stays (the CPU profiler is already gated); the visualization is
  a debug tool, not shipped in dist.

## Seeded: contact shadows (user 2026-08-26)

Origin: same parity doc - "Lumix-only: contact shadows (SSS)" (also in Lumix's
deferred-effects list). Draconic ships CSM (cascaded shadow maps, texel-snapped,
per-cascade caster culling - [[renderer-roadmap-status]], shadows track) but has
NO screen-space contact shadows, so fine contact occlusion the cascade
resolution misses (the gap right where objects touch a surface, thin-feature
peter-panning) is absent.

What they are: a SHORT-RANGE screen-space ray march along the light direction
through the DEPTH buffer, adding fine occlusion at contact points. COMPLEMENTS
CSM (min of the two shadow terms), does not replace it.

Why it fits cleanly: we already have a depth prepass + the directional light in
the terrain/mesh view UBO. A contact-shadow term reads depth + light dir,
marches a handful of steps in screen space, and attenuates the DIRECT sun term.
Portable - it needs only the depth buffer, so it works Vulkan / WebGPU / DX12
with no special feature (unlike the DX12-only Lumix effects).

Scope for the week: a directional-light (sun) contact-shadow term with the usual
knobs (step count, max world distance, thickness), combined with the CSM sample.

Open questions for week start:
- Where it slots: folded into the forward lighting sun sample (cheapest, reuses
  the depth already bound) vs a separate screen-space pass writing a shadow
  factor the lighting reads.
- Noise/undersampling: dither the march start and let TAA resolve it (we have TAA
  - [[renderer-postfx-plan]]); pick step count vs cost.
- Sun-only v1 vs also the strongest local lights (defer locals unless cheap).
- Tests: a probe where an object hovering just above a surface shows a contact
  shadow CSM alone misses; Vk/WebGPU parity; the no-op case (contact shadows off)
  stays byte-identical.

## Seeded: CDLOD morph for terrain seam quality (user 2026-08-26)

Origin: same parity doc, terrain section - "Verdict: Lumix-ahead on seam
quality, Draconic-ahead on culling; CDLOD morph is Draconic's documented
deferral." Our terrain HARD-SWITCHES chunk LODs and hides the resulting cracks
with double-sided skirts (Terrain.cppm:124-146), which is robust and portable
but POPS at the transition; Lumix geomorphs at ring boundaries for popping-free
seams. This closes that gap. It was explicitly deferred in terrain.md and the
[[terrain-track]] deferral list.

Goal: continuous (CDLOD-style) LOD - in the terrain VS, morph a chunk's
higher-detail vertices toward their lower-detail PARENT positions across a
transition band as the camera nears the LOD switch, so a chunk blends smoothly
into the next level instead of snapping. Popping-free seams, no skirt pop.

Why it fits our model (and stays portable): it is a VERTEX-SHADER change, not
tessellation - the shared 65x65 chunk grid morphs odd vertices toward their even
(parent-grid) neighbours by a per-vertex morph factor derived from camera
distance vs the chunk's LOD range; height re-sampled from the already-bound
height texture. No hull/domain shader, so the WebGPU-portability ruling
(terrain.md:165-167, one path incl. WebGPU) HOLDS.

Scope for the week: the morph factor (per chunk, from its screen-coverage LOD
distance band - align the morph window to the EXISTING coverage LOD selection +
hysteresis, [[view-frustum-culling]] / mesh-lod coverage math) + the VS vertex
morph + a decision on skirts (morph aligns edges, so skirts may shrink to a
safety net or drop). Terrain-only.

Open questions for week start:
- Morph-factor source: a per-chunk uniform LOD-range (start/end) the VS lerps
  over, vs fully per-vertex - keep it aligned with the coverage LOD + hysteresis
  that already picks the chunk LOD so the morph completes exactly at the switch.
- The DEPTH/shadow VS: morph there too (so shadows match) or keep the coarse
  clamp (shadow-pass LOD clamping already exists) - matching avoids self-shadow
  swim.
- Skirts: keep as a belt-and-suspenders crack guard, or remove once the morph
  aligns boundary edges - measure.
- Tests: the morph-factor math is unit-testable headless (distance -> factor,
  boundary continuity); a Vk/WebGPU terrain probe that the morphed boundary is
  seamless (band-edge vertices agree with the neighbour LOD); morph-off stays
  byte-identical to today.

## Seeded: erosion + hole cutting for terrain (user 2026-08-26)

Origin: same parity doc, terrain section - "Both lack erosion + hole cutting"
and (holes/caves) "Both absent". Both are documented Draconic deferrals
([[terrain-track]] deferral list: holes, erosion/derived bakes). We already have
strong terrain sculpt: headless-testable brush cores (raise/lower/smooth/flatten,
Heightfield.cppm), region-delta undo per stroke, live BumpVersion re-upload. Two
DISTINCT workstreams hang off that spine - pick phasing at week start.

### Erosion (a heightfield transform)

What: hydraulic + thermal erosion on the heightfield samples - carve drainage,
settle talus slopes, deposit sediment - the natural-terrain look sculpting alone
cannot cheaply make. It is a SAMPLE-MATH op on the pure Heightfield model (the
headless-testable layer), so it reuses the sculpt spine: region-delta (or
full-field) undo + BumpVersion re-upload; no render change.

Scope: a terrain-page/tool BAKE with parameters - thermal (talus angle) as the
cheap starter, then hydraulic (droplet or grid, iterations / rain / evaporation /
sediment capacity). Live as a whole-terrain bake button and/or an erosion brush.
Open Qs: whole-field vs brush; a reusable Heightfield::Erode* core the sculpt
tool and a page button both call; determinism (seeded) for the headless tests.

### Hole cutting (a new terrain channel + render + physics)

What: mask out terrain cells for caves / cellar mouths / tunnels - a per-texel
HOLE MASK; the renderer skips holed cells and physics skips collision there.

The pieces:
- **Data**: a dedicated hole raster on the terrain (1 bit/texel), cooked to a
  sidecar exactly like the splat weights / palette streams (and DELETED-when-empty
  per the on-demand-sidecar rule just fixed). A hole BRUSH paints it on/off on
  the same brush spine.
- **Render**: v1 = sample the hole mask in the terrain PS and `discard` holed
  cells (portable, alpha-test-style, works Vk/WebGPU/DX12); crisp index-level cell
  skip at chunk build is the follow-on. Watch the interaction with skirts + the
  CDLOD morph at hole edges.
- **Physics**: Jolt HeightFieldShape already supports non-colliding cells
  (`cNoCollisionValue` - the collider path pads with it today, [[terrain-track]]);
  holed samples map to that at shape build, so collision opens up for free through
  the existing shared-Ref<Heightfield> collider. Verify the mapping.

Open Qs: hole-mask storage (dedicated 1-bit raster vs a sentinel height like some
engines use) - a dedicated mask is cleaner and avoids height aliasing; render
approach (PS discard v1 vs index skip); edge cleanup where a hole meets a skirt
or a morph band.

Tests: erosion core is deterministic + headless (seeded field -> expected
carve/deposit); hole mask round-trips through cook (present/absent + delete);
a Vk/WebGPU probe that a holed cell renders nothing and a Jolt test that a ray
passes through a holed cell but hits an unholed one.

## Seeded: grass / vegetation / foliage for terrain (user 2026-08-26)

Origin: same parity doc - "GRASS/VEGETATION: LUMIX AHEAD - TOTAL GAP, the LARGEST
in this domain. Draconic: zero vegetation of any kind ... planned as terrain
phase 3." Also the in-session user question (2026-08-26): how to paint grass on
terrain - answered as a SEPARATE scatter system, procedural density-grass first,
prop scatter second. This is a big track: DESIGN FIRST (write the spec), then
phase the build.

What Lumix ships (the target to reach, not copy verbatim): density-mask-driven
procedural grass (a type mask in the splatmap, ~1024 candidates/quad), per-quad
instance buffers + AABB cull + distance density fade, an authoring brush, prefab
scattering, and baked octahedral impostors as the far LOD.

Our hooks (why this is buildable on what exists):
- **Render path**: `InstancedMeshComponent` is SHIPPED (persistent buffer, shared
  ramp, O(1)/frame - [[instanced-mesh]]); grass renders as instanced draws, no
  new render primitive.
- **Partitioning**: the terrain quadtree already gives per-chunk sets - the
  [[terrain-track]] note is that grass instanced sets partition PER CHUNK so the
  shipped per-set coverage LOD selection + hysteresis works FOR FREE (build/free
  a chunk's grass set as it LODs / comes into range).
- **Placement mask**: a density/type mask (a dedicated grass raster, or derived
  from the top-K splat) drives where + which grass scatters.

Phasing (pick how far at week start; likely spec + P0 only):
- **Spec first** - the queued foliage design (procedural grass, then prop
  scatter). Land the mask + scatter + LOD/cull model on paper before code.
- **P0 procedural grass** - per-chunk deterministic (seeded) scatter of grass
  instances from the density mask into instanced-mesh sets, distance density
  fade + per-chunk AABB cull, optional VS wind sway. CPU scatter -> instanced
  draw is the portable v1; GPU-driven scatter (the compute cull/LOD/indirect
  chain) is the follow-on and shares the occlusion / DrawIndirect work seeded
  this same week.
- **P1 prop / prefab SCATTER brush** - paint mesh instances (rocks, bushes) with
  density + jitter + slope/height rules + collision rejection.
- **P2 DEFER** - baked impostors as the far LOD (also a standalone parity gap),
  GPU-driven scatter culling.

Open questions for week start:
- Density/type mask: a dedicated grass raster (paint brush + cooked sidecar, the
  splat-weights pattern) vs deriving from the splat top-K - a dedicated mask is
  more controllable but is another channel.
- Determinism: seed scatter PER CHUNK so it is stable frame-to-frame AND
  headless-testable (seed -> expected instance transforms).
- Set lifecycle: build/free per-chunk instanced sets as chunks stream + change
  LOD; interaction with the CDLOD morph seeded this week (grass rides the morphed
  surface height).
- Wind: VS sway now vs later; culling reuses the terrain quadtree AABBs.

Tests: deterministic scatter (seed -> instance transforms, headless); per-chunk
set build/free; a Vk/WebGPU probe that grass renders + density fades with
distance; scatter-off byte-identical.

## Seeded: root motion for animation (user 2026-08-26)

Origin: same parity doc, animation section - "ROOT MOTION: Lumix decisive - full
pipeline ... Draconic: ZERO hits for RootMotion/rootMotion across Code/. HARD
BLOCKER for locomotion-driven characters." We have the surrounding stack
(skeletal runtime, a Mecanim-style AnimationGraph FSM + BlendTree2D, clip/graph/
skeleton editor pages, CharacterVirtual) but nothing extracts or applies the
root bone's motion, so animation cannot DRIVE character movement. Directly
relevant to PaperKid ([[first-game-paperboy]]) if the bike/character is
animation-driven. Cross-cutting (import + clip resource + graph + scene +
physics) - DESIGN FIRST, then phase.

The four pieces (mirror Lumix's shape on our stack):
1. **Author/flag** - a per-clip root-motion flag + which bone is the root;
   import-time identification of the root track.
2. **Bake-time strip** - extract the root bone's per-frame DELTA (typically
   planar XZ translation + yaw) OUT of the pose so the skeleton animates
   IN PLACE, storing the delta separately on the clip resource
   (AnimationResource). New sidecar/field + a version bump (serializer-strict).
3. **Graph accumulation** - the AnimationGraph blends the per-clip root deltas
   by the SAME weights it blends poses (per-node accumulation), producing one
   root delta per frame. This is the subtle correctness piece.
4. **Scene application** - a component applies the accumulated delta to the
   entity Transform each frame; for a physics character, feed it to
   CharacterVirtual ([[physics-p1]]) instead of teleporting the transform.

Scope for the week: likely the spec + P0 (flag + bake strip + single-clip apply
to the entity transform) so a walk clip MOVES the entity in place; graph
blending of deltas + the CharacterVirtual path as P1.

Open questions for week start:
- Root DOF: planar XZ + yaw only (the common locomotion case) vs full 6-DOF -
  start planar, keep the data general.
- Loop wrap: the delta across a clip's loop boundary must be continuous (no
  teleport at wrap).
- Blend correctness: blending DELTAS by pose weights (and across FSM
  transitions / exit-time) must not drift vs the visual pose blend.
- Physics: apply to CharacterVirtual (collide + slide) vs a kinematic transform
  for non-physics actors - support both.
- Editor: the clip/graph preview shows in-place vs moving; the clip page marks
  root motion.

Tests: bake extraction (moving-root clip -> in-place pose + expected per-frame
delta, headless); two-clip delta blend by weight; application accumulates to the
right end transform over N frames; loop-wrap continuity; root-motion-off is
byte-identical (clip plays as authored).

## Seeded: inverse kinematics (user 2026-08-26)

Origin: same parity doc, animation section - "IK: Lumix decisive - FABRIK N-bone
graph node, editor-authorable. Draconic: none (only camera LookAt matrices).
Neither has foot-IK / look-at / two-bone." A total gap; sibling to root motion
(both are POST-graph pose modifications, and foot-IK plants feet DURING
locomotion so it pairs with the root-motion work above).

Goal: solve bone chains to reach targets - a pose-modification pass that runs
AFTER the AnimationGraph produces the pose and BEFORE skinning, adjusting named
chains toward world targets (hand-to-object, foot-to-ground, head look-at).

Solver set (build cheapest-first):
- **Two-bone (analytic)** - arms/legs; exact, cheap; the workhorse for foot +
  hand IK.
- **Look-at / aim** - single-bone (head/eyes/weapon) toward a target.
- **FABRIK (N-bone iterative)** - general chains (spines, tails); Lumix's choice.
- **Foot-IK** - two-bone legs + a ground RAYCAST (physics is available,
  [[physics-p1]]) + pelvis drop, so feet plant on uneven ground.

Where it runs (design decision at week start): AnimationGraph IK NODES (Lumix's
shape - authored in the graph) vs a post-graph list of IK CONSTRAINTS on the
animator component. Either way it mutates the local/model pose on the Skeleton
before the palette is computed, with a per-constraint blend alpha (IK weight).

Scope for the week: likely spec + P0 = the two-bone solver + look-at, applied
through one chosen seam with a blend weight; FABRIK + foot-IK (ground ray +
pelvis) as P1; editor chain authoring as it matures.

Open questions for week start:
- Target source: a world transform, another entity, or a raycast hit (foot).
- Pole/hint vectors for the two-bone knee/elbow direction.
- Blend: per-constraint alpha (IK off <-> full) so IK eases in/out, not a hard
  snap; ordering when multiple constraints touch overlapping bones.
- Interaction with root motion: foot planting vs the locomotion delta.
- Editor: how chains are authored (graph node vs component) + previewed.

Tests: two-bone reaches a REACHABLE target exactly + clamps an unreachable one
to max extension (headless); look-at aims within tolerance; FABRIK converges;
blend alpha 0 is byte-identical to the un-modified pose.

## Seeded: ragdoll (user 2026-08-26)

Origin: parity doc - "Both absent: ragdoll (Lumix REMOVED theirs; Draconic has
Jolt's Ragdoll / SkeletonMapper VENDORED but UNWIRED)." So unlike most gaps this
is a WIRING job, not a port: `ThirdParty/JoltPhysics/Jolt/Physics/Ragdoll/`
(Ragdoll.cpp/.h) + Jolt's SkeletonMapper already ship; nothing above the physics
layer drives them. We have the skeleton + Jolt world + CharacterVirtual
([[physics-p1]]) + the animation pose to feed it.

Goal: physics-driven skeletons - bones become constrained rigid bodies for
death/impact reactions, and (powered/active ragdoll) blend between the animated
pose and the simulation.

The pieces:
- **Ragdoll asset** - per-bone collision shapes + joint constraints (limits)
  built FROM the skeleton, authored/generated (Jolt's RagdollSettings shape).
- **Activate** - switch a character from animated to simulated: instantiate the
  Jolt Ragdoll, seed body poses from the current animated pose (no snap).
- **Read-back** - map the simulated body transforms back onto the skeleton pose
  (Jolt SkeletonMapper does exactly this) so skinning follows the simulation.
- **Blend (follow-on)** - powered ragdoll / partial (upper-body hit reaction) /
  get-up, driving Jolt motors toward the animated pose.

Scope for the week: likely spec + P0 = a ragdoll asset from the skeleton +
full-body activate + SkeletonMapper read-back (a character that goes limp on a
trigger). Powered/partial/get-up as P1.

Open questions for week start: where the ragdoll asset lives + how it is authored
(from the skeleton page? auto-generated shapes?); the animator <-> ragdoll
hand-off (pose seeding + read-back each frame); interaction with CharacterVirtual
(disable the controller while simulated); LOD/sleep for many ragdolls.

Tests: asset build from a skeleton (bodies + joints match the bone hierarchy);
activate seeds bodies from the pose (no first-frame snap); SkeletonMapper
read-back drives the pose (headless, a dropped ragdoll settles); ragdoll-off is
byte-identical (animation plays normally).

## Seeded: morph target animations (user 2026-08-26)

Origin: parity doc - "Both absent: ... morph targets (Draconic has a RESERVED
SEAM AnimationPose.cppm:23 + importer TODO; Lumix ZERO TRACE)." A chance to get
AHEAD, not just reach parity: the runtime pose already reserves
`morphWeights` (per-morph-target weights, `HasMorphWeights()`), and glTF carries
morph targets natively - so the seam exists, the import path just does not read
them yet.

Goal: blend-shape / morph-target animation - per-vertex position (+ optional
normal) DELTAS blended by weight, for facial expressions, shape variation, and
authored deform animation.

The pipeline (mirrors the skin/LOD stream shape we already cook):
- **Import** - the glTF loader ([[image-model-port]]) reads the primitive morph
  targets (position/normal deltas per target) + default weights + any animation
  channels that target the weights array (the importer TODO).
- **Cook/store** - the cooked mesh gains a MORPH-TARGET stream (N targets of
  vertex deltas) beside the geometry/skin streams; version bump = the migration
  (pre-bump meshes = zero targets).
- **Drive weights** - per-target weights over time. Our property/timeline
  animation is decisive (parity) - weights can ride a weight track, or a
  dedicated morph channel on the clip; the reserved `AnimationPose.morphWeights`
  carries them into the frame.
- **Apply** - the skinning VS sums `pos += sum(w_i * delta_i)` BEFORE the bone
  palette (a small loop for facial rigs; a sparse/compute path if target counts
  grow). Portable - delta buffers bound + a weights uniform, no special feature.

Scope for the week: likely spec + P0 = import + cook the delta stream + VS apply
with SET/scripted weights (a face mesh deforms when a weight changes); animating
the weights via clips/property animation as P1.

Open questions for week start:
- Weight-count budget + the VS-loop-vs-compute threshold (facial rigs are few
  targets; some assets have dozens).
- Deltas: positions only vs positions + normals (lighting on strong morphs).
- Where weights are authored/driven: reuse the property-animation channels vs a
  dedicated morph track on the clip.
- Interaction with skinning (morph THEN skin) + motion vectors (prev-frame
  weights, mirroring the prev-frame bone palette we already keep).

Tests: import round-trips N targets + deltas; VS apply moves vertices by the
weighted sum (a pixel/vertex probe: weight 0 == base mesh byte-identical, weight
1 == the target); weights animate over a clip; compat (a no-morph mesh loads as
zero targets, byte-identical).

## Seeded: animation compression (user 2026-08-26)

Origin: parity doc - "Compression: Lumix decisive - constant-track collapse +
per-channel bit-packing driven by authored error tolerances + smallest-three
quats, with in-editor size stats. Draconic stores RAW f32 keys
(AnimationResource.cppm:163) - THOUGH variable keyframe density + cubic tangents
are preserved (the better substrate for a future compressor)." So we pay full
raw-key memory/bandwidth, but our data model is the RIGHT substrate to compress.

Goal: an offline (cook-time) clip compressor with error-bounded quality, a
compact runtime form the sampler decodes, and editor size stats.

The techniques (biggest-win-first):
- **Constant-track collapse** - a track whose keys never change (within epsilon)
  collapses to ONE value; huge for the many bones a clip does not move.
- **Keyframe reduction** - drop keys the curve can reconstruct within an AUTHORED
  per-channel error tolerance, exploiting the cubic tangents we already keep
  (fit-and-cull, not fixed decimation).
- **Quantization / bit-packing** - rotations as SMALLEST-THREE quats (drop the
  largest component, N-bit each + index); translations/scales range-packed per
  track to the authored precision.
- **Editor size stats** - per-clip before/after on the clip page (Lumix's parity
  feature).

Scope for the week: P0 = constant-track collapse + error-bounded keyframe
reduction (the memory/quality wins, minimal format churn); P1 = quantization /
bit-packing + the smallest-three quat form + the editor stats. Clip resource
version bump = the migration (raw clips still load; re-cook compresses).

Open questions for week start:
- Error tolerance: a global default + per-clip (or per-channel) override; the
  metric is POSE error (bone position/rotation deviation), not raw key delta, so
  a distant bone tolerates more angular error.
- Runtime cost: the sampler must decode the compact form cheaply (the sample hot
  path); keep the smallest-three unpack + range-expand branch-light.
- Ordering vs mesh LOD's meshopt work: independent, but both are cook-time size
  wins worth landing behind a size-report so regressions are visible.

Tests: a compressed clip samples within the authored tolerance of the raw clip
at every frame (per-bone pose-error bound, headless); a constant track collapses
to one key; measured size reduction logged by the cook test; sampler round-trip
(compress -> decode == within tolerance).

## Seeded: alpha-coverage-preserving mips + stochastic mips (user 2026-08-26)

Origin: parity doc, textures - "Lumix-ahead on two mip features Draconic lacks:
alpha-coverage-preserving mips and stochastic mips" (also the gaps list,
"coverage-preserving mips"). Both are ALPHA-TESTED (cutout) texture-quality
features - foliage cards, chain-link, grass blades - so they PAIR DIRECTLY with
the grass/vegetation item seeded this week (cutout grass thins and shimmers at
distance without them).

- **Alpha-coverage-preserving mips (COOK)**: a plain box-filter mip DOWNSAMPLE
  shrinks the fraction of texels above the alpha-test threshold, so a cutout
  silhouette THINS or vanishes at distance. Fix (Castano/NVIDIA): after each mip
  reduction, binary-search an alpha SCALE so that mip's coverage (fraction >=
  threshold) matches mip 0, and bake the scaled alpha. Lives in the texture cook
  (TextureAsset mip generation) behind a per-texture flag (auto for cutout/
  foliage presets). This is the general cousin of the terrain
  [[terrain-coverage-mask]] R4 deferral (coverage-preserving mip scaling).
- **Stochastic / hashed alpha ("stochastic mips") (SHADER)**: alpha-test with a
  per-pixel HASHED threshold so the cutout DISSOLVES smoothly (order-independent)
  instead of a hard binary edge, and mip transitions stop shimmering; TAA
  ([[renderer-postfx-plan]]) resolves the dither to smooth coverage. A per-
  material option on alpha-tested surfaces. (Confirm the exact reading of
  "stochastic mips" at week start - hashed-alpha testing vs stochastic mip-level
  selection; hashed alpha is the usual foliage companion to coverage mips.)

Scope for the week: P0 = alpha-coverage-preserving mips in the texture cook (the
concrete, testable win) + a cutout/foliage preset that turns it on; hashed-alpha
in the cutout material shader as P1, landing WITH or just before grass so its
cards read correctly at range.

Open questions for week start:
- Which textures opt in (a flag, auto-on for the cutout/foliage preset) and where
  the alpha-test threshold comes from (texture setting vs material).
- Cook version bump = re-cook affected textures; unaffected textures byte-
  identical.
- Stochastic reading (above) + TAA interaction (jitter the hash per frame so TAA
  integrates it).

Tests: coverage-preserving - a checkerboard/known-coverage alpha texture keeps
~constant coverage (fraction >= threshold) across ALL mips vs the box-filter
baseline that decays (headless, measured per mip); non-cutout textures byte-
identical; hashed alpha - the dither is deterministic + TAA-stable (probe).

## Seeded: impostors - octahedral bake as final LOD (user 2026-08-26)

Origin: parity doc - "Lumix ships octahedral IMPOSTORS (bake + runtime + shadow)
... impostor baking as the last LOD"; Draconic's mesh-LOD chain ends at the
COARSEST REAL MESH (no impostor level). Also the deferred P2 of the grass item
seeded this week. An impostor is a single billboard that samples a baked
octahedral atlas of a mesh's appearance from many view directions - it REPLACES
geometry entirely at extreme distance (forests, rock fields, distant props), the
biggest far-field win for dense vegetation.

Goal: an octahedral impostor as the LOD level AFTER the last real mesh LOD -
bake, runtime billboard, and a cheap shadow.

The pieces (builds on shipped mesh LOD [[mesh-lod-track]] + instanced mesh
[[instanced-mesh]]):
- **Bake (offline/editor)** - render N x N octahedral views of the mesh into an
  atlas: albedo + packed NORMAL (so it still lights) + DEPTH (parallax / edge).
  An editor bake step (the reflection-probe bake is the precedent for an
  editor-driven capture) producing an impostor asset / a mesh-LOD extension.
- **Runtime** - a camera-facing billboard, selected by the SAME coverage LOD math
  beyond the last mesh LOD; the VS/PS pick the octahedral cell by view direction
  (optionally blend the nearest cells to hide the pop), lit via the baked normal.
- **Shadow** - the impostor casts a cheap shadow (Lumix bakes this too).

Scope for the week: likely spec + P0 = bake + runtime billboard for ONE mesh at
extreme distance (a tree that becomes a card far away, view-correct). Cell
blending, shadow, and instanced-crowd integration (grass/forest) as follow-ons.

Open questions for week start:
- Atlas resolution + view count (octahedral grid density, e.g. 8x8-16x16);
  hemispherical (ground props) vs full sphere.
- Channels: albedo + normal + depth (depth for parallax/soft edge) - budget vs
  quality.
- Integration point: a SYNTHETIC final level in the mesh LOD chain (one
  Ref<StaticMesh> stays, coverage picks the impostor) vs a separate component.
- Adjacent-cell blend to hide the octahedral pop; where the bake runs + asset
  storage; the shadow-caster form.

Tests: bake produces a view-indexed atlas; the runtime billboard picks the
correct cell for a given view direction (the impostor from angle X matches the
real mesh's silhouette within tolerance, headless/probe); coverage LOD selects
the impostor beyond the last mesh LOD; impostor-off = the mesh chain unchanged.

## Seeded: PLAN - in-editor shader editing + material/shader node-graph editor (user 2026-08-26)

Origin: parity doc - "MATERIALS: par - both shader-ref + property-set with editor
pages, NO node graph either side" and "SHADERS: Draconic-ahead ... Lumix-ahead on
one nicety: in-editor shader editor WITH LIVE DISASSEMBLY." User asks for this as
a PLANNING item - the deliverable this week is a DESIGN DOC, not a build.

We are well-positioned (the plan should exploit what already exists):
- Offline shader cook to a versioned SPIR-V/DXIL/WGSL pack with a variant lint
  ([[shaders-track]]) - so "live disassembly" is our COOK OUTPUT, already produced.
- `CodeEditView` + per-language lexer registry ([[code-editor-track]]) - a shader
  code page reuses it (an HLSL lexer + save -> re-cook / hot-reload).
- `MaterialEditorPage` (shader-ref + property set + preview) - the material page a
  graph would feed.
- A `NodeGraph` UI foundation (post-port, [[nodegraph-evolution]]) + the working
  `AnimationGraphPage` node editor (3-pane, persisted node positions) as the
  precedent for a graph editor.
- Custom materials without renderer changes ([[renderer-direction]]) - what a
  shader graph would author.

Two candidate tiers for the plan to weigh + sequence:
1. **In-editor shader source editing (the cheaper win)** - open a `.hlsl` in a
   CodeEditView page, edit with the HLSL lexer, save -> the offline cook re-runs
   + hot-reloads, and show the cooked SPIR-V/WGSL DISASSEMBLY (Lumix's nicety, but
   from our compiler-free cooked pack, not a runtime FXC). Modest - reuses the
   code editor + the shader cook.
2. **Material / shader NODE GRAPH (the big feature; neither engine has it)** -
   visual nodes (texture sample, math, blend, ... -> surface output) that GENERATE
   HLSL fed through the EXISTING cook (SPIR-V/DXIL/WGSL, variant lint, compiler-
   free dist all still apply). Authors custom materials without hand-writing
   shaders. Rides the NodeGraph UI + the AnimationGraphPage editor pattern.

The plan must decide: ship tier 1 first (fast, high-value, low-risk) with tier 2
as the larger follow-on, or commit to the node graph directly; the node-graph
DATA MODEL (nodes -> generated HLSL, variant/feature interaction, the material
property surface it exposes); how generated shaders ride the variant/lint cook;
and the graph's serialization + editor shape (reuse AnimationGraphPage's spine).

Deliverable: the design doc (Documentation/Specs/) with the tiering
recommendation + a phased sketch for the chosen path. No shader-graph code this
week (tier 1's code page could be a stretch build if the plan lands early).

## Seeded: investigate large world support (user 2026-08-26)

Origin: parity doc - "Lumix-ahead on WORLD SCALE - DVec3 double-precision
positions + camera-relative getRelativeMatrix. Draconic is FLOAT-ONLY: no
large-world support." Our scene transforms store `Float3` positions, so beyond a
few km from origin f32 precision degrades - vertex swim, z-fighting, physics
jitter. INVESTIGATE first (does our content need it, and which approach), not a
committed build.

The approaches, cheapest-first (the investigation should tier + recommend):
- **Camera-relative rendering (the pragmatic first step)**: keep float storage,
  but translate geometry to be RELATIVE TO THE CAMERA before the GPU (subtract the
  camera position at high precision on the CPU, upload camera-relative matrices).
  Fixes the VISIBLE jitter/z-fighting with no storage change, and is PORTABLE -
  GPUs are float-only, so rendering MUST go camera-relative even if positions
  become double later. Likely the recommended v1; moderate, high-value.
- **Double-precision scene positions (Lumix's DVec3)**: store positions as f64,
  derive camera-relative float matrices for rendering. Fixes gameplay/physics math
  far out too, but INVASIVE (the whole transform system + serialization + physics
  interop; Jolt is float unless the heavy double build is used). Only if truly
  planet-scale.
- **Floating origin / rebasing**: periodically recenter the world around the
  player (shift all positions) to stay near origin - simpler than double, but
  needs discontinuity handling + touches anything caching world positions.
- Relates to the STREAMING/PAGING gap (parity: both absent; Draconic documents
  multi-terrain tiling as the interim) - tiles with local origins compose with any
  of the above.

Deliverable: a recommendation - do our target games need large worlds, and if so
which tier (almost certainly camera-relative rendering FIRST since it is the
portable prerequisite, double/floating-origin only if the scale demands it) - with
a phased sketch. No large-world code this week beyond a possible camera-relative
spike if the investigation lands early.

Open questions for week start: the target world SCALE (what do the games actually
need - a big level, or planet-scale?); how camera-relative threads through the
render graph + the froxel/clustered-forward view UBOs + shadows (all consume view
matrices); physics far-from-origin behaviour (Jolt float limits); whether
multi-terrain tiling already covers the near-term need.

## Seeded: gizmo vertex snapping (user 2026-08-26)

Origin: parity doc, editor - Lumix has VERTEX SNAPPING; "Draconic snaps drag
DELTAS only." Our transform gizmo quantizes the drag delta to grid increments
(translateSnap/rotateSnap/scaleSnap, Ctrl-held, PlayCanvas-style - Gizmo.cppm:
89-92), but cannot snap an object's point ONTO a vertex of other geometry -
precise assembly (align this corner to that wall's corner) is manual.

Goal: a vertex-snap mode for the translate gizmo - while dragging (a modifier /
toggle), snap the moved object so its snap SOURCE point lands exactly on the
nearest VERTEX of the geometry under the cursor.

The pieces (builds on the existing gizmo + scene pick):
- **Target pick** - ray-pick a mesh under the cursor (QueryRay, already used for
  selection/terrain), then find its NEAREST VERTEX to the hit (needs the mesh's
  vertices in world space at edit time - from the bound mesh resource).
- **Source point** - v1 = the moved object's PIVOT snaps to the target vertex;
  v2 = pick a source vertex ON the moved object (Blender-style) so any corner can
  be the anchor.
- **Apply** - offset the object so source -> target; this is an ABSOLUTE snap
  (unlike today's relative delta quantization), a distinct gizmo mode.

Scope for the week: P0 = pivot-to-nearest-vertex snap on the translate gizmo
under a modifier, with a highlight of the target vertex. Source-vertex pick +
edge/face (surface) snapping as P1.

Open questions for week start:
- UX: the modifier/toggle (a held key like Blender's V, vs a toolbar snap-mode
  toggle vs Ctrl-variant) - and coexistence with the existing delta snap.
- Vertex data access at edit time: nearest vertex to the ray hit - do we walk the
  bound mesh's CPU vertices, or sample the picked triangle's corners (cheaper, and
  enough for pivot->vertex)?
- Snap targets: vertex only (v1) vs also edges / faces / the grid; visual feedback
  for the candidate target.

Tests: with vertex snap on, dragging an object onto a mesh places its pivot at the
EXACT nearest vertex position (headless gizmo test, known mesh + known drag);
snap-off leaves the existing delta-snap behaviour byte-identical.

## Seeded: memory profiling (user 2026-08-26)

Origin: parity doc - Lumix has "a hierarchical MEMORY profiler with tag
allocators ... memory profiler UI grouping live allocations by stack tree";
"Draconic: no memory profiling" (called out as part of "Draconic's WIDEST editor
gap" with the CPU/GPU timeline). We already have the PRIMITIVE:
`TrackingAllocator` reports LiveBytes / TotalBytesAllocated / PeakBytes
(Core/Memory/TrackingAllocator.cppm) - what is missing is per-subsystem
attribution and a VIEW. Pairs with the GPU-profiler + profiler-visualization item
seeded this week (memory is the third panel beside CPU + GPU).

Goal: know WHERE memory goes (per subsystem/tag: render, physics, audio, scene,
UI, ...) with live + peak + an over-frame graph, and catch growth/leaks.

The pieces (build on TrackingAllocator):
- **Tagged allocators** - a per-subsystem tag/category so live/peak ROLL UP by
  owner (Lumix's tag allocators). Cleanest on our stack: a `TrackingAllocator`
  instance per subsystem (or a tag threaded through Allocate), reported into a
  registry.
- **Aggregation** - a registry that each frame collects per-tag live/total/peak
  (the numbers TrackingAllocator already exposes).
- **View** - a memory panel: grouped bars/tree by tag + a memory-over-TIME graph,
  UNIFIED into the profiler visualization (same panel as CPU/GPU). Compile-gated
  debug tool, not shipped in dist.
- **Stretch** - call-site / stack-tree grouping for leak hunting (ASAN already
  covers hard leaks; this is for growth attribution).

Scope for the week: P0 = per-subsystem tagged tracking + the aggregation registry
+ a live/peak readout and over-frame graph (reuse TrackingAllocator per
subsystem); stack-tree/call-site grouping deferred. Land the panel INTO the
profiler-viz work so there is one profiler surface, not three.

Open questions for week start:
- Tag assignment: a TrackingAllocator per subsystem (natural given the type
  exists) vs a tag argument on Allocate (finer but more invasive) - start
  per-allocator.
- Overhead: keep tracking cheap + compile-gated (the CPU profiler is already
  gated); atomics on the hot alloc path only when enabled.
- UI home: fold into the profiler-visualization panel (shared timeline) rather
  than a separate window.

Tests: a tagged-allocator registry rolls up per-tag live/peak correctly (allocate
+ free under tags, assert the rollup, headless); peak tracks the high-water mark;
tracking-off (gated out) is zero-overhead.

Context: the data source shipped with I5 this week (per-tag live/peak/total +
`MemoryTagReport()` rollup rows - record in week-2026-08-29.md); the
panel/graph work here remains.

## Seeded: editor key rebinding (user 2026-08-26)

Origin: parity doc, editor platform - "Lumix-ahead DECISIVELY on keybindings - a
full rebinding UI with COLLISION DETECTION vs Draconic's four HARDCODED
accelerators (Ctrl+Z/Y/S/...)". Our editor shortcuts are hand-registered on the
UI `ShortcutManager` (ApplicationImpl.cpp:2989+, `AddGlobal(KeyCode, Modifiers,
...)`) - fixed, not user-remappable, not persisted. NOTE: distinct from the GAME
input subsystem's rebinding ([[input-subsystem]], runtime rebind capture) - that
maps GAME actions; this is the EDITOR's own hotkeys.

Goal: user-rebindable editor shortcuts - a UI to view/change bindings with
collision detection, persisted per-user.

The pieces (build on the ShortcutManager + editor settings):
- **Named action registry** - each editor shortcut becomes a NAMED command
  ({id, default binding, handler}) instead of a hardcoded `AddGlobal` - so a
  binding can be remapped by action id. Migrate the existing Undo/Redo/Save
  accelerators into it first.
- **Rebinding UI** - a Preferences panel (the EditorPreferencesDialog exists)
  listing actions + their current binding, capturing a new key, and DETECTING
  COLLISIONS (two actions on one chord). The game input subsystem already has a
  key-CAPTURE widget/flow to reuse.
- **Persistence** - store overrides in the PER-USER editor settings
  (editor.settings.xml, the [[reorg-role-grouping]]-era EditorUiSettings home) as
  a new `EditorShortcutSettings` section; on startup the ShortcutManager registers
  each action's binding (default, unless overridden).

Scope for the week: P0 = the named-action registry (wrapping today's hardcoded
shortcuts) + persisted overrides + a rebind UI with collision detection. Per-user
(a shortcut is a user preference, not per-project - the asset-browser/scene-grid
scope precedent: match the scope of the thing).

Open questions for week start:
- Registry shape + how existing hardcoded `AddGlobal` sites migrate onto it.
- Collision policy: warn, block, or reassign-and-clear-the-other.
- Reuse the input subsystem's key-capture widget vs a small editor-local one.
- The per-user settings section + defaults (a missing/older file = built-in
  bindings; serializer-strict-gated).

Tests: the action registry round-trips a rebinding through the settings store
(default vs override); collision detection flags a duplicate chord; applying a
stored override registers the new chord on the ShortcutManager; no override =
the built-in accelerators unchanged.

## Seeded: crash reporting + dumps (user 2026-08-26)

Origin: parity doc, editor platform - Lumix has "crash reporting (WINDOWS)";
Draconic has none. A chance to be AHEAD via portability: Lumix's is Windows-only,
ours should be CROSS-PLATFORM (Linux + Windows; web is separate) - the house bias
is platform logic in Core/System BACKENDS, not #if ([[naming-full-and-backend-fanout]]).

Goal: on an unhandled crash (segfault / abort / unhandled exception), capture a
STACK TRACE + a minimal dump + the log tail to a crash folder, so a crash in the
editor or the exported PLAYER leaves a diagnosable artifact instead of vanishing.

The pieces (a Core/System platform feature, per-backend):
- **Handler install** - POSIX: `SIGSEGV`/`SIGABRT`/`SIGFPE` handlers; Win32:
  `SetUnhandledExceptionFilter`. Async-signal-SAFE on the POSIX path (minimal
  work, no allocation in the handler).
- **Capture** - a backtrace (POSIX `backtrace` / Win32 StackWalk) + the crashing
  context; include build id/version, loaded modules, and the recent LOG TAIL (the
  logger already buffers).
- **Write** - a crash report file in the user-data dir (`GetUserDataDirectory`);
  Win32 minidump via `MiniDumpWriteDump` (dbghelp) as the richer artifact.
- **Symbolication** - resolve frames to symbols (DEBUG builds carry them -
  [[dev-build-config]]); in-process `backtrace_symbols` now, offline addr2line on
  raw offset+build-id as the robust path.

Scope for the week: P0 = install the handler + write a stack trace + log tail +
build/version to the user-data crash folder on POSIX and Win32 (raw frames if
symbolication is not ready). Win32 minidump + any upload/telemetry are follow-ons
(upload is an outward-facing decision - keep local-only by default).

Open questions for week start:
- Symbolication path (in-process vs offline addr2line on offsets + a stored build
  id) and whether dist strips symbols.
- What the report contains (stack, log tail, version, OS/GPU info) + the format.
- Editor AND exported player both install it (the player is where field crashes
  happen); web/Emscripten is out of scope (browser handles it).
- Signal-safety discipline on the POSIX handler; re-entrancy / double-fault guard.

Tests: the report WRITER formats + writes a given synthetic stack/context + log
tail correctly (headless); install/uninstall of the handler is clean; a
dev-only "force crash" command verifies end-to-end manually (a real crash is not
unit-testable).

## Seeded: mountable content directories (user 2026-08-29)

Origin: reviewing Sedulous's VFS while building the editor's `data://` discovery+mount. Sedulous lets
a project MOUNT MULTIPLE content directories. The ask: allow an editor project to mount additional
content dirs beyond its own Sources/ - so authored content can be packaged (zip a data dir), shipped
to someone else, and they DOWNLOAD + MOUNT it into their editor project (asset packs, shared prefabs,
sample content). Builds on foundation.vfs (schemes/mounts) + the content DB.

Sketch: a project references N mounted content roots (its own + external packs); the content DB and
asset browser union them; GUID stability across packs (a pack's assets keep their guids so references
resolve after mounting). Decide: mount persistence (in project settings), conflict handling (same
guid in two packs), whether external mounts are read-only, and product/cook handling for mounted
sources. Relates to the `data://` discovery work + [[vfs-and-resource-stack]].

## Carried: page-toolbar roll-out - remaining pages (from week-2026-08-29)

The PageToolbar (Save / Undo / Redo / Discard, self-gating on dirty + stack
state) landed 2026-08-31 on: sound cue (already had it), bus layout, collision
shape, texture, image. The audit corrected the old seed: NO other page carried
it. Remaining editing pages to wire (same recipe - wrap the content root in a
column with the toolbar on top, Refresh() in OnUpdate):
material, animation clip, animation graph, particle effect, font, heightfield,
terrain, input map, and the generic asset form. Gotcha from the first batch: a
page that mutates its asset DIRECTLY (no commands) must override DiscardChanges
to reload from the source DB - the base "undo the stack" default clears dirty
without reverting (collision shape needed this); pages whose edits are blob
commands (texture, image, bus layout) keep the base. Interface units that add
the OnUpdate override need `import foundation.runtime.client`.

## Seeded: real-time global illumination (user 2026-09-03)

Origin: user request 2026-09-03; design discussion in-session. Constraint that
decides everything: WEBGPU-FIRST - hardware ray tracing does not exist on the
web target, so HWRT can only ever be a native-only enhancement behind
capability flags (the RT pipeline objects in Vulkan/DX12 are dormant but
proven - Sample021/028), never the GI itself.

Recommended ladder (agreed direction, not yet signed off):
- **Tier 1 - SSGI**: "SSR for diffuse" - cosine-weighted screen rays sampling
  last frame's lit color, sky/probe fallback on miss, temporal accumulation.
  Rides the existing SsrPassImpl trace core + GTAO sampling + TAA/motion
  vectors. Small, ships on every backend, kills the flat-ambient look.
- **Tier 2 - irradiance probe volumes (the backbone)**: IrradianceVolumeComponent
  grid of diffuse probes; DDGI-style octahedral irradiance + mean/variance
  depth for visibility weighting (anti-leak) - or SH9, resurrecting the
  deferred SH9 item from [[reflection-probes-plan]]. Updates amortized
  round-robin through the EXISTING runtime probe-capture path (N probes/frame,
  time-of-day-speed dynamism, no RT needed). Dynamic objects sample the
  volume; specular stays reflection probes + SSR. Plain textures + compute =
  WebGPU-safe.
- **Tier 3 (later)**: ray-traced probe UPDATES against a scene BVH - compute
  traversal everywhere, dormant HWRT natively. Upgrades tier 2's update speed,
  not its representation.

Rejected: voxel cone tracing (poor clustered-forward + WebGPU fit, dated),
SDF-GI/Lumen-style (needs a mesh-distance-field pipeline we don't have; thin
walls leak; live terrain sculpting would invalidate the global SDF
constantly; the SDF only answers WHERE a ray hits - a surface-radiance cache
as big as the SDF layer is still needed on top).
Radiance cascades = watching brief only.

SURFEL-CACHE GI (GIBS/SEED family) - evaluated 2026-09-03, the candidate
EVOLUTION of tier 3 (not a step before it): screen-spawned world-persistent
surfels accumulating irradiance via rays; no content pipeline, no thin-wall
problem, dynamic/skinned-friendly, ~MBs of memory, multi-bounce ~free
(surfels sample surfels), ray count scales with surfel count not resolution
(software-BVH friendly, WebGPU-feasible: compute + atomics + indirect).
Gated on the scene BVH (shared with the lightmap baker - build once, use
thrice) and on debug views (surfel systems fail by boiling/flicker, hardest
class to debug blind). It COMPOSES with tier 2, never replaces it:
never-seen areas have no surfels, so probe volumes stay as the fallback
layer; specular stays SSR + reflection probes. Decision point: once the BVH
exists, choose RT-updated probes vs surfel cache over probe fallback.

Open rulings for week start: probe encoding (DDGI octahedral+depth vs SH9),
volume placement/authoring UX, SSGI half-res vs full-res, where the indirect
term lands in the forward shader (replaces ambient for lit pixels).

## Seeded: lightmap baking (user 2026-09-03)

Origin: user request 2026-09-03; design discussion in-session. LAST in the
decided order (after debug views -> SSGI -> probe volumes). A PIPELINE
feature more than a renderer one - the cook substrate (content-hash recipes,
headless CLI, WriteData sidecars) and the nav-bake editor precedent (progress
UI, parallel bake, partial rebake, stage viz) are exactly the needed shape.

Recommended shape (not yet signed off):
- **UV2 unwrap**: vendor xatlas (MIT - fits the license rule). Chart
  generation at model import or bake time; meshoptimizer does not unwrap.
- **Baker core**: GPU compute progressive path tracer (Godot/Bakery-style):
  lightmap-space G-buffer raster (position/normal per texel via a UV2-space
  pass), CPU SAH BVH of the static scene uploaded to compute, accumulate
  bounces, dilate, a-trous denoise. Host-side tool = Vulkan/DX12 regardless of
  the web runtime; dormant HWRT can accelerate later. AVOID vendoring embree
  (heavyweight; collides with [[vendored-binary-portability]]).
- **Directionality**: L1 SH lightmaps (~4 textures, Witness/Bakery-style) so
  normal maps survive on static geometry. RGBA16F first; this is the concrete
  forcing function for the BC6H gap in the parity doc.
- **Runtime**: per-instance UV2 atlas scale/offset + "lightmap static" flag on
  MeshRenderer; forward shader substitutes lightmap irradiance for
  probe/ambient diffuse on static instances. Dynamic objects sample the GI
  tier-2 probe volume - and the SAME baker bakes those probes, so static and
  dynamic agree.
- **RULING NEEDED**: GPU path tracing is not byte-deterministic across GPUs,
  which collides with the cook's same-input->byte-identical philosophy.
  Proposed: lightmaps are an AUTHORED ARTIFACT (explicit bake action, stored
  source-side like the baked navmesh), not a cook product - cook stays
  deterministic, bake uses whatever hardware is present.

DECIDED ORDER (user 2026-09-03) across all four renderer seeds:
1. debug views -> 2. GI tier 1 (SSGI) -> 3. GI tier 2 (probe volumes) ->
4. lightmap baking LAST. Runtime probe updates / RT acceleration remain a
later follow-on; the baker's scene BVH seeds GI tier 3 - build once, use
twice.

## Backlog absorbed from roadmap.md (archived 2026-09-01)

The engine roadmap synced to reality and moved to Documentation/Archive/
roadmap-history.md; everything still open there lives HERE now (the weekly docs
are the one planning surface). Grouped by track, each with its trigger/state:

**Core hardening (build when a consumer appears)**
- Local convenience test-runner (one command -> all ctest presets).
- SIMD hot-path migration - MEASURED and deferred: the naive swap was ~1.05x;
  a real win needs data kept in SIMD across a whole hot loop. Profile-gated.
- Concurrent / lock-free containers (only the JobSystem's internal deque exists).
- Allocators: general-purpose heap (TLSF/buddy) + debug guard-page/quarantine.
- Unicode normalization + collation (basic decode/encode only today).
- Pak compression (zstd/lz4; kCompressionNone today) + runtime write/watch.

**Rendering**
- Texture cook tail: BC6H for HDR + the normal-map format fix (SPEC READY:
  texture-compression-hdr-and-normals.md, ships after texture-page-ux.md) and
  KTX2/basis import (the WASM/Android transcode path).
- GPU memory sub-allocation: Vulkan -> VMA, DX12 -> D3D12MA (one allocation per
  resource today; the vkAllocateMemory-FAILED warnings stay as the tripwire).
- Spatial acceleration STRUCTURE (BVH/octree) - brute-force sphere culling
  shipped; dense/shadowed scenes are the trigger.
- Post-processing phases 4/5 - phases 1-3 built and PAUSED pending the user's
  on-screen verify; 4/5 resume after.
- DX12 blit pipeline TODO (needs D3DCompile, DxDevice.cppm) - Windows-side.
- GPU-compute particle sim (CPU sim + full authoring triad shipped).
- Scene streaming / partitioning.

**Subsystems**
- Behavior trees (navmesh/pathfinding/crowd complete; this is the AI half).
- Networking tail: per-field bitmask delta, receive-RPC-into-script,
  project-settings -> NetworkStartup, Win32 socket backend validation; the
  net-extraction spec (P1-P4) is written and unbuilt.
- Save-game (settings/config + i18n foundation shipped; save-game is the gap).

**Portability**
- Web tail: web networking (WebSocket client), Firefox validation, export
  integration ([[web-platform-track]] carries the state).
- Android: shell backend + touch/input + build (Vulkan covers the GPU side).

**Editor + MVP closer**
- Asset browser: drag-from-assets + importer chooser (thumbnails shipped).
- Export-depth hardening + ONE real end-to-end export smoke - the last MVP gap.
- Integrated showcase sample: PaperKid covers input/physics/audio/scripts/
  prefabs/game-UI; a networking showcase remains (net demo scripts are the seed).

## Backlog absorbed from navigation.md (archived 2026-09-01)

The navigation track doc moved to Documentation/Archive/navigation-history.md
(COMPLETE + review PASS; terrain surfaces feed the bake since 2026-08-31).
Still open, each with its trigger:

- **Editor on-screen verify (user sign-off owed)**: the P4b Bake button + zone
  gizmo + inspector flow, exercised in a real editor session.
- **Player-path nav demo**: a cooked scene with agents loaded by DraconicPlayer
  (demo B) - rides the authored-editor-sample item, not nav work itself.
- **Async bake**: BakeNavigationZone stays SYNCHRONOUS by ruling; go async
  (job system + progress) only when a MEASURED bake stutters (>~100ms on a real
  zone). The call site is one function, so the move is mechanical.
- **Interactive box-extents gizmo handles**: a SHARED problem - decals, probes,
  and nav zones all want the same handles. Build ONE shared helper for all
  three when any of them needs it; never a nav-specific one.
- **NavigationTool (viewport tool + panel)**: the genuine trigger is modal
  authoring - off-mesh LINK placement (jump/ladder) or walkable-AREA painting.
  Build the tool then, on the parked IViewportTool + tool_panel seams.
- **P2 features**: cross-zone stitching/portals, dynamic obstacles
  (DetourTileCache - or possibly just RebakeNavigationZoneRegion triggers,
  which shipped 2026-09-02 with the full partial-rebake chain), NavBlocker
  volumes, MCP `nav_bake` tool (rides the MCP resume list).
  Terrain-as-bake-source is DONE (0c05ad89), the bake is TILED + PARALLEL
  (toggleable) and partial rebake is wired end to end - do not re-plan
  any of those.
- **Lumix nav parity round: ALL FOUR DONE 2026-09-02** - full record in
  week-2026-08-29.md (per-agent speed/stopDistance, agent introspection,
  tiled bake + per-tile regen, bake stage viz). USER VISUAL VERIFY owed
  (agent labels, stage overlay, tiled zone in a real session).
- **Sequencing rule already recorded in terrain-backlog.md**: terrain HOLES
  must subtract their cells from the bake when hole cutting lands.

## Backlog absorbed from docking-v2.md (archived 2026-09-01)

The docking-v2 doc moved to Documentation/Archive/docking-v2-history.md (BUILT
2026-08-24: Linux OS-chromed floats, drop preview + tab chip, close-veto,
stuck-drag watchdog). Still open:

- **BUG - secondary-window viewport render**: a detached viewport only draws
  while the main window is visible. The window loop is ruled out (all windows
  render uniformly - verified); suspects in order: the page's offscreen scene
  render scheduling after a float (host rebind), present/acquire when the
  primary is occluded, UIHost single-window update routing starving the
  float's layout. Needs a live session to bisect.
- **User visual verify on Wayland**: float a panel (OS title bar + close/
  resize/snap working), re-dock via header drag (chip + drop preview, window
  stays put) and via double-click, dirty-page veto on the OS close button.
- **Optional, unprompted-by-need**: dock-zone hints from OS window-move events
  (platforms that report positions mid-move; not Wayland), and a user setting
  over SetOSChromeOverride if anyone wants borderless Linux floats back.

## Backlog absorbed from Core.md (archived 2026-09-01)

The Core implementation plan moved to Documentation/Archive/Core-history.md
(done + load-bearing; the archive stamp corrects its stale §10 - most of that
list shipped). Genuinely still open:

- **Win32 runtime validation depth**: the Win32 System/Threading backends and
  MSVC build are CI-green as COMPILATION; runtime validation on real Windows
  (incl. CaptureStackBackTrace on the assert path) has never had a proper
  pass. Rides any real Windows session.
- **Async IO**: no async file API exists; no consumer has needed one (the job
  system + sync streams cover the cook and streaming paths so far). Build when
  a measured stall names it.
- **DynamicLibrary name/extension resolution**: callers pass full platform
  file names today. Minor; build with the first real plugin consumer (the
  plugin SYSTEM itself is phase-never by MVP ruling - static-link modules).

(Lock-free containers, TLSF/guard allocators, Unicode collation, pak
compression are in the roadmap-absorbed backlog above - not duplicated here.)

## Backlog absorbed from renderer-improvements.md (archived 2026-09-01)

The renderer optimization backlog moved to Documentation/Archive/
renderer-improvements-history.md (the full measured analysis lives there; this
is the work list). Priority order preserved:

1. **Parallel command recording + draw-list sort (1a)** - THE priority. The
   Godot 4.7 A/B (same RTX 2060, shadows on) put Draconic ~40% behind per
   sphere at 144k, and the entire gap is single-threaded CPU: BuildDrawList
   sort 6.47ms, pass record single-threaded while extraction is already
   job-parallel. renderer.md section 7 pre-designed the thread-range seam;
   fill it. Target: CPU below the ~30ms GPU -> GPU-bound -> Godot parity.
2. **Shadow instance reuse across cascades (1b)** - shadow.resolve 8.96ms:
   each of 4 cascades independently re-fills InstanceData. Build caster
   instance data ONCE, per cascade emit only a DataOffsets index list (the
   prepass->forward sharing extended to shadows). NOTE the recorded
   correction: the reverted resolve-SoA attempt RELOCATED the scattered read
   instead of eliminating it - "at the practical CPU floor" was true only of
   that approach, not the shared-buffer fix.
3. **De-dup depth/forward/shadow fills (1c)** - mostly covered once 1a+1b land.
4. **MultiMesh deferrals** - compact per-instance data (144B -> 3x4 48B),
   per-instance LOD selection (pairs with GPU-driven), coarse spatial-split
   culling for huge sets, no-prepass mode for cheap-shading crowds (must stay
   a MODE - the prepass feeds SSAO/motion vectors), static shadow caching for
   static crowds.
5. **GPU-driven / indirect-draw path** - the designed-not-built seam (batches
   produce indirect args, compute cull drops in). Build AFTER 1a shows its
   ceiling; they share the submission-interface assumption.
6. **Spatial acceleration structure (BVH/octree)** - same item as the
   roadmap-absorbed entry above; drops in behind the existing cull seam
   (worldCenter/worldRadius are the ready-made index). Trigger: a profile
   where culling dominates (many objects x many views/lights). A stress-test
   no-op by construction (everything framed).
7. **Feature backlog** - SSR polish (reproject, Hi-Z, stochastic GGX, IBL
   de-double-count, metallic tint), OIT transparency, reflection-probe
   parallax/blend extensions, further post passes - as product needs pull.

Shadow knobs (distance/far-fade/per-cascade culls) SHIPPED and tunable;
remaining shadow perf is item 2 or accepting the far cascade's honest GPU cost.

## Backlog folder absorbed (Documentation/Backlog -> Archive, 2026-09-01)

All 21 Backlog docs moved to Documentation/Archive/*-history.md; still-open
items live HERE, corrected against the tree. The archives keep the full detail
(measurements, per-issue diagnoses, growth paths) - this is the index.

**Subsystem follow-ups (all consumer/demand-gated; details in each archive):**
- *Input*: per-player device pairing (awaits a split-screen consumer),
  action-triggered haptics, per-scene input.
- *Audio*: the grain-bank GROWTH PATH a-d (in-loop-out cues -> parameter
  system -> blend cues -> composite cues; grown, never big-bang ported -
  user ruling 2026-07-19). Plus issue I7: clip-page pause + volume, runtime
  volume API (small).
- *Physics*: convex decomposition (V-HACD vendoring decision), gravity
  volumes, JPH_DEBUG_RENDERER wiring, per-world Jolt job-pool consolidation,
  contact events into per-entity script handlers.
- *Scripting*: live-state-preserving hot reload (blocked on a getter
  convention), script-defined editor tooling hooks, debugger P2 profiler /
  P3 remote transport / P4 extras, luau-analyze CLI (P5b), delegate
  per-signature funcdefs (also a carried weekly item), Entity type into the
  script contract lib, Coroutine namespace completeness. CORRECTION: Wren
  retirement is DONE (backend set = AngelScript + Luau) - the archive's
  "deferred by the user" note is stale.
- *Game UI*: world-tier option-A direct draw (VG-consult-gated), dirty-gated
  world-panel redraws, atlas packing + panel MIP chains, declarative markup
  bindings, theme variants, UIDocumentPage per-line gutter diagnostics,
  two-interactive-scenes pointer routing hardening, the toolkit test tail
  (~212 upstream tests + UISandbox tabs).
- *Networking*: the full refinement list (per-field bitmask delta,
  render-rate interpolation, priority/bandwidth budget, relevancy helpers,
  quantization; congestion/encryption/authority/lockstep/IPv6/web transports/
  NAT+services later) + Win32 socket validation - the one genuinely
  UNVERIFIED surface. Supersedes the shorter roadmap-absorbed networking
  entry above.
- *VG*: overlay-tier MSAA (resolve-attachment plumbing is the real work),
  EvenOdd clip paths, nested clips - all deferred by choice post-#121.
- *Web (task #112)*: (A) WebSocketTransport + native WS accept path BUILT
  2026-09-01 - full record in week-2026-08-29.md. OWED: the user browser
  smoke (native host, the net demo recipe; browser WebScene joins,
  replicated state moves) - node cannot proxy WS so the browser proof is
  manual by design. Then (B) Firefox compat, (C) audio autoplay
  resume-on-gesture + AS browser smoke.

**Editor polish (remaining after P1/P2 shipped):** import-dialogs audit,
theming-gaps audit (hardcoded colors -> palette), authored SVG icons for the
placeholder spots, docking feel (hit zones, drag preview, re-dock hysteresis),
per-page panel-size persistence in editor settings.

**Build times (task #136):** MEASURED (profile in the archive: clean 228s,
ThirdParty ~46%, 3 chokepoint interface edges with 90+ TU fan-out); fixes NOT
started. CORRECTION to the candidate levers: ccache is BANNED with modules
(proven stale-BMI corruption) - that lever is dead; the live ones are
interface hygiene on the fat modules, module splits, mold/split-dwarf, and
ThirdParty rebuild confirmation.

**UI core (audit follow-through):** P0-P2 + the damage gate SHIPPED; remaining
= P3 styling dogfood (themes to .sss + UA default sheet + state ladder -
scoping investigated, in the archive) and any P4 leftovers noted there.

**Issues triage (user-filed, 2026-08-08) - still open:** I2 mouse-capture
never released (WATCH, hard repro), I4 memory ballooning (one half fixed;
re-verify the rest), I5 allocator plumbing DONE (TRACK
COMPLETE 2026-09-02, P0-P7 incl. the tripwire lockdown - full record in
week-2026-08-29.md),
I7 audio clip page small items, I8 Linux exported-player colors (root cause
unconfirmed - harden the swapchain fallback regardless), I10 Windows
path-separator normalization (trivial, exact fix known), I11 scene-pass MSAA
(same item as the renderer backlog). I1 collider gizmos + I6 compression are
FIXED/SHIPPED.

**Deferred-by-design (do NOT build without a user go-ahead):** GPU particle
sim, post 4/5 (1-3 VERIFIED 2026-09-03 - unblocked, still needs the
go-ahead), AS debugger P2/P3, draconic.gui
(parked entirely - the keyframes fix shipped), SIMD layout migration, web
pthreads, the WebGPU map-flush MemCompare. (Skinned crowds GRADUATED -
shipped, no longer on this list.)

**Superseded records now archive-only:** parity-2026-08 (replaced by the
Parity/ folder surveys; its MISSING list is stale - navigation, terrain,
property animation all shipped since), editor-pages-gap (the coverage gap
CLOSED - every asset type opens), gui-gaps (moot while draconic.gui is
parked), sedulous-backport (raw commit lists for the OTHER repo's back-port -
consult when that work resumes).

## Backlog absorbed from asset-thumbnails.md (archived 2026-09-01)

The thumbnail spec moved to Documentation/Archive/asset-thumbnails-history.md -
generation is DONE (13 asset types across the CPU lane + the GPU stage; CPU
tripwire 5, scene tripwire 8). Still open:

- **P4 tail**: picker-DIALOG thumbnails DONE 2026-09-01 (record in
  week-2026-08-29.md; live row refresh on OnThumbnailReady deferred until
  Function grows multicast/tokens, the UI-core P4 item). Remaining: a
  settings knob for thumbnail size, and a cache-maintenance action
  (clear/regenerate all; the cache dir is <project>/.cache/thumbs, safe to
  delete).
- **On-device pixel probe for the GPU stage**: needs an editor-host harness
  (RenderSubsystem + SceneSubsystem + resources + a cooked product) no test
  target stands up today - the one honest test gap in the track.
- **User visual verify PASSED 2026-09-03**: thumbnails confirmed working
  in-editor (record in week-2026-08-29.md).
- **Terrain thumbnail** stays icon-by-ruling (recorded in terrain-backlog.md).

## DONE: W1 ruling - TypeOf<T>() slot made process-single (2026-09-05)

The Windows agent's W1 prototype (5d1af532: generated `.def` for functions +
`ENGINE_EXPORT_DATA` for static data, no import macros; shared Core links
clean, 266/284) left one ruling: `TypeOf<T>()`'s function-local `TypeInfo`
is a template static = one copy per image on PE, so `REFLECT_*` patches made
inside Core.dll were invisible to consumers (18 core-reflection cases).

Ruled (b) over (a): the `TypeInfo` now lives in a process-single slot table
(`detail::TypeInfoSlot`, Core/RTTI/TypeInfoImpl.cpp, keyed by signature id,
STATIC storage - .bss index + pool, heap only past 4096 types; the per-image
static is a REFERENCE). A first heap-backed cut leaked 472 bytes under ASAN
when the static-lane test plugin (own embedded Core) was dlclosed; static
storage has nothing to orphan. Facts that decided
it: 170 raw `TypeInfo*` compares outside Reflection.cppm, and REFLECT_VALUE
rewrites the id so an id-keyed Canonical() would not even find the
consumer's copy. Hot reload falls out: a rebuilt module re-patches its
existing slots; only size/align refresh from the prototype.

Linux now models PE for this symbol: `TypeOf` is `COMPILER_ATTR_HIDDEN` on
GCC/clang. With the attribute alone the shared lane reproduced the Windows
failure exactly (same test, same null property, SIGSEGV); with the slot,
Core.Tests 287/287 and the cross-boundary plugin case proves the plugin's
`&TypeOf<Float3>()` is the host's with Core's 3 patched properties. Unused
CORE_API placeholders deleted. Windows prompt updated: W2 unblocked.

## DONE: Windows lane landed (W2/W3 + clang-on-Windows) - Linux regression pass (2026-09-05)

The Windows session finished W2 (msvc-shared preset + CI, 182 DLLs, 148/148),
W3 (dev loop + hot reload on Windows, staged-copy load for the file-lock
wrinkle, platform-shaped manifest with cross-platform fallback, vcvars
ruling) and a clang-on-Windows shared build (WIN32-gated .def generation,
LINKER:/DEF:, ENGINE_EXPORT_DATA back to COMPILER_MSVC - cl and clang need
opposite things). SDL3 now follows the engine library type.

Linux re-verification found ONE regression: `ShipCompilerIsMsvcCl` (exporter,
anonymous namespace) is only called under PLATFORM_WINDOWS, so clang/gcc
`-Werror` failed on `-Wunused-function`. Guarded the helper the same way.
With that: clang, gcc, clang-shared 151/151; ASAN 151/151. The shared lane
confirms libSDL3.so is staged beside the executables and resolved via rpath.
Windows prompt gained the rule.

## DONE: fonts library family renamed to the greenfield shape (2026-09-05)

User question: is Fonts.Importer a pipeline thing? Ruling: right layer, wrong
name - it is a pure TTF -> coverage-atlas baker with no Asset/cook/content-DB
concept, used by pipeline-free consumers (font page live preview, thumbnails,
VG pixel probes), so Foundation is correct. The name collided with the
pipeline's IFileImporter meaning. User then asked for the greenfield family
and chose to apply it. Named on two axes - atlas PIXEL FORMAT + ROLE:

- Fonts.TTF -> Fonts.TrueType (`foundation.fonts.truetype`; no abbreviations)
- Fonts.Baked -> Fonts.Coverage (`foundation.fonts.coverage`; runtime product)
- Fonts.Importer -> Fonts.Coverage.Baker (`foundation.fonts.coverage.baker`;
  class FontBaker) - exactly parallel to Fonts.DistanceField.Baker
- Fonts.IO FOLDED INTO Fonts as partitions :backend_interfaces / :factories /
  :font_manager ("IO" was the backend extension seam, not I/O; a separate
  library only because Sedulous had one); Fonts.IO.Tests merged into
  Fonts.Tests (13 cases / 155 asserts). Battery is 150 suites now.
- FontResourcePixels::Alpha8 -> ::Coverage (serialized by ordinal: wire-safe).
  FontBakeMode::RasterRamp left alone (a bake MODE, reflected by name).

Type names unchanged (BakedFont/BakedFontAtlas, DFFontAtlas...). Follow-on if
wanted: DF* type names are abbreviations under the naming rule.

## DONE: fonts - abbreviated identifiers expanded (2026-09-06)

Follow-on to the family rename (user: "fix the abbreviated names"). The
distance-field classes and everything named after them now carry full
names: DFFontAtlas -> DistanceFieldFontAtlas, DFFontAtlasBaker ->
DistanceFieldFontAtlasBaker, DFFonts -> DistanceFieldFonts; files, module
primary units, file sets and test sources follow. Members/fields too:
dfSize -> distanceFieldSize, dfPixelRange -> distanceFieldPixelRange,
m_dfFs -> m_distanceFieldFragmentShader, m_dfPipeline/m_dfModule/
m_dfSizeRow, dfBaker, dfOpts, dfShader. WIRE NAMES KEPT: the serialized
keys and reflected property name stay "dfSize" / "dfPixelRange" (data
format, not identifiers; changing them = source-asset migration).
Deliberately untouched: HLSL cbuffer fields (DFPxRange/DFAtlasW/DFAtlasH)
and their C++ mirrors (shader source is its own track), the 'DFAC'/'TTFF'
magic tags, terse channel locals (dfR/dfG/dfF), and MSDF (an acronym, the
msdfgen term). Gate: clang, gcc, clang-shared 150/150; ASAN 150/150.

## DONE: fonts - wire keys + shader fields follow the full names (2026-09-06)

User ruling on the previous entry's carve-outs: consistency wins; migration
and shader recompilation are fine; terse channel locals stay.
- FontAsset: DataVersion 1; key "dfSize" -> "distanceFieldSize". Serialize
  branches on ar.Version(): v0 payloads (every pre-2026-09-06 project) read
  the old key and upgrade on next save. Reflected property name follows.
  NEW TEST: writes a v1 XML asset, rewrites it into the v0 form (chain version
  0 + old key), reads it back (Fonts.Pipeline.Tests, 11 asserts).
- FontResourceEntry: "dfPixelRange" -> "distanceFieldPixelRange";
  FontAssetBuilder Version 2 -> 3 so cooked fonts RE-COOK once.
- VG shaders (Data/Shaders vg.vs/vg_df.ps + the unreferenced copies under
  VG.Renderer/shaders): DFPxRange/DFAtlasW/DFAtlasH ->
  DistanceFieldPixelRange/AtlasWidth/AtlasHeight; C++ mirrors in VGUniforms,
  VGBatch, VGContext follow. Layout unchanged. Desktop compiles HLSL at
  startup; the web WGSL is produced at cook time - nothing cooked is checked
  in, so the rename takes effect on the next run / next web cook.
Still untouched: 'DFAC'/'TTFF' four-char magic tags, MSDF (acronym), dfR/dfG/dfF.
Gate: clang, gcc, clang-shared 150/150; ASAN 150/150.

## DONE: ModelBone local matrix convention + glTF matrix-only nodes (2026-09-06)

An external agent's report (verified): ModelBone::updateLocalTransform built
its matrix in the COLUMN-vector convention (translation in the last column,
T * (R * S)) while the engine is row-vector everywhere (Translation writes
the last row, TransformPoint reads it, Transform::ToMatrix = Scale *
Rotation + last-row position). Never showed because NOTHING reads the bone
matrix: the cook and ModelResource carry the TRS fields. What the report
missed: glTF nodes authored with a `matrix` (not TRS) memcpy'd it into that
dead field and left the fields at identity -> those nodes COOKED AT THE
ORIGIN (real data loss).

Fix: the builder is `Transform{t, r, s}.ToMatrix()`; the glTF matrix path
decomposes with Transform::FromMatrix into the fields, then derives the
matrix. FBX audited: its convertMatrix transposes ufbx columns into rows
(correct) and inverse bind matrices go through it; TRS was already
populated there. Tests: Model.Tests pins (1,0,0)->(12,0,0) (old builder:
(2,0,0), proven by running the test against the stashed code) plus an
independent S*R-vs-R*S check via RotateVector (reviewer feedback: the first
cut compared the builder to its own implementation); NEW Model.GLTF.Tests
(matrix-only node lands in the TRS fields and matches the TRS node).
Battery is 151 suites again. Gate: clang, gcc, clang-shared, ASAN.

## DONE: XmlDocument reports the real parse error position (2026-09-07)

Found by the Beef-port agent while closing the audit's gap table:
ErrorLine/ErrorColumn were set to 1 at the top of Parse and never written
again, so the editor's UI document/theme pages (which subtract 1 for buffer
lines) marked every XML diagnostic at line 0. Fix: the parsers thread ONE
`remaining` view by reference, so on a non-Ok result Parse derives line and
column from where that view stands (1-based; columns count code points, UTF-8
continuation bytes do not advance). Test pins line 3 for a mismatched close
tag on the third line, code-point columns, end-of-text for truncated input,
and 1/1 after success. Gate: clang, gcc, clang-shared 151/151; ASAN.

## DONE: FbxLoader - content-compared vertex welding + OBJ .mtl sidecars (2026-09-07)

Two more findings from the Beef port (found by porting, not reading):
- The welder keyed HashMap<size_t,i32> on the vertex HASH alone and returned
  early on a hit without comparing anything, so two different vertices that
  collided became one (a pulled seam nothing in the source explains). Now
  `VertexWelder`: the hash picks a bucket (intrusive index chain, no per-
  vertex allocation), the BYTES decide; buildVertex assembles into a stack
  scratch and returns the welded index.
- OBJ files never had their .mtl read: ufbx only loads sidecars with
  load_external_files, which m_loadOpts never set - every OBJ material
  arrived NAMED with default colours and looked fine. Now load_external_files
  + ignore_missing_external_files (a missing sidecar is not an error) +
  obj_search_mtl_by_filename.
NEW Model.FBX.Tests (OBJ documents written to scratch, no binary fixtures):
welder keeps colliding-but-different vertices apart and welds identical ones;
a shared-edge quad welds to 4 vertices / 6 indices end to end; `usemtl red`
carries Kd (1,0,0); a missing .mtl still loads. Battery = 152 suites.
Gate: clang, gcc, clang-shared 152/152; ASAN.

## DONE: BC6H for HDR textures (texture-compression-hdr-and-normals P1-P3, 2026-09-07)

The plan's encoder premise was false: bc7enc_rdo has no BC6H encoder (verified
upstream), Basis only inside its transcoder, DirectXTex DirectXMath-bound. Shipped
an IN-HOUSE encoder (Pipeline/Texture.Compression/Bc6hEncoderImpl.cpp): unsigned
mode 11, bounding-box endpoints refined by least squares, error in the decoder's
near-log internal domain, and a flat-block candidate (fixed first-step weight +
per-channel second endpoint) that recovers a 4-unit grid for the flat blocks
skies are made of. Measured: flat block 0.08% relative error, sky-like gradient
2%. Reference decoding in tests via vendored ThirdParty/bcdec (MIT/Unlicense,
single header, test-only). Policy HDR row -> BC6HRGBUfloat on BC targets; cook
RGBA32F branch (MaybeCompressHdr) + TextureAssetBuilder Version 5 (HDR assets
re-cook: BlueSky's 134 MB RGBA32F sidecar becomes ~9-11 MB); cook test with a
hand-written flat Radiance .hdr; the GPU probe gained a BC6H leg that samples
(217,57,57) on Vulkan AND WebGPU, identical to BC1 of the same colour.
Battery = 152 suites. Gate: clang, gcc, clang-shared, ASAN.

## Carried from week-2026-08-29 (incomplete; moved 2026-09-07)

Still-open items from last week that had no entry here. Completed items stay in
week-2026-08-29.md as that week's record; the full write-ups are there too -
this is the list, with the section to read for each.

### Work items

1. **Editor extensibility seams** (carried since week-2026-08-22; user ruling
   2026-08-18 "ultimately must fix"): per-component inspector action-row
   registry, external gizmo-renderer registration (a provider list the
   ScenePage consults, not a global mutable registry), migrate
   Editor.Navigation onto both so Editor.Scene drops its nav imports; audit
   of the central InspectorView entries as a stretch. Read: "Editor
   extensibility seams" under "Carried from week 2026-08-22".
2. **Script backend as a PROJECT setting** (punted 2026-08-18): chosen at
   project creation, stored in project settings, gates the editor's script
   surface and which VM runs; only among COMPILED-IN backends; decide
   reload-vs-live. Read: "Script backend as a PROJECT setting".
3. **AngelScript delegate funcdef surface** (Fable, not urgent): only `Action`
   (void()) and the misnamed `ScriptDelegate` (double(double)) funcdefs
   exist; a blessed set or auto-registration per reflected delegate-param
   signature. Read: "AngelScript delegate funcdef surface is ad-hoc".
4. **Spline P2 remainder**: scatter-along-spline, particle spline opcode, road
   extrusion (P0/P1/path-follow DONE 2026-08-31). Read: "spline + spline editor".
5. **Renderer debug views remainder**: pixel inspector, wipe/compare, semantic
   coverage for terrain/sprite/unlit shaders when needed (layers 1 + 2 LANDED
   2026-09-03). Read: "renderer debug views in the editor viewport".
6. **Bone attachments** (design, triggered by the first game need):
   BoneAttachmentComponent (copy one joint's model-space pose onto an entity
   after the animation tick) + the drop-pure-joint-nodes import option, which
   RIDES the import-UX planning pass (merge-meshes). Read: "bone attachments
   + joint-node import noise".
7. **PaperKid editor-use feedback** (still open): organize the new-component
   menu; drag an instance into a group; seed input map / bus layout / UI theme
   for new projects (font, sky, primitives already seeded); Shift+up/down
   multi-select bug; WASD default input map; detached window does not render
   its viewport unless the main window is visible.
8. **Asset-variants sign-off (a)**: the web pak selection is MANIFEST-FREE
   (pak-name convention) - needs the user ruling, or a manifest gets built.
   (b) browser/mobile functional check of the variant paks = user UAT.

### Verifications owed (user visual)

- Texture page UX pass: profiles row on top, Content then Sampling, the lint
  on a Normal + sRGB combination, Cooks-to updating with usage/compression.
  Cooks-to now reads "BC6H" for HDR assets (2026-09-07).
- Screen-Space GI on a lit scene (coloured wall near a floor bleeds; stable
  under camera motion) + the grain retest, still and moving.
- The remaining smoke-checklist property-animation items not covered by the
  live UAT rounds.

### Small follow-up

- ApplyProjectUiDefaults recreates the UI ResourceFontService on every
  cook-finished callback ("default font bound" log churn) despite documenting
  itself idempotent: make SetDefaultFont early-out on the same Font product.

## DONE: rhi::BytesPerPixel covers every uncompressed format; one table (2026-09-07)

Beef-port finding (ported as-is there and pinned): the RHI's BytesPerPixel table
omitted sixteen ordinary uncompressed formats (R8/RG8/RGBA8 Snorm/Uint/Sint,
RG16 integer, RGB10A2Uint, RGB9E5Float, RG32Sint, RGBA16Unorm/Snorm), all
falling through to the compressed default of 0 - anything sizing an upload
from one got zero bytes. TextureData carried its OWN copy of the table, wrong
differently (unknown formats defaulted to 4: wrong for RGBA16Unorm=8 and
Stencil8=1). Fix: one complete table in rhi::BytesPerPixel; TextureData
delegates; TextureData::CalculateMipSize sizes block-compressed levels via
CompressedLevelBytes instead of a per-texel figure. Tests: RHI.Null.Tests walks
the WHOLE TextureFormat enum (every format is per-texel or block sized, never
neither - a new enumerator missing from the table fails) + spot checks of the
formerly-missing ones; Texture.Tests pins RGBA16Unorm=8, Stencil8=1, BC7=0.
Gate: clang, gcc, clang-shared 152/152; ASAN.

## Audit: Beef RHI.Null + RHI.Validation ports (2026-09-07)

Null: clean - all 30 types, factory -> constructor, As*Ext -> casts; 19 Beef
tests vs Raptor's 4. Validation: structurally complete (13 wrappers; per-class
Log* helpers -> central ValidationLog + callback + severity; the finished
bundle is returned unwrapped instead of via ValidatedRenderBundle; encoder
state = two open-pass booleans instead of the 4-state enum). ONE BEHAVIOURAL
GAP sent back: Raptor gates twenty encoder operations (Begin*Pass,
CreateRenderBundleEncoder, Barrier, the four copies, Blit, GenerateMipmaps,
resolves, query ops, the ray-tracing calls) on the encoder being in plain
Recording state via checkState(); Beef checks only "not finished", so a nested
BeginRenderPass or a copy/barrier issued while a pass is open goes
undetected (Beef only catches Finish with a pass open, and its 33 tests have
no nested-pass case). Fix = the same check on each gated op + a test.

## Audit: Beef RHI surface port confirmed faithful (2026-09-07)

Same cross-check as the Foundation audit, scoped to Sedulous.RHI: all 84
classes/structs present (I-prefixed interfaces), 42/43 enums identical (the
43rd is the nested RayTracingShaderGroup::Type hoisted to
RayTracingShaderGroupType, same values), every interface method present
(As*Ext accessors -> casts, GetSize -> Size property), descriptor fields
identical incl. the nullable ones. One restructure, not a gap: AdapterInfo's
~30 capability/limit fields moved into DeviceFeatures via
AdapterInfo.SupportedFeatures. Beef has 31 RHI tests vs Raptor's 0 on the
core surface. Null/Validation backends in progress on their side - rerun the
audit + test-name cross-check when they land.

## DONE: Vulkan transfer batch honours TextureDataLayout (2026-09-07)

Beef-port finding, verified: VkTransferBatch stored the caller's layout in its
copy record and built VkBufferImageCopy with bufferRowLength/bufferImageHeight
left at 0, so a PADDED upload copied its padding as texels (DX12 + WebGPU
honoured the stride; the Vulkan command encoder's buffer->texture copy did
too - only the batch forgot). Fix mirrors the encoder: texel stride =
bytesPerRow / BytesPerPixel, tight (0/0) for block-compressed data, and
bufferOffset += layout.offset. Probe: Integration.TextureCompression gained
"a padded row pitch uploads without its padding becoming pixels" - an 8x8
RGBA8 texture with 32 padding bytes (blue) per row rendered on the cube and
counted: OLD batch = 4593 blue-dominant pixels on Vulkan (WebGPU 0), fixed =
0 on both. Gate: clang, gcc, clang-shared 152/152; ASAN slices.

## APPROVED, QUEUED: acceleration-structure sizing (user 2026-09-07 - "keep it in the weekly, don't do it yet")

Raptor's CreateAccelStruct allocates a FIXED 256 KB buffer (VkDevice.cppm
init(..., 256 * 1024)) and nothing anywhere calls
vkGetAccelerationStructureBuildSizesKHR: AccelStructDesc carries no geometry,
so creation cannot size, and BuildBottom/TopLevelAccelStruct never validates
the AS or scratch capacity. Small builds fit the floor and work (the Beef
port floored at 1 KB, which is why ITS builds were rejected); anything past
256 KB fails silently at build. Ray tracing has NO Raptor test - the recording
-only test the agent cites is the Beef one. PROPOSED (needs the user's call,
it changes the shared RHI surface and touches DX12 which cannot be built
here). Design APPROVED by the user 2026-09-07; scheduling deferred - do not start
until asked. (1) Device::QueryAccelStructSizes(const AccelStructBuildDesc&,
AccelStructSizes& out) returning {structureBytes, scratchBytes,
updateScratchBytes} - Vulkan via vkGetAccelerationStructureBuildSizesKHR,
DX12 via GetRaytracingAccelerationStructurePrebuildInfo, Null returns a
deterministic estimate, Validation forwards; (2) AccelStructDesc gains
`u64 sizeBytes` (0 = the backend's floor, kept for the existing callers);
(3) Build* validates dst capacity + scratch size against the query and
reports (Validation layer error), instead of letting the driver reject.
Then a Vulkan probe: one triangle BLAS + one-instance TLAS build + trace.

## Audit: Beef RHI.Vulkan port (2026-09-07)

Method-level parity plus a stronger check: the SET of Vulkan API calls and
the set of extension names each backend uses are identical (Raptor loads the
KHR/EXT entry points through vkGetDeviceProcAddr, Beef calls them by name).
All 36 classes present (Vulkan* naming); logging -> Core logger; adapter
sort -> AdapterSelection; the stride conversion is correct incl. the
block-compressed case. ONE GAP: VulkanDevice does not override
MaxColorDepthSampleCount / SupportsSampleCount, so it inherits IDevice's
defaults (1 / count <= 1) and MSAA is silently unavailable on Beef Vulkan;
Raptor derives both from the physical-device limits (VkDevice.cppm:367/382).
41 Beef Vulkan tests vs Raptor's 0.

## DONE: Vulkan queue-family rules - three Beef-port findings (2026-09-07)

From the Beef port's MultiQueue run, all verified in Raptor:
1. SampleApp never requested a compute queue (DeviceDesc defaults
   computeQueueCount = 0), so Sample017 always took the single-queue fallback.
   Now asks for one; the device clamps to what the adapter has.
2. The barrier helper mapped ShaderRead/ShaderWrite to ALL_GRAPHICS |
   COMPUTE_SHADER regardless of queue - invalid on a compute-only (or
   transfer-only) family. New maskStagesForQueue(stages, QueueType) cuts a
   stage mask to what the recording pool's family can execute (an emptied
   mask becomes ALL_COMMANDS, never zero); the command pool now keeps its
   QueueType and the encoder applies the cut at all six barrier sites.
3. The device-latched swap-chain acquire/present semaphores were consumed by
   WHICHEVER queue submitted first (async compute in Sample017), so the
   compute submit waited on acquire for nothing and signalled present before
   the frame was drawn. Only the GRAPHICS queue's fenced Submit consumes them
   now.
NEW RHI.Vulkan.Tests (Vulkan-gated; device cases skip without an adapter):
the stage cut is pinned as a pure function; a device created with
computeQueueCount = 1 grants a compute queue exactly when the adapter has a
compute-only family (1 on this GPU) and records a ShaderWrite -> ShaderRead
barrier on a compute pool. GOTCHA: the test must link Vulkan::Vulkan so GCC
sees the SDK's vulkan.h, not /usr/include's - two different headers trip the
module ODR check. Battery = 153 suites. Gate: clang, gcc, clang-shared,
ASAN slices.

## Audit: Beef Samples.RHI port (2026-09-07)

All 30 samples + the Smoketest present. Per-sample comparison of the RHI
calls each makes (Create*/Draw*/Dispatch*/Set*/Copy*/Barrier/...): identical
except three naming-only differences (004/017 barriers, 018's texture view
in a loop). The two documented divergences are sound: the Beef null backend
owns its adapter/devices/surfaces so per-object DestroySurface would double
free; ShaderGroupHandleSize = 0 alongside "ray tracing: supported" matches
Raptor (device.Features = adapter capabilities; the handle size is filled
only when RT was actually enabled). DX12/WebGPU sections dropped (no Beef
backend yet). Their Sample017 docstring already states the compute-family
stage rule.

## DONE: the two gaps the Beef port found in the queue-family fix (2026-09-07)

Their Sample017 on a REAL dedicated compute queue found both, each with the
error count as perturbation evidence:
1. The swap-chain gate alone broke presentation: Raptor's wait-fence Submit
   overload deliberately did not consume the swap-chain sync ("the 2-arg
   submit handles it"), and Sample017's graphics submit IS that overload
   (waits on the compute fence). Once the compute submit stopped taking the
   semaphores, nothing did: "pWaitSemaphores[0] has no way to be signaled",
   3 errors a frame. Now BOTH fence-signalling overloads consume the sync
   when the queue is Graphics (acquire wait at COLOR_ATTACHMENT_OUTPUT,
   present signal appended).
2. Cutting stages without cutting accesses is still invalid: VertexBuffer ->
   ShaderWrite on compute kept VERTEX_ATTRIBUTE_READ under ALL_COMMANDS,
   which expands per family and the compute expansion has no vertex input
   (VUID-VkBufferMemoryBarrier2-srcAccessMask-03902), 20 errors a frame.
   Added maskAccessForQueue and maskForQueue(StageAccess) so stage and
   access are cut as ONE pair; the encoder applies the pair at all six sites.
Tests: the paired cut is pinned (VertexBuffer on compute -> ALL_COMMANDS with
an EMPTY access mask; shader access survives on compute, not on transfer;
graphics untouched) and the device case now records that exact vertex-
buffer -> storage barrier on the compute pool. Battery 153; gate: clang,
gcc, clang-shared 153/153; ASAN slices. Beef adopted all of it (3914e4f,
552a010); their integration test mirrors Sample017's submit shape, which
we cannot run headless here - the sample is the Raptor-side proof.

## DONE: fenced Submit contract made consistent + an RHI log sink (2026-09-07)

Beef-port divergence, and they were on the right side: Raptor's wait-fence
Submit tolerated a null signalFence (submitted unsignalled - and, on the
graphics queue, consumed and signalled the swap-chain present semaphore doing
so) while the 3-arg overload returned early, and the validation layer reported
the null only on the 3-arg overload. Contract now stated in Queue.cppm and
enforced everywhere: the fenced overloads REQUIRE a fence (the plain overload
is the unsignalled path); Vulkan's wait-fence overload rejects a null fence
BEFORE consuming the swap-chain sync; the validation layer reports it on both.
To test a void-returning validation report the RHI log gained a SINK:
rhi::SetLogSink(fn, ctx) / DispatchToLogSink, the slot in a new RHI impl unit
(LogImpl.cpp - process-wide state per the shared-libraries rule). Test:
"a fenced submit without a fence is reported on BOTH fenced overloads" (and
not on the plain one). Gate: clang, gcc, clang-shared 153/153; ASAN slices.

## DONE: one supported layout per type - every legacy-format and old-RTTI compat path removed (2026-09-08)

User ruling (2026-09-07, mirrored on the Beef side): "drop all legacy format
compat support here too, and drop the old rtti compat too. only support clean
current version everywhere." Now the rule in Process/CONVENTIONS.md.
- Core: BeginVersionedPayload compares the STORED data-version chain with the
  type's current one (count, type ids, versions) and fails the payload
  (NotSupported) on any difference - no migration, ever. ar.Version() inside a
  payload therefore always reports the current version; every `ar.Version()
  >= N` / `< N` gate (26 files, ~60 sites) is unwrapped into unconditional
  reads and every else-branch migration (audio sourceType inference, the
  singular-material fold, the 48-byte v1 vertex expansion, the terrain
  fixed-4-layer mapping, the fonts dfSize key) deleted. TypeRegistry::
  FindByName is an exact lookup: the draconic:: / rtti::editor:: remap and its
  four tests are gone (one negative test replaces them).
- Scene stream: readers REQUIRE magic + version 3 (the v1 name-sniff and the
  v2 gates are gone, SerializeGuid/SerializeTransform lost their version
  parameter, the legacyProbe parameter left SerializeScene); the prefab
  section accepts only Referenced3 / Expanded2 and FAILS the read otherwise;
  a prefab payload with more than one root is refused (spawned entities torn
  down); nested-record tails that are not the current shape skip LOUDLY;
  apply-to-prefab falls back to the baseline diff for a non-current template.
- Formats retired outright: mesh geometry inline in the envelope (always the
  sidecar now; the flag left the wire; sidecars are stamped by the SOURCE
  type - the asset-type stamp was a latent mismatch the strict reader
  exposed; StaticMeshAsset/StaticMeshSource 5), UI document/theme inline text
  (the linked Sources/ file is the text; UIDocumentAsset 2, UIThemeAsset 3;
  editor pages refuse to save an unlinked asset), the navigation frame stamp
  (every bake is rigid-frame; bakedFrame left source/asset/product; source 2,
  asset 2, builder 3) and the single-tile navmesh blob (Build = BuildTiled;
  Load refuses v1), the splatmap pixels-only sidecar migration (the cook and
  the factory refuse a lone raster; the PNG import keeps its fixed-layer
  conversion under the honest name SplatWeightsFromFixedLayerRaster), the
  in-memory forward-material upgrade (ForwardMaterialSourceIsComplete: the
  factory refuses a stale source, the page logs it).
- Content sidecars: ReadData still resolves a stream under either suffix -
  that is by design (readers do not know the encoding; a stream never exists
  under both), so only the wording changed.
- Tests: every "still loads / migrates" case became "is REFUSED" (versioned
  payloads, project manifest, export template + presets, audio bus layout,
  scene short/stale streams, v2 text scene, multi-root prefab, retired prefab
  section, navmesh v1 header, UI unlinked assets, splat lone raster); the
  Core negative cases pin the stale-version, extra-entry and wrong-type chains.
  Battery 153; gate: clang, gcc, clang-shared 153/153; ASAN 153/153 (slices).
- Consequence for existing projects: anything saved under an older data
  version no longer opens - re-import / re-save / re-cook with the build that
  wrote it first. The editor logs the stored type + version on refusal.

## Audit: Beef Scene + Scene.Resource + Input ports (2026-09-08)

Scene/Scene.Resource: faithful (every entry point maps to a Beef static
class; magic/version/prefab tags identical; strict header + strict versioned
payload; retired tags, multi-root payloads and duplicate guids handled as
here). Feedback (untested storage entry points, refusal cases, absent-plugin
override, a stale "Raptor still reads v1/v2" comment) all addressed by them:
SceneRefusalTests + a Scene.Resource integration suite over a real content DB.
Input + Input.Resource: faithful (68 tests over our 20; wire keys identical;
enums identical; API renames are Beef idiom). ONE Raptor finding, fixed here:
InputMap's inline "version" field was written but never CHECKED on read, and
SerializeBinding still gated the touch/region fields on a version parameter
the sweep missed (it keyed off a local, not ar.Version()). Reader now refuses
any version but kInputMapVersion (the Beef port already did); gate removed;
test "a map stamped with another wire version is REFUSED". Gate: Input tests
clang/gcc/clang-shared; lanes rebuilt clean. Not yet ported: Input.Pipeline
(the cook layer, like the other pipelines).

## Audit: Beef Texture port + the R16 Convert finding (2026-09-08)

Sedulous.Texture (5fcb1fc): faithful - the five types, three enums and every
Raptor case matched (18 Beef tests over our 5); block-compressed formats are
sized through the RHI helpers only (no encoder/decoder there, same as here).
Idiom: Pixels as one Span (size derived), a FromImage overload that takes the
image's own colour space (their Image carries one, ours does not; the explicit
form stays primary). Skipped by design: the external-RTTI enum registration
(Beef reflection is opt-in; no consumer yet). ONE Raptor finding, fixed here:
TextureFormatUtils::Convert had no case for PixelFormat::R16, so it fell to
the catch-all default = RGBA8Unorm (wrong channel count and depth). Harmless
today (R16 only comes from heightmap loads, which upload through
HeightTexture as R16Uint and never reach Convert), but a guess is a guess:
R16 now maps to the same R16Uint integer container, and the switch is
exhaustive (no default) so a new PixelFormat is a compile error, not RGBA8.
Test pins R16 -> R16Uint in both colour spaces. Gate: Texture tests on
clang, gcc, clang-shared.

## Audit: Beef Texture.Resource port + two Raptor findings fixed (2026-09-08)

Sedulous.Texture.Resource (ccf124d): faithful - the four types and every
Raptor case matched (29 Beef tests over our 4, incl. the device-backed path on
a null device: shared binding, cube view, empty payload, wrong stored type,
missing instance, async equivalence, 12 concurrent loads). They split the
sampler description and the upload plan out of the factory so both are pure;
that split found a Raptor defect, FIXED here:
- Compressed cubemaps uploaded NOTHING: the cube branch sized a face as
  width * GetBytesPerPixel * height, and the per-texel size of a block-
  compressed format is 0 - so faceBytes was 0, the bounds check always passed
  and six zero-byte writes went out. Now the upload plan is a pure function,
  EnumerateTextureWrites(record, payloadBytes, out), sized by block footprint
  for compressed formats (faces AND mip levels through one helper); the
  factory consumes the plan. Tests pin a BC7 8x8 cube (six 64-byte faces,
  block-row layout), the 1x1 whole-block case, a truncated payload, an
  uncompressed cube and both chain kinds.
- Also from the Input port's tests (missed last time): a Key binding whose
  modifier mask names a GROUP (KeyModifiers::Shift = both shift bits) was
  UNREACHABLE - the check required every bit of the mask, so a Shift+W
  binding needed both shifts held. Now per group (shift/ctrl/alt/gui) either
  held side satisfies a group mask and a specific side stays specific; the
  lock keys stay exact. Test pins Shift via left, via right, and LeftShift
  refusing right.
Gate: clang lane rebuilt; Texture.Resource + Input tests on clang, gcc,
clang-shared, ASAN.

## Audit: Beef Materials + Materials.Resource ports; MaterialSource keys follow the full-name rule (2026-09-08)

Sedulous.Materials (4a614c2 + 1d919fc): faithful - 15 types / 5 enums / 4
cases matched, 45 tests; the builder's uniform methods went back to Float/
Float2/3/4 per the user (Beef follows Raptor names even where a C++ shadowing
workaround motivated them). Watch item recorded on THEIR MaterialInstance:
it BORROWS its material (no refcount), and Raptor's MeshRenderer relies on
the strong ref twice (stale prune = RefCount() <= 1; the old material stays
alive until the prune detaches the bind group + uniform buffer) - their
renderer owes a uid-vs-handle stale signal and deferred product retirement.
Sedulous.Materials.Resource (5a9b65e, 16e8208): faithful - both types and all
5 cases matched, 18 tests; the forward-shader contract names the missing
property; PBR packing offsets identical (0/16/20/32/48/52/56, 64 total).
Wire-key finding, fixed HERE: their [Serializable] records emit the field
name as the key. For TextureResource, SceneDocument, PrefabDocument and
InputMapResource the lower-cased field name IS our key; MaterialSource was
the one mismatch, and only because Raptor abbreviated: propNames/propTypes/
propBindings/propOffsets/propSizes broke the full-names rule. Renamed to
propertyNames/propertyTypes/propertyBindings/propertyOffsets/propertySizes
(members, wire keys, reflection properties); MaterialSource DataVersion 2->3
(cooked materials re-cook). The factory's and the material page's refusal
now name the missing forward property (ForwardMaterialSourceIsComplete gained
an out parameter), matching their contract. Ask on the Beef side: the
attribute should lower-case the first letter of the generated key so text
envelopes match Raptor's exactly. Gate: clang/gcc/clang-shared lanes rebuilt;
Material, EditContext and Editor.Scene tests green on all three; ASAN on
Materials.Resource.

## Audit: Beef Materials.PipelineCache port; our PSO cache tests no longer need DXC (2026-09-08)

Sedulous.Materials.PipelineCache (feb95f1): faithful - types, functions and
both Raptor cases matched, 15 tests. Materials is COMPLETE on their side (78
tests over our 11 cases). Their BlendState.Multiply spells the alpha factor
DstAlpha where ours says Dst - the same factor on hardware, no change.
Adopted their test shape HERE: our two PSO-cache cases opened with "DXC
unavailable; skipping", so on any machine without DXC the cache's own
behaviour was never exercised. Nothing in the cache compiles anything - it
needs a shader module to point at and a version to poll - so the tests now
run the ShaderSystem compiler-less over a CookedShaderPack of placeholder
bytes (the Null device accepts any bytecode) and run everywhere. Added the
per-shader version-isolation case (invalidating "other" leaves "forward"'s
pipeline alone and rebuilds only its own). 19 assertions, none skipped, on
clang, gcc, clang-shared. Six other test files still carry the DXC gate
(Shaders, Shaders.System x2, Shaders.Resource, Materials.Resource, Render
PipelineTests, Engine.Terrain renderer) - those DO compile, so the gate is
honest there; noted, not changed.

## Audit: Beef VG stage one; CPU-vs-shader gradient agreement pinned here too (2026-09-08)

Sedulous.VG stage one (c341a59): faithful - 10 of the 11 VG enums with no
enumerator drift (PathCommand is stage two), VGVertex, CornerRadii/
StrokeStyle, the whole fill family + GradientStop + ColorUtils; 28 tests.
Their sweep of Math.* corlib calls to Core's bare free functions is the
right convention. Adopted their test idea HERE: the GPU gradient path hands
the shader fill.GradientCoord and derives t per pixel (radial = length,
conic = frac(atan2/2pi), vg_grad_radial/conic.ps.hlsl) while GetParameterAt
is the CPU truth - nothing pinned that the two agree. VGStyleTests now
replays the shader math at five points for a radial fill and for a conic
fill at four start angles (compared modulo 1 at the wrap). 116 VG cases on
clang, gcc, clang-shared. Their note: the build box is deep in swap (a
clang++ link died with a bus error once) - same memory pressure we run
under; one build at a time stays the rule.

## Audit: Beef VG stage two; odd dash patterns follow the SVG rule (2026-09-08)

Sedulous.VG stage two (4d3bc08): faithful - CurveUtils, PathCommand/
PathSegment/PathIterator, Path, PathBuilder, PathFlattener, ShapeBuilder,
DashGenerator; all 11 VG enums now present with no drift; the two extras
(PointToLineDistanceSq, an allocation-free PathIterator struct) are theirs.
Their builder rules (a drawing command before any MoveTo implies one at the
origin; a Close before any move is a no-op) match ours. ONE Raptor defect
from their tests, fixed here: DashGenerator decided dash-vs-gap by the raw
pattern index, so an ODD-length pattern never alternated - [10] was "index 0
forever" = a solid line drawn in ten-unit pieces. SVG/CSS repeat an odd
stroke-dasharray to even length ([10] = ten on, ten off). The generator now
walks that doubled cycle (offset normalisation and the start search included;
even-length patterns are unchanged). Tests pin [10] over a 40-unit line and
[5,3,2] repeating as six. VG.SVG does not import stroke-dasharray yet; when
it does it can pass arrays through unchanged. 117 VG cases on clang, gcc,
clang-shared.

## Audit: Beef VG tessellation; geometry tests adopted here (2026-09-08)

Sedulous.VG tessellation (5bc9774): faithful - Triangulator, FillTessellator,
StrokeTessellator match method for method (their EmitFan/AddQuad are a
readability refactor of our open-coded fans and the nine repeated quad
patterns; same output). Raptor already carried the three degenerate guards
(fan fallback after a full pass with no ear, the miter limit, the
zero-length edge skip) but our tests only COUNTED triangles and vertices,
so none of them was pinned and an overlap or a gap would have passed.
Adopted their tests: the emitted triangles' signed area equals the polygon's
(L-shape 75; a 20x20 square with a 10x10 hole = 300 through the hole-merge
path), a bowtie yields a bounded fan with every index in range, a hairpin's
miter stays under the limit (nothing past 200 units on a 100-unit line), and
a zero-length edge produces no NaN. 121 VG cases on clang, gcc, clang-shared.

## Audit: Beef VG batch/clip/cache; PathCache keys by instance id, matches on every input (2026-09-08)

Sedulous.VG batch, clip and cache (432867d): faithful (155 VG tests). Their
one deliberate change was the right call and is our own written rule
(bind-group-cache-versioning: caches key by generation, never raw pointer):
Raptor's PathCache was HashMap<const Path*, CachedPath>, so a freed path's
address could serve its dead mesh to whatever landed there next. FIXED here:
Path mints an InstanceId (copy = new instance, move carries it, the
moved-from gets a fresh one) and the cache keys on it. The counter lives in
a NEW impl unit (VGImpl.cpp) - and ImageData's counter, which was a static in
its module INTERFACE (instantiated per shared library = two libraries can
mint the same id), moved to ImageDataImpl.cpp for the same reason.
The two behaviours they kept-and-documented are fixed here instead:
StrokeMatches compared width/cap/join only, so an animated dash offset (or
a changed pattern, miter limit or tolerance) was served the stale mesh -
every tessellation input is now part of the match (fill: + tolerance).
Eviction: a full cache settles AT capacity (unchanged) and SetCapacity now
trims to exactly the new capacity (it left capacity-1). PathCache has no
runtime caller yet (VGContext tessellates directly), so all of this was
latent. Tests: instance-id semantics, id-keyed entries (a copy is a third
entry), every stroke input in the match, eviction both ways. 125 VG cases;
gate: clang, gcc, clang-shared lanes rebuilt (Image interface changed),
VG + Image + shared-lib tripwire tests, ASAN on VG and Image.

## Audit: Beef VG core COMPLETE (VGContext); two context pins adopted (2026-09-08)

Sedulous.VG core is complete with VGContext (a38310d; 213 VG tests). The
context matches ours method for method. Of the four silent-rot behaviours
their tests name, Raptor already pinned two (the self-intersecting star
through the stencil under both rules; one content-keyed ramp LUT stable
across frames) and lacked two, now added: an evicted ramp is still a
readable 256x1 image during the frame that announces it (the one-frame hold
that keeps the renderer's keys from dangling mid-flight), and a plain draw
after a per-pixel gradient is back on texture 0 (the white passthrough)
and the Default draw mode. Both pass against the existing code. Note for
their SVG importer: Raptor's DashGenerator now applies the odd-pattern
doubling itself (2f65935c), so if their generator keeps the raw behaviour
the importer owes the doubling; matching the generator is the simpler
answer. 126 VG cases on clang, gcc, clang-shared.

## Audit: Beef VG.Renderer; dispatch decisions now testable headlessly here (2026-09-08)

Sedulous.VG.Renderer (7fb026a): faithful - VGRenderer matches method for
method (the two missing names are private pipeline-creation helpers), both
enums identical. All three ownership rules they name were already ours,
each with a test: evicted textures RETIRE for one frame per in-flight
frame; the cache keys on ImageData::InstanceId (address reuse detected);
external views are caller-owned and skipped by batch eviction. What Raptor
lacked was headless coverage of the DISPATCH decisions: our pixel probes
(VG.Backend.Tests) need a real backend, so on a bare machine the renderer's
per-command choices went unchecked. Added: VGRenderer::LastRenderStats()
(drawn / skipped counts from the last Render - a skipped command is one
whose pipeline was never built) and ONE headless test that drives every
blend mode x (plain, per-pixel radial, per-pixel conic), a stencil fill
under both rules unclipped and inside a clip path, and all three gradient
spreads through a Null render pass: with stencil pipelines + gradient
shaders nothing is skipped (the lazy blend variants build on first use);
without stencil pipelines exactly the stencil-phase commands are skipped
and the rest still draw. 10 renderer cases; gate: clang, gcc, clang-shared
lanes rebuilt (renderer interface changed), VG targets green, ASAN on the
renderer tests.

## Audit: Beef VG.SVG document model + parsers; rotate(a, cx, cy) fixed here (2026-09-08)

Sedulous.VG.SVG document model and the path/colour/transform parsers
(57c708e, 41 tests): faithful. A REAL Raptor defect from their tests,
reproduced and fixed: rotate(angle, cx, cy) composed its translate-rotate-
translate group as three successive pre-multiplications, and with row-
vector matrices (left operand applied first - what the translate/scale
list composition already relies on) that runs the group backwards: for
rotate(90, 50, 50) the centre landed at (-150, 50). The group is now ONE
factor T(-c) * R * T(+c) pre-multiplied onto the list. Every imported SVG
that rotates about a centre was misplaced before this. Pins added: the
centred rotate keeps its centre and swings a neighbour 90 degrees (and
composes with a translate), arc flags that run together with the next
number ("0 0110 10") parse, and the keyword green is (0,128,0) while lime
is full green. Raptor already pinned the implicit LineTo after a move and
both smooth-curve reflections. 36 SVG cases on clang, gcc, clang-shared.

## Audit gap closed: the Beef font family + three more libraries; cook versions paired with record bumps (2026-09-08)

The user caught a review gap: the Beef Fonts.TrueType port had shipped
WITHOUT TrueTypeFontService (they were adding it when asked), and my
surface/member audits had run over a hand-written pair list that covered
only the base Fonts library - the six font satellites, Model.FBX, the SDL3
shell (our Shell.Desktop) and the two Graphics splits were never audited.
Audited now, all faithful: every Raptor case in every font library is
covered (idiom: FontRect for our Rectangle, Detach for TakeOwnership, the
UTF-16 name read's any-language fallback), FBX welds by content, the SDL3
shell matches Shell.Desktop bar a private button map. The audit scripts now
derive the pair list from the Sedulous directory listing (every library
with source), never a hand list. One divergence to raise with them: their
cooked FontResource is ONE flat record of parallel lists where ours nests
entries/glyphs/kerning/regions.
Raptor fixes from the pass: the glyph bounds keys were abbreviated
(bbX/bbY/bbW/bbH) against the full-names rule - now boundsX/boundsY/
boundsWidth/boundsHeight, FontResource DataVersion 2, font cook 4. And a
gap in my own earlier commits: MaterialSource 3 and StaticMeshSource 5
changed cooked records without bumping their BUILDERS' cook Version(), so
products cooked under the old record version would have been refused at
load rather than re-cooked (the recipe hash folds the builder version, not
the record's). MaterialAssetBuilder and both mesh builders now carry cook
version 2. Rule recorded: a cooked record's DataVersion bump always pairs
with the builder's Version() bump. Gate: clang, gcc, clang-shared lanes
rebuilt; Fonts, Materials.Pipeline, Geometry.Pipeline, ModelImporter and
Pipeline.Cook tests green on all three.

## SVG loader skips unrecognised elements whole; Beef Render phase one reviewed at the logic level (2026-09-08)

A defect the Beef port found that Raptor shared, reproduced and fixed here:
the SVG loader's SkipTag consumed only an unrecognised element's OPENING
tag, so a <metadata>, <desc> or <style> container's children were parsed at
the current level and its closing tag ended that level - every sibling
after it was dropped, which is most of an editor-exported file (the old
loader kept 0 of 3 shapes in the new test). SkipTag now skips the whole
subtree by depth (quotes honoured inside tags, self-closing handled) and
steps over comments, CDATA and processing instructions as units. Test: an
export-shaped document with all of the above ahead of three shapes.
37 SVG cases on clang, gcc, clang-shared.
Beef VG.SVG loader + renderer, the VG sandbox, the TrueType font service,
the four cooked-record versions, the dash rule and the attribute comment:
all landed as asked.
Render phase one (Render.Api + the data contract, views, frame arena,
buffer pool, retire queue, uniform ring, dispatch seams, draw emission,
mesh LOD; 92 tests) reviewed to the logic standard: sort-key packing and
depth quantisation identical bit for bit; the Halton jitter identical; LOD
coverage/pick/hysteresis delegate to the same shared LOD math; GpuLight
(64) and GpuLocalShadow (96) match field for field; retire age =
framesInFlight + 1 on both; limits (16 shadow tiles, 64 entries, 16
probes), chunk sizes and the G-buffer formats (RG16Float normal +
velocity, RG8Unorm material) all match. Classes and the one API enum
match; the 4 unmatched Raptor cases belong to the orchestrators and
systems still to come, as does the AoMode enum. One nit: SortKeys uses
corlib Math.Clamp where the rest of the port uses Core's bare Clamp.

## Audit: Beef Render complete + PropertyAnimation, reviewed to the logic standard (2026-09-09)

Seven Render commits (shadows/clusters/first post passes; reflections,
bounce, exposure, decals, sprites; environment chain + probes; debug draw;
mesh renderer; forward passes + frame driver) and PropertyAnimation
(30 tests over our 9; classes + enum match).
Render, checked beyond names:
- Classes: every Raptor class present bar IRenderDataProvider (an
  interface artefact of the script); 6 of 7 enums match, the 7th is theirs.
- Frame driver: the 21 pass declarations run in EXACTLY Raptor's order.
- Numerics identical: sort-key packing + depth quantisation; Halton jitter;
  LOD delegation to the shared LOD math; cascade splits (near 0.1, shadow-
  distance floor, lambda 0.5, camera-far fraction); cluster grid (16-px
  tiles, 24 slices, log scale); retire age = frames in flight + 1; all 29
  ViewPostConfig defaults; every landed constant (37 same, the 8 "differ"
  are notation, the atlas 2048/512, capture 128, IBL env 256/5 mips/
  prefilter 256/BRDF 256 all match).
- GPU layouts: ViewData (33 fields, order + defaults), ObjectData 160,
  InstanceData 144, ShadowViewData 64, GpuLight 64, GpuLocalShadow 96,
  GpuProbe 64, DecalUniforms 240, SpriteInstance 96, SkyUniform, IblPush,
  BloomPush, TaaPush, PrefilterPush and ClusterBuildParams match field for
  field; the SSR/SSGI/AO push structs match the HLSL push blocks (the
  shared truth) exactly.
- Lifetime (the borrowed material): their MeshRenderer keys instances by
  material uid like ours but prunes by AGE (240 frames idle) instead of
  RefCount() <= 1, detaching bind group + uniform buffer into the retire
  queue; ReleaseInstance never dereferences the material, so the dangling
  borrow inside the window is never read. Acceptable, with that invariant
  now the thing to guard.
- The four Raptor pipeline behaviours the test cross-check could not pair
  all exist in code: parallel emit in ForwardPass behind the same 256
  threshold, SSGI declared into the scene-HDR chain, IBL contexts keyed per
  scene, mesh-cache Clear through a transfer batch.
THE FINDING (theirs to fix): their 149 Render tests are ALL data-layer
(arena, sort keys, LOD, shadow math, debug draw, registries, views,
resources, mesh layout). There is no test for the frame driver, the mesh
renderer, the cluster/shadow/IBL/probe systems or any post pass - Raptor's
15 null-device frame cases (one-cube view, instanced batching, parallel
emit, empty view, multi-scene shadows, probe ranges, per-scene IBL, unlit
material, prepass view matrix, both debug views, exposure slots, SSGI
chain, plus the two mesh-cache cases) have NO counterpart. The name
cross-check reported only 4 unmatched because it paired the rest with
unrelated names. Everything read above is right by inspection; nothing
pins it. No Raptor change this round.

## Beef Render: the frame-driver gap closed (2026-09-09)

Two commits answer the finding from the morning: the engine shaders (all
88 .hlsl/.hlsli byte-identical to Data/Shaders) and 29 tests that run the
frame on the Null device through a real ShaderSystem - RenderFrameTests
(6), RenderPostChainTests (6), DepthPrepassParityTests (3),
GpuMeshCacheTests (4), IblSystemTests (3), ReflectionProbeSystemTests (4),
RenderShadowCompositionTests (3). Every one of Raptor's 15 null-device
frame cases and both mesh-cache cases now has a counterpart (one-cube view
+ second-frame reuse, instanced batching, parallel emit, empty view, unlit
material, graph inventory, multi-scene shadows, probe ranges, per-scene IBL
with sky-change rebuild, prepass view parity, both debug views, exposure
slots, SSGI chain), plus four Raptor lacks: a tone map adding its own
resolve, the startup probe-recapture window draining, a probe keeping its
slice across frames, a scene with no caster binding no map. The fixture
gates on DXC + a shader root exactly as our PipelineTests do (parity, not
a divergence). Also landed: miniaudio-Beef (Audio is next).

## Audit: Beef PropertyAnimation.Resource + Audio (device-independent half) (2026-09-09)

PropertyAnimation.Resource: faithful - the cooked clip record's 15 wire keys
are identical, unversioned on both sides (plain RTTI_DEFINE_OBJECT here, a
bare attribute there), factory + service match; 7 tests over our 2.
Their two deliberate divergences are idiom: the binding resolver uses
Beef's language reflection instead of our hand-rolled tables, and
Sample returns a plain 4-float value where ours returns a Variant (Beef's
Variant heap-allocates above a machine word and sampling runs per track
per frame) - the consumer still switches on TrackValueKind, so the shape
holds.
Audio, the half that landed (clips, cues, the reverberator; 21 tests):
faithful - AudioAttenuationModel, AudioBus, AudioBusEffectKind enumerators
identical; the Freeverb tunings identical (comb 1116..1617, allpass
225/341/441/556, 0.015/0.28/0.84). The 11 unmatched Raptor cases are all
AudioEngine (steal policy, fade-then-reap, dying-list bound, per-scene
groups, streaming, distance low-pass, PlayMusic cross-fade, layout rebuild
keeping voices alive, master volume) plus the WAV encode->probe->decode
round-trip - none of which is ported yet; WAV support has no Beef file so
far and belongs with the engine half. miniaudio-Beef: a C handle surface
rather than mirrored config structs - the right call (a drifted mirror is
silent corruption). No Raptor change.

## Audit: Beef Audio complete (engine + codec + cooked half) (2026-09-09)

Five commits: the bus vocabulary, the codec seam (WAV encode/probe/decode +
waveform peaks), a miniaudio group-fade shim, the engine, the cooked half.
59 tests over our 31 in Audio, 12 in Audio.Resource; every behaviour queued
from the first half is pinned BY NAME: steal policy (lowest lower priority,
equal priority steals the farthest), the bounded dying list skipping silent
victims, dedupe window + opt-out, one-shot reap, stop fade-then-reap,
pause/resume fade, setters, stereo spatial downmix, StopAll, master volume
clamp/boost, cursor wrap, scene-group pause/stop/teardown incl. custom-bus
voices, distance filter glide, PlayMusic cross-fade + StopMusic, per-voice
reverb send handover on steal, streaming through the stream pool, layout
apply/named routing/rebuild keeps voices alive + re-homes orphans/degenerate
defuse/reverb splice; WAV round-trip, stereo downmix, refusals. Classes and
enums match (AudioBus keeps Count as a const inside the enum).
WIRE FINDING, a pattern now (third time after FontResource): their cooked
AudioBusLayoutSource is ONE flat record of parallel lists (busVolume[],
busEffectStart[]/Count[], effectKind[]...) with a BARE attribute (version 0)
where ours nests bus -> effects -> custom buses under DataVersion 2; their
SoundCueSource is lists (clipId[], weight[]) where ours is a count-prefixed
variant loop. AudioClipSource matches key for key. Cooked products are
per-engine today so nothing breaks, but the two engines now differ
structurally on three cooked records. DECISION for the user: do we want
cross-engine identical cooked formats (then flattening stops and these three
records take our nesting + versions), or is key/name parity the line (then
the attribute versions still must match ours: AudioBusLayoutSource 2)?
No Raptor change this round.

## Ruling: cross-engine cooked-product compatibility is not a goal (user 2026-09-09)

The flattened Beef records stand. What the audits keep checking per cooked
record from here: full-name keys, and a real version on any record whose
layout has ever changed (a bare attribute cannot refuse anything); the
number itself need not match ours.


## UI spec P0 built: LayoutStyle on View (2026-09-09)

Spec ui-layout-and-style-model.md section 1, phase P0, on all four lanes.
One `LayoutStyle` value per View (`Layout()` / `SetLayout()`), read by all six
layouts; the five `LayoutParams` classes, `CreateDefaultLayoutParams`, the
markup's parent-typed `RegisterLayout`/`RegisterLayoutParam`/`CreateLayoutParams`
machinery and the `:layout_params` partition are gone. 79 consumer files
rewritten mechanically (`MakeRef<...LayoutParams>(alloc)` -> `LayoutStyle lp;`,
field renames Grow/Shrink/X/Y/Row/Column -> FlexGrow/FlexShrink/Left/Top/
GridRow/GridColumn) then hand-fixed: FloatingPanel (drag/clamp through
SetLayout), the billboard placement in Engine.UI, the property-animation
timeline pane, Expander/Docking SetContent overloads. Grid auto-flow no longer
writes cells back into the child (intent survives reordering).
Markup attributes are the CSS names on ANY element: `flex-grow`, `flex-shrink`,
`align-self`, `gravity`, `dock`, `left`, `top`, `grid-row`, `grid-column`,
`grid-row-span`, `grid-column-span` (+ width/height/margin); `.sml` in the tree
used none of the old parent-typed spellings, so no data changed. Two behaviour
shifts, both intended: root-element width/height/margin now apply (they were
dropped), and the old spellings surface as unknown-attribute warnings.
Tests: UI.Tests +6 (keep-style / set-style / reparent-keeps-intent /
SetLayout damage / custom container from Layout() / grid auto-flow intent),
MarkupLoaderTests rewritten +4 (grid+absolute attrs, parent-independent
attrs, old spelling unknown, vocabulary == completion). Deviations from the
spec sketch are listed in the spec STATUS block (PascalCase fields, unknown
attributes stay warnings for now, self-only hit-test rule kept).
NEXT: P1 cascade + vars + units when asked.

## Audit: Beef Physics (world + cooking seam + cooked half) (2026-09-09)

Five commits (b1c078f..8c7c45a): a joltc C++ shim for the shape cooking seam
(save/restore/triangles over a byte blob + the hull-tolerance setter; vendored
joltc untouched, folded in by a root CMakeLists), two generated-binding fixes
(result structs lost `public`; 16 callback parameters were pointer-to-pointer),
PhysicsWorld.bf (~1,200 lines) + 15 vocabulary types, the cooked half. All 22
Raptor cases ported + 4 cooking-seam + a stale-character-handle case + 4 resource.
Logic read end to end against PhysicsWorldImpl.cpp: layer/broad-phase rules,
group matrix both ways, body creation (density/CCD/mass override/sensor), shape
building (scale wrapper, compound, heightfield offset/scale, plane), ray/point/
overlap/sweep (mask filter, dedupe, normal negation, mesh slot via leaf), contact
buffer (approach speed, End resolved by id through the no-lock interface), all
five joints (limits, motor limits, rope semantics, wake-by-id on destroy),
CharacterVirtual (supporting volume, step settings = Jolt defaults). Faithful.
Divergences ACCEPTED: dense layer encoding semantic*32+group (joltc pair filter
is a table; identical behaviour); query volumes heap-built per call (no
SetEmbedded through C; note for the auto-aim path); procs tables process-wide
and static (joltc stores the pointer - their stack-smash lesson).
ASKS (Beef): RayCast takes a second shared lock on the already-locked body via
JPH_BodyInterface_GetShape (use JPH_Body_GetShape on the locked body); SetJointMotor
wakes through the constraint's Body pointers (dangling after a body destroy -
mirror the Raptor fix below); dead cTempAllocatorBytes; shim header should say
save/restore cover LEAF shapes only (SaveBinaryState omits children/materials);
mirror the lone-shape rotation fix; QueryPoint fill semantics now match.
RAPTOR FIXES (this audit): SetJointMotor dereferenced the constraint's Body
pointers - heap-use-after-free once a connected body was destroyed before the
joint, and the subsystem syncs motors EVERY fixed step (ASAN-proven by the new
test, then fixed by waking through the slot's ids like DestroyJoint); QueryPoint
now FILLS (clears first) like ShapeOverlap; a lone ShapeDesc with a local
ROTATION was built as the bare shape (rotation dropped) - now a one-child
compound, ray-probed by a test. Physics.Tests 24 cases, clang/gcc/asan green.

## UI spec P1 built: cascade, selectors, variables, units (2026-09-09)

ui-layout-and-style-model.md section 3 on the clang lane (other lanes below).
Selector chains (descendant + `>`, `#id`, `:first-child/:last-child/:empty`,
`:active/:focus/:focus-visible` aliases, unknown types match nothing) with CSS
specificity; the cascade orders every matching rule by (specificity, source
order) across context sheet -> ancestors' local sheets -> inline sheet, last
wins per property, cached per view. CACHE KEY DESIGN (user question mid-build):
a first cut used one process-wide "style world version" - correct across
contexts but an edit in an embedded game context flushed the editor's caches.
Replaced with per-sheet versions summed along each view's own chain: contexts
with different themes never invalidate each other; an inline edit invalidates
one view. `inherit`/`initial`; text-dim-color + word-wrap join the inheritable
set. Custom properties (`--name`, typed by literal) + `var(--name, fallback)`
with nesting; palette prepended as a `View { --key }` rule; View::CustomProperty
is the custom-layout extension point. Unit = component sum (dp/px/pt/%/em) so
calc() is a value; % against the containing box and em against the computed
font size in View::Measure; markup `width="50%"`; sheets get Length values via
ResolveStyleLength. NOT built (recorded in the spec STATUS): `[attr=value]`,
`:selected`, sheet-driven layout properties (P2), theme-sheet em/% consumer
migration (P2). Tests: CascadeTests.cpp, 18 cases (3-rule specificity, source
order per property, combinators + backtracking, structure, aliases, unknown
type, 3-level inheritance, keywords, var + fallback + nesting + inheritance +
palette, units in sheets and markup, calc, cache invalidation by class / sheet
edit / reorder / local sheet / inline, two-context independence).

## Audit: Beef Animation + Navigation (the agreed sequence is complete) (2026-09-09)

Animation (95c55c4, 40e6dfd): skeleton, clips, sampler, player and the whole
graph stack read against ours - keyframe lookup, cubic spline (incl. the
shipped quaternion approximation), positive-modulo looping, the event span
rules (half-open, tail then wrapped head, dominant clip only in blend trees),
1D sorted insert + clamped ends + empty-slot fallbacks, 2D inverse-distance
folding with the running-average weight, transition pick (any-state, no
self-transition, exit time, lowest priority), trigger consumed after all
layers, override/additive layer combine, motion-vector matrix snapshot.
Faithful. Their two named decisions are right: `as` instead of the NodeType
tag (no -fno-rtti there), derived skeleton data rebuilt from the parents
(ours does the same in FillSkeleton). Cooked half: skeleton/clip/graph
records; the graph is flat pools with bounded runs (ruling: flattening is
their call). 46 + 6 tests cover all 40 of ours plus lookup clamps, stable
sort, child-before-parent, orphans, stop/pause, direct bone set, graph
rebuild, run-past-pool, wrong record type.
Navigation (d8f5fd0, 22cca82, cba202e): blob format byte-compatible (packed
= our natural layout), same grid derivation, same rcConfig, same stage
capture, same tile assembly, Patch/ReadGrid/Load/ReplaceTile/DebugTriangles
logic line for line, query and crowd identical, the crowd snapping through
the crowd's own query. The binding's bitfield fix ([Bitfield] on
rcSpan/rcCompactCell/rcCompactSpan) is correct against Recast.h. 16 + 3
tests; the parallel/serial identity test has nothing to compare (their bake
is serial; ParallelBake is accepted and ignored) - noted, not a gap.
ASKS (Beef): NavigationZoneSource is BARE while ours is RTTI version 2 (the
"real version on a record whose layout changed" rule - same pattern as
AudioBusLayoutSource); the skeleton name map is keyed by the bone's OWN
String (a rename after BuildNameMap leaves a stale hash - copy the key or
document); the stage-sample cap is global across tiles where ours is per
tile (diagnostics only; their bound is arguably better - say so in the
comment); ParallelBake accepted but unused although Sedulous.Core has JobSystem.ParallelFor
(already driving the DF font baker and ForwardPass) - WIRE it: per-tile result and stage
buffers baked in parallel, assembled row-major afterwards (a shared outStages appended
from workers would interleave non-deterministically and race the sample cap), and port
our parallel-vs-serial byte-identity test.
RAPTOR FIX (this audit): AnimationClipSource::FillClip indexed the keyframe
pool UNBOUNDED (trackStart/trackCount trusted, arrays not length-checked) -
a truncated or corrupt cooked clip read past the end. Now validates the
table and every run against the pool, returns false and leaves the clip
empty; the factory binds nothing. Test: run past the pool, short table,
exact-fit run, and the manager bind refusing. Their port had already
bounded this (AnimationResourceTests: ARunPastThePool...).
Their physics follow-up (85ba9f6) applied all six asks. Beef port sequence
complete: Scene, Input, Texture, Materials, VG, Fonts, Heightfield, Terrain,
RenderGraph, Render, PropertyAnimation, Audio, Physics, Animation,
Navigation. UI waits on the style model (P0/P1 built, P2-P4 next).

## Beef follow-ups verified (2026-09-09)

08a412b: the tiled bake runs across JobSystem.ParallelFor (grain 1, a pool per bake)
with a result buffer AND a stage buffer per tile, assembled row-major; stages
concatenate in order under a per-tile cap (independence) plus a bake-wide cap of
240k samples (deliberately tighter than ours, now said so). New test: parallel and
serial blobs byte-identical AND the captures equal point for point. 2bb7ff5:
AudioBusLayoutSource and NavigationZoneSource stamped `[Serializable(2)]`, matching
ours. 19a511d: the skeleton name map owns copied keys and frees them on clear and
destroy. All three read correct; nothing further to ask.

## Net: unification ruling + the DNS gap closed (2026-09-09)

Assessment (Documentation/Plans/net-unification.md): legacy Beef Sedulous.Net is a socket /
HTTP / WebSocket toolkit with DNS, IPv6 address types and blocking sleep loops; Raptor is the
full game stack (datagram seam + sim, bit wire, reliable transport, session, RPC, replication,
WS gateway, pump HTTP + SSE) with a thinner socket line (dotted quads only, no DNS). RULING:
Raptor is the foundation, the new Sedulous ports Net directly from Raptor. Gap filled here:
`Core::ResolveHostIPv4` on Linux (getaddrinfo) and Win32 (getaddrinfo under a Winsock ref),
literal fast path; `ResolveEndpoint`, `TcpSocket::Connect` and therefore `Net.connect`,
JoinServer and `HttpFetch` take a host NAME. Tests: localhost resolves to loopback for both
UDP endpoints and a TCP connect; `.invalid` names fail closed; empty host refused. IPv6 (the
u64 DatagramEndpoint) and TLS stay deferred. Win32 path written against the same shape as the
Linux one; the MSVC lane is the user's to run.

## UI style model P2 - box model (2026-09-09)

Spec section 2 built (Documentation/Specs/ui-layout-and-style-model.md, STATUS block has
the as-built list). LayoutStyle fields became `Declared<T>` so a sheet can fill what code
left unset (`width/height/margin/min-*/max-*/position/insets/z-index/flex-grow/flex-shrink/
align-self`; inline wins even at the default value; `View::Layout()` = effective,
`DeclaredLayout()` = inline; refreshed by SetLayout and at the top of Measure). New:
`Position::Absolute` in ANY container (six layouts + base ViewGroup skip `!IsInFlow`;
Measure/Layout in the base do shrink-to-fit inside the insets and far-edge anchoring),
z-index draw + hit order, `box-shadow` (a new VG draw mode: `VGContext::FillBoxShadow`,
`vg_shadow.ps.hlsl` rounded-box SDF x erf Gaussian, four quadrant quads; renderer
pipeline + hosts UIRuntime/UISubsystem/VGSandbox/GUISandbox pass the module), `overflow:
hidden`, min/max clamps (%/em too), `ResolveStyleFloat` resolving Length values (the
consumer migration deferred from P1 - theme em now reaches every control), Flex stretch
respecting a Fixed cross size. Deviations recorded in the spec: opacity stays per-vertex
(no VG layer), percent insets read 0, no VisualStyle object, borders/gradients/cursor
from sheets not built. Tests: VG.Tests (shadow geometry: quads/extent/coverage, inset,
zero blur), VG.Renderer.Tests (dispatch with/without the module), UI.Tests/BoxModelTests
(z draw + hit order, absolute in Flex / anchoring / pinning / no-grow, shadow bounds under
the child, shadow parsing, opacity composition, cascade-filled fields + inline wins,
min/max, sheet position/z/insets/overflow/flex-grow/align-self, markup vocabulary,
Declared semantics). Four lanes green. NEXT when asked: P3 transitions.

## Beef Image.Resource / Model.Resource / Particles / Json reviewed (2026-09-09)

Their report (08a412b..c02c189): Particles (SoA streams, 7 initializers, 13 behaviours,
simulator, effect + instance + sub-emitter routing, 54 tests), Particles.Resource (5),
Json (26). Two deliberate departures accepted: emitter position/velocity ride in the
update context instead of being pushed onto initializers (removes the two transient
fields our Serialize excludes by hand); CollisionBehavior serializes itself (their
generator cannot do fixed shape arrays). Registry ruling accepted: RegisterAll always
populates the global table (a custom registry would otherwise cook effects whose modules
never come back). Json: values own children, Clone is explicit (ours copies because
values cross into script VMs - fine either way).
RAPTOR FIXES from this review, both real:
- Particles: an unknown module type id was "skipped" - its parameters stayed in the
  positional binary stream and everything after shifted (their test allocated 4 GB from a
  garbage particle budget). They framed each module; we REFUSE instead (house rule: strict
  readers, no skipping): `SerializeInitializers/Behaviors` FailPayload(NotSupported) on a
  type the registry cannot construct or that is not the right module kind, and the system
  / effect loops stop reading at the first failure so no later budget is read from a
  shifted stream. Tests: clean round trip (control), patched type id refused with nothing
  read past it, wrong-kind type refused (Particles.Resource.Tests/
  ParticleEffectSerializerTests.cpp).
- Json: the number scan accepted `01` / `-01` / `007` (RFC 8259 forbids a leading zero);
  now refused, the zero forms `0 -0 0.5 0e2` still parse (Json.Tests).
Nothing to ask back.

## UI style model P3 - transitions (2026-09-09)

Spec section 4 built (STATUS block has the as-built list). `transition:` lists parse
(property | all, ms/s, five easings, delay, `none` = empty list); a view keeps per-property
entries `{from, to, clock}` that `ResolveStyle` overlays (`ComputeStyle` = cascade only);
`LerpStyleValue` interpolates colors/floats/lengths/thicknesses/shadows. The trigger is the
style-cache rebuild: when only the generation or the control state moved, the old cache is
read through the overlay first (so a hover-out mid-flight retargets from the CURRENT value,
no snap), then differing listed values start/retarget entries; sheet swaps move a new
context sheet epoch and never animate (stale rule pointers are never read - the UAF the
snapshot could have caused). `UIContext::BeginFrame` ticks listed views; damage per the
property's kind. State-aware backgrounds cross-fade through the now NON-virtual
`Drawable::Draw` (blend consumed once, `DrawState` is the override point; Layer/Inset/
StateList renamed). UA defaults: `SetStyleSheet` prepends `all 120ms ease-out` on the
interactive control types, themes override by source order. Tests
(UI.Tests/TransitionTests.cpp): parsing, lerp, class toggle animates only listed props,
duration 0 instant + no-change generation bump starts nothing, hover in/out retarget,
easing + delay, state cross-fade vertex alphas, layout-vs-visual damage, UA defaults +
theme override, sheet swap ends transitions, detach de-lists. Every existing battery
(Toolkit/Editor/Engine.UI) passes with the defaults on. Four lanes green. NEXT when asked:
P4 wrap/gap/ellipsis, then the visual iteration at the desk (import dialog + inspector).

## UI style model P4 - wrap, gap, ellipsis (2026-09-09)

Spec section 5 built; the spec's phases are complete. FlexLayout rewritten around a line
model: `Wrap` breaks items against the definite main size (first item always fits, half-pixel
tolerant re-break at arrange time), grow shares distribute PER LINE, `AlignContent`
(Start/End/Center/SpaceBetween/SpaceAround/Stretch, default Stretch) packs the lines; a single
nowrap line still fills the cross axis as before. Gaps on both axes: `Spacing`/`LineSpacing`
in code, CSS axis names `RowGap`/`ColumnGap` for markup `gap`/`row-gap`/`column-gap`, mapped by
Direction. `flex-basis` (LayoutStyle::FlexBasis, sheet + markup; %, em) - auto stays 0 for a
growing child so every existing grow test holds. `text-overflow: ellipsis` from the sheet
reaches Label (`EffectiveEllipsis`, property wins). Tests: UI.Tests/FlexWrapTests (line
count/positions, stacked measure with both gaps, vertical columns, all six align-content modes,
single-line ignore, axis gaps by direction, basis inline/sheet/without grow, markup vocabulary,
text-overflow) + Fonts.Tests/TextUtilTests (TruncateToWidth never exceeds the box, codepoint
aligned, carve-outs). Every pre-P4 flex test passes unchanged. Four lanes green. NEXT: the
visual iteration at the desk with the user (import dialog + inspector are the yardsticks);
the Beef port's UI item can start porting P0-P4.

## Beef Net / Http / WebSocket / Mcp / Replication / Manager reviewed (2026-09-09)

Their commits e9d3558..7f4efcf (126 files, ~11.8k lines) port the whole net line straight
from Raptor as ruled: Net (bit wire, transport seam + loopback, datagram sim, reliable UDP,
session, RPC, corlib socket backends; 47 tests), Http (parser, pump server, SSE, blocking
client; 17), Net.WebSocket (handshake, RFC 6455 codec, gateway, hybrid socket, native accept
only; 22), Mcp (JSON-RPC server, tools/resources, stdio + in-memory; 25), Mcp.Reflection
(type_list/type_info on Beef reflection; 10), Mcp.Http (4), Net.Replication (attribute mark,
offset codec, delta/relevancy/prefab spawn, interpolation; 15), Net.Manager (transport /
replication halves, factories, startup; 7). Every count is at or above ours and the case
names track ours. Spot-checked faithful: ReliableTransport rule for rule (SeqGreater, ack
bits, 0.9/0.1 RTT, 1.5x resend, 256 window), the RFC 3174/4648/6455 vectors, batch refusal,
202 for notifications, bearer on both routes, the layout harvest excluding unsupported types
(ours does too), NetworkManager's explicit destructor order. Accepted departures: script
facades not ported (by design), browser WebSocket client not ported (no web platform yet),
the replicated mark is an attribute, the field codec reads storage at its offset (no Variant
allocation per field per frame), Beef reflection replaces the type registry (no domain /
inPlayer keys), Tool/HostInfo own a context object (closures have no destructor).
RAPTOR FIX from their garbage-input test: the HTTP first-line parser let a request line
with only ONE separator ("GET /path", no version) through its bounds check, then read the
version from one past the line (a debug assert, UB in release). Requests now need the third
part; a response may still omit its reason phrase. Tests: "GET /path", "GET", a trailing
space, and a reason-less 204 that still parses. Their "quiet socket is not a closed one"
fix was their own port's bug: ours already returns 0 for would-block and -1 for closed.
Nothing to ask back. Beef port status: everything but UI is ported; UI can start from the
completed style model (P0-P4).

## UI style model: desk check (2026-09-09)

User ran UISandbox and the editor on the P0-P4 build (LayoutStyle, cascade, box model,
transitions with the UA hover defaults on, flex line model): no regressions found. The
style model is CLOSED as a track; the visual iteration on the import dialog and the
inspector is a design pass to schedule on its own, not a blocker.

## Transitions: motion moved into the themes (2026-09-09)

User ruling on the P3 user-agent defaults: the engine must never hard-code a list of
control types. Removed the `kInteractive` prepend from `UIContext::SetStyleSheet` and the
sheet flag with it; each shipped theme's `View` defaults block now declares the motion
(`transition:` on background/text/border/accent colors, box-shadow, opacity - 120ms
ease-out, geometry excluded), and a control opts out with `transition: none`. To make a
theme-wide rule cheap the trigger no longer resolves every listed property on a cache
rebuild: it keeps the old winners array and starts an entry only where the winning RULE
pointer changed (same rule + same sheets = same value); `from` is a running entry's
current value, else the old rule's value, else the inherited value; a child without a rule
inherits the parent's mid-flight value through the overlay. Tests: theme-data rule reaches
Button and leaf, `none` opts a type out, the dark theme carries the rule, inheritance
mid-flight, unchanged winners start nothing. Four lanes green.

## Beef UI line reviewed + the Raptor fixes it surfaced (2026-09-12)

Their 136 commits since 7f4efcf port the whole UI line: UI proper (every subsystem and
control, the markup loader, the five themes with the legacy builders), UI.Toolkit (bars,
property grid, colour/vector editors, curve canvas + timeline, floating panel, docking, node
graph, code editor, theme extension), Gamekit, Runtime host, Shell bridge, Viewport, VFS,
Resource, Application, and the sixteen-tab UISandbox on screen - plus a leak sweep under
LeakSanitizer across the earlier libs. Our own suites drove it (P0-P4 included: the
cascade, box model, transitions and flex-wrap cases all green on their side). New porting
method: Raptor source is copied into the tree under RaptorCode/ and STRUCK in the same
commit as its Beef port; Foundation is done except what is not being ported (Script, Luau,
Mcp.Script, Engine.UI.Script struck as such); the agent has started the Engine layer.
Notes addressed to Raptor, each verified in our code and FIXED here:
- QuaternionField's Euler decomposition put the arcsine on X; the forward composition is
  Z-Y-X so the middle axis (Y) takes it - compound rotations read back wrong and were then
  rewritten on the first keystroke. Fixed; helpers made public; round-trip test over six
  triples plus the control's three fields.
- GridView rebound every visible cell per layout pass and never pruned a positional
  selection when the data set shrank - both fixes ListView already carried. Mirrored, with
  the bind-count and prune cases.
- StyleSelector matched a compound state with ANY bit (`:hover:checked` fired on hover
  alone) while its doc said all flags. Now `HasAllFlags`; test with a state-injecting view.
- DockLayout deflated by the chrome but added back only the padding: a bordered dock
  measured short by the border. Fixed with a test.
- Timeline was never registered for selectors, so the toolkit sheets' Timeline block
  styled nothing and the hand-kept type count could not notice. Registered; the count is
  gone; a test asserts no rule of either fragment is UnknownType and that Timeline is styled.
- VGRenderer::Variant fell back to the flat pipeline when a BoxShadow blend variant failed
  to build (flat colour over everything around the box); it now skips. No test: the
  failure is not reachable headlessly.
- Suites mirrored that Raptor lacked: DragDropTests (7: potential/threshold, quiet press,
  enter/over/leave/drop, rejecting target, Escape, no data, mid-drag teardown) and
  TooltipTests (7: delays, leave, ancestor owner keeps the clock, provider first, no focus,
  interactive hit-testing, deleted target). Writing the tooltip suite found a Raptor gap:
  hit-test visibility is self-only, so a non-interactive tooltip's CONTENT still caught the
  pointer; the tooltip now disables interaction for its subtree unless interactive.
Left as they are, noted: ScrollView's axis asymmetry (vertical unbounded unless Never,
horizontal only when Always) is the intended document shape; UndoStack's `i32&` outputs
and RemoveView's advisory deleteChild flag are spelling, not behaviour; the sandbox tree
rows' hard-coded indent is cosmetic. ContentButton / DrawableView measure cases they added
are still ours to mirror.
UI.Tests 827, UI.Toolkit.Tests 237. Four lanes green.
