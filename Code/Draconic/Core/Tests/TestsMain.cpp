// Draconic::Core tests — doctest entry point. The only TU that defines main;
// all other *Tests.cpp files just register cases.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
