// Draconic::FontsDF - the `draconic.fonts.df` module.
//
// Runtime distance-field font atlas type: pure data, no msdfgen dependency.
// The import-time baker lives in a separate library (draconic.fonts.df.baker).

export module draconic.fonts.df;

export import :atlas;
