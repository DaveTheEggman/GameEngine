# AudioPlayground - audio consumer proof: an ambient Music-bus loop + four pitched 3D
# emitters (components, autoplay, listener-on-camera) + click-to-fire positional
# one-shots, demonstrating attenuation/pan/pitch/doppler on the miniaudio-backed
# subsystem. The tiny WAV assets are generated by Data/generate_audio.py (committed).
add_executable(AudioPlayground main.cpp)

target_link_libraries(AudioPlayground PRIVATE
    Foundation::Core
    Foundation::Runtime
    Foundation::Runtime.Client
    Foundation::Shell.Desktop Foundation::Runtime.Desktop
    Foundation::Graphics
    Foundation::Graphics.Gpu
    Engine::DefaultApp
    Foundation::Scene
    Engine::Scene
    Engine::Render
    Foundation::Audio Engine::Audio
    Extensions::Imgui
    Foundation::Policy)
util_copy_sdl3_dll(AudioPlayground)
